<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>「BUAA-OS」 葵OS典</title>
      <link href="/2026/06/22/OS%20lab0/"/>
      <url>/2026/06/22/OS%20lab0/</url>
      
        <content type="html"><![CDATA[<h2 id="初识OS"><a href="#初识OS" class="headerlink" title="初识OS"></a>初识OS</h2><p>操作系统是计算机专业一门非常重要的必修课，同时也是很多骚操作的基础，在将来找工作面视时也是绕不开的一环，其重要程度不亚于计组，希望诸位能好好学习。操作系统分为理论和实验两部分，理论部分如果觉得课程组提供的PPT难以下咽的话可以参考王道408。实验部分主要有两类：</p><ul><li>每次只做exam就跑路：课下代码填空可以不求甚解，只要把pre-exam掌握就可以通关课上exam</li><li>希望尝试extra：课下代码最好参考pre-extra考察板块详细理解，最起码搞明白某些关键变量的含义以及函数功能，在考试时调用做到相对熟练。</li></ul><p>个人认为最理想的学习方式是弄懂整个操作系统的运作流程，不强求知道每一行代码的作用，但是必须要能梳理清楚典型功能例如<code>内存管理</code>，<code>文件管理</code>等的函数调用逻辑。</p><p>当然实验的核心素养还在于<code>C</code>语言的熟练<code>coding</code>,建议大一程序设计没学好的同学巩固一下。</p><p>本片博客的创作原因是实在看不懂指导书，所以就用AI尽可能把所有代码梳理了一遍，形成了一版较为<strong>浅显易懂</strong>（可能吧）的个人心得体会，偶尔会包含上机实验的些许灵感。读者可以把他当作一个辅助理解来使用，同时请保持批判性思维，欢迎指出表述有歧义或是不准确的地方，感激不尽。</p><h2 id="lab-0"><a href="#lab-0" class="headerlink" title="lab 0"></a>lab 0</h2><p>lab0 的主要内容是 Linux 命令行指令的熟悉。可以参见<a href="https://cordial-kid.github.io/2026/03/03/Linux%E6%8C%87%E4%BB%A4/">这篇博客</a>。以下是上机考试的题目，大家可以当作练手。</p><h3 id="lab0-exam"><a href="#lab0-exam" class="headerlink" title="lab0-exam"></a>lab0-exam</h3><p><strong>题目要求：</strong></p><ul><li>casegen：编译生成输入生成程序 casegen;</li><li>ver1: 使用共同依赖，以及依赖 calc1.c， 编译生成程序 ver1;</li><li>ver2: 使用共同依赖，以及依赖 calc2.c， 编译生成程序 ver2;</li><li>all: 编译生成上述所有程序 casegen, ver1, ver2;</li><li>run: 先执行 casegen 获取输入， 再分别执行 ver1, ver2 获取输出;</li><li>clean: 清理编译好的可执行文件 casegen, ver1, ver2，以及运行过程中产生的所有后缀为 .txt 的临时文件。</li></ul><p><strong>思路分析：</strong></p><p>我们观察到 ver1 和 ver2 都是使用共同依赖加上各自的依赖来编译生成程序。因此共同依赖可以提取出来 <code>COMMON_FILE = main.c post_calc.c common.h</code>，ver1 和 ver2 就可以写成：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs makefile"><span class="hljs-section">ver1: <span class="hljs-variable">$(COMMON_FILE)</span> calc1.c </span><br>        gcc calc1.c post_calc.c main.c -o ver1<br></code></pre></td></tr></table></figure><p>别忘了变量引用时要加上 <code>$</code> 符号。接下来比较困难的就是<strong>run</strong>。执行带参数的程序时要在读懂题意的基础上合理地传参，综上所述，最后的 Makefile 如下：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs makefile"><span class="hljs-keyword">include</span> makefile.inc<br><span class="hljs-meta"><span class="hljs-keyword">.PHONY</span>: clean run</span><br><br>COMMON_FILE = main.c post_calc.c common.h<br><br><span class="hljs-section">run: casegen ver1 ver2</span><br>        ./casegen <span class="hljs-variable">$(CASE_NUM)</span> <span class="hljs-variable">$(INPUT_FILE)</span><br>        ./ver1 <span class="hljs-variable">$(INPUT_FILE)</span><br>        ./ver2 <span class="hljs-variable">$(INPUT_FILE)</span><br><br><span class="hljs-comment"># TIPS: casegen, instead of casegen.c , is one of the dependencies, why?</span><br><span class="hljs-section">all: casegen ver1 ver2</span><br><br><span class="hljs-section">ver1: <span class="hljs-variable">$(COMMON_FILE)</span> calc1.c </span><br>        gcc calc1.c post_calc.c main.c -o ver1<br><br><span class="hljs-section">ver2: calc2.c <span class="hljs-variable">$(COMMON_FILE)</span></span><br>        gcc calc2.c post_calc.c main.c -o ver2<br><br><span class="hljs-section">casegen: casegen.c </span><br>        gcc casegen.c -o casegen<br><br><span class="hljs-section">clean:</span><br>        rm -rf casegen ver1 ver2 *.txt<br></code></pre></td></tr></table></figure><h3 id="lab0-extra"><a href="#lab0-extra" class="headerlink" title="lab0-extra"></a>lab0-extra</h3><p>题目要求我们从 0 开始写一个 .sh 脚本来实现要求的功能。我们可以逐条分析题目要求。<strong>注意：本题一定要善用路径变量</strong>，否则你会很痛苦。</p><ol><li>如果日期没有给出，就打印<code>analyze.sh: No date provided</code>到标准输出，并立即退出。如果日期给出则向下分析。此处的难点是参数个数统计，bash中可以通过<code>$#</code>来获取参数个数。于是我们可以写：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-keyword">if</span> [ <span class="hljs-variable">$#</span> -eq 0 ]<br><span class="hljs-keyword">then</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;analyze.sh: No date provided&quot;</span><br><span class="hljs-keyword">else</span><br>    <span class="hljs-comment"># 继续分析</span><br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure><ol start="2"><li>新建报告根目录 reports ，与日志根目录同级，报告均保存在以对应日期命名的子目录中。新建所有目录的权限为 rwxrwxr-x ，所有普通文件的权限为 rw-rw-r– 。我们根据要求新建目录并设置权限：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">REPORTS_DIR=<span class="hljs-string">&quot;reports&quot;</span><br>DST_DIR=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;REPORTS_DIR&#125;</span>/<span class="hljs-variable">$&#123;DATE&#125;</span>&quot;</span><br>LOGS_DIR=<span class="hljs-string">&quot;logs&quot;</span><br>SRC_DIR=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;LOGS_DIR&#125;</span>/<span class="hljs-variable">$&#123;DATE&#125;</span>&quot;</span><br><br><span class="hljs-built_in">mkdir</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;REPORTS_DIR&#125;</span>&quot;</span><br><span class="hljs-built_in">chmod</span> 775 <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;REPORTS_DIR&#125;</span>&quot;</span><br><span class="hljs-built_in">mkdir</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;DST_DIR&#125;</span>&quot;</span><br><span class="hljs-built_in">chmod</span> 775 <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;DST_DIR&#125;</span>&quot;</span><br></code></pre></td></tr></table></figure><ol start="3"><li>在指定日期的错误日志中检查是否存在含有 ERROR 字样的记录，如果有则将这类记录筛选出来，按原顺序保存到相应报告子目录下的 error.log ，否则不要创建此文件。我们查找 ERROR 字样可以使用 <code>grep</code> 命令。那么怎么保留记录呢，我们可以使用字符串变量 <code>errors=$(grep &quot;ERROR&quot; &quot;$&#123;error_src&#125;&quot;)</code>,并判断字符串是否为空来决定是否创建 error.log 文件：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">error_src=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;SRC_DIR&#125;</span>/error.log&quot;</span><br>error_dst=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;DST_DIR&#125;</span>/error.log&quot;</span><br><br>errors=$(grep <span class="hljs-string">&quot;ERROR&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;error_src&#125;</span>&quot;</span>)<br><span class="hljs-keyword">if</span> [ -n <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;errors&#125;</span>&quot;</span> ]<br><span class="hljs-keyword">then</span><br>    <span class="hljs-built_in">touch</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;error_dst&#125;</span>&quot;</span><br>    <span class="hljs-built_in">chmod</span> 664 <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;error_dst&#125;</span>&quot;</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;errors&#125;</span>&quot;</span> &gt; <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;error_dst&#125;</span>&quot;</span><br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure><ol start="4"><li>在指定日期的访问日志中将 &#x2F;127.0.0.1 字样全部替换为 &#x2F;localhost ，其它内容保持不变，全文保存到相应报告子目录下的 access.log ，不要更改原文件。这当然是sed的主场了：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">access_src=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;SRC_DIR&#125;</span>/access.log&quot;</span><br>access_dst=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;DST_DIR&#125;</span>/access.log&quot;</span><br><br><span class="hljs-built_in">touch</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;access_dst&#125;</span>&quot;</span><br><span class="hljs-built_in">chmod</span> 664 <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;access_dst&#125;</span>&quot;</span><br>sed <span class="hljs-string">&#x27;s/\/127.0.0.1/\/localhost/g&#x27;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;access_src&#125;</span>&quot;</span> &gt; <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;access_dst&#125;</span>&quot;</span><br></code></pre></td></tr></table></figure><ol start="5"><li>在指定日期的访问日志中统计每个 IP地址 的总请求次数，并将结果按访问次数从高到低排序，保存到相应报告子目录下的 summary.txt ，访问次数相同时的排序不做要求，每行格式如下：<code>IP地址 总访问次数</code>，不要更改原文件。我们可以使用 <code>awk</code> 来统计 IP 地址的访问情况，结合给出的sort和uniq来实现本题：</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">summary_dst=<span class="hljs-string">&quot;<span class="hljs-variable">$&#123;DST_DIR&#125;</span>/summary.txt&quot;</span><br><span class="hljs-built_in">touch</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;summary_dst&#125;</span>&quot;</span><br><span class="hljs-built_in">chmod</span> 664 <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;summary_dst&#125;</span>&quot;</span><br>awk <span class="hljs-string">&#x27;&#123;print $1&#125;&#x27;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;access_src&#125;</span>&quot;</span> | <span class="hljs-built_in">sort</span> | <span class="hljs-built_in">uniq</span> -c | <span class="hljs-built_in">sort</span> -r -n | awk <span class="hljs-string">&#x27;&#123;print $2, $1&#125;&#x27;</span> &gt; <span class="hljs-string">&quot;<span class="hljs-variable">$&#123;summary_dst&#125;</span>&quot;</span><br></code></pre></td></tr></table></figure><p>解释一下最后一条指令：</p><ul><li><code>awk &#39;&#123;print $1&#125;&#39; &quot;$&#123;access_src&#125;&quot;</code>：提取访问日志中的第一列（IP地址）。</li><li><code>sort</code>：对提取的IP地址进行排序，以便后续统计。</li><li><code>uniq -c</code>：统计每个唯一IP地址的出现次数，输出格式为“次数 IP地址”。</li><li><code>sort -r -n</code>：根据访问次数进行降序排序。</li><li><code>awk &#39;&#123;print $2, $1&#125;&#39;</code>：调整输出格式为“IP地址 总访问次数”，并将结果保存到 summary.txt 文件中。</li><li><code>&gt; &quot;$&#123;summary_dst&#125;&quot;</code>：将最终结果写入 summary.txt 文件中。</li></ul><h2 id="lab-1"><a href="#lab-1" class="headerlink" title="lab 1"></a>lab 1</h2><h3 id="lab1课下实验"><a href="#lab1课下实验" class="headerlink" title="lab1课下实验"></a>lab1课下实验</h3><p>lab1的主题是<strong>内核，启动和输出</strong>，即让MOS的最小内核能够被编译，链接，加载到QEMU中运行，并完成最基本的内核输出能力。具体分为以下几个板块：</p><h4 id="操作系统的启动"><a href="#操作系统的启动" class="headerlink" title="操作系统的启动"></a>操作系统的启动</h4><p>我们的实验代码是在<code>Linux</code>环境中写的，但MOS是运行在MIPS平台上的，所以需要通过<code>交叉编译</code>生成MIPS可执行文件，再交给QEMU模拟的硬件环境运行</p><h4 id="ELF的启动"><a href="#ELF的启动" class="headerlink" title="ELF的启动"></a>ELF的启动</h4><p>MOS内核最终会被编译成一个ELF格式的文件，然后又QEMU加载到内存中运行。ELF主要由五部分组成：</p><ul><li>ELF头：包括程序的基本信息，例如段头表和节头表的相对文件的偏移量</li><li>段头表&#x2F;程序头表：记录各个segment信息，用于运行时加载</li><li>节头表：记录各个section的信息，用于编译和链接</li><li>段头表表项：说明某个段应该被加载到内存的什么地方</li><li>节头表表项：说明代码段，数据段等节的信息，用于链接段。</li></ul><h4 id="理解MIPS布局"><a href="#理解MIPS布局" class="headerlink" title="理解MIPS布局"></a>理解MIPS布局</h4><p>MIPS的虚拟地址空间分布如图：</p><p><img src="/images%5CMIPS%E8%99%9A%E6%8B%9F%E5%9C%B0%E5%9D%80%E7%A9%BA%E9%97%B4.png" alt="MIPS虚拟地址空间"></p><ul><li><code>kuseg</code>:需要TLB进行虚拟地址和物理地址的转换；对这段地址的存取都会经过cache。</li><li><code>kseg0</code>:不需要TLB进行虚拟地址和物理地址的转换；对这段地址的存取都会经过cache。</li><li><code>kseg1</code>:不需要TLB进行虚拟地址和物理地址的转换；对这段地址的存取不会经过cache。</li><li><code>kseg2</code>:需要TLB进行虚拟地址和物理地址的转换；对这段地址的存取都会经过cache。</li></ul><p>内核刚启动时，TLB还没有配置好，所以内核不能放在需要TLB地址转换的区域，所以MOS的内核<code>.text</code>,<code>.bss</code>,<code>.data</code>段会被放到<code>kseg0</code>中。</p><h4 id="从-start启动MOS"><a href="#从-start启动MOS" class="headerlink" title="从_start启动MOS"></a>从_start启动MOS</h4><p><code>_start</code>是内核函数的入口，写在汇编文件<code>init/Start.s</code>中，他的作用是做早期的初始化，然后跳转到C语言函数——<code>mips_init()</code>。整体调用流程如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">QEMU / Bootloader<br>      ↓<br>加载内核 ELF<br>      ↓<br>跳转到 _start<br>      ↓<br>设置必要的启动环境<br>      ↓<br>调用 mips_init()<br>      ↓<br>MOS 内核开始执行<br></code></pre></td></tr></table></figure><h4 id="实现printk"><a href="#实现printk" class="headerlink" title="实现printk"></a>实现printk</h4><p>printk就是内核的printf，用于在QEMU控制台里输出调试信息，直接写C语言就好。</p><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>通过上述描述，我们可以大概得知lab1的主要内容：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">Makefile 负责构建<br>    ↓<br>ELF 描述内核文件结构<br>    ↓<br>Linker Script 决定内核放到哪里、从哪里开始执行<br>    ↓<br>MIPS 内存布局解释为什么要这样放<br>    ↓<br>_start 完成最早期启动<br>    ↓<br>mips_init 表示内核 C 代码开始运行<br>    ↓<br>printk 提供后续调试输出能力<br></code></pre></td></tr></table></figure><h3 id="lab1课上考试"><a href="#lab1课上考试" class="headerlink" title="lab1课上考试"></a>lab1课上考试</h3><p>lab1实验主要是启动和printf的实现。往届的exam一般是要求实现一个简单的printf函数，extra则要求你在对print的机理有更深入的掌握和理解的基础上实现相关功能。2026年春季学期进行了改革，exam考察<strong>readelf</strong>文件的理解，extra则沿袭往届的考察习惯。</p><h4 id="lab1-exam"><a href="#lab1-exam" class="headerlink" title="lab1-exam"></a>lab1-exam</h4><p>本题用到的关键变量如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">Elf64_Off e_shoff;      <span class="hljs-comment">/* 节头表在文件中的偏移 */</span><br>Elf64_Half e_shnum;     <span class="hljs-comment">/* 节头表表项数量 */</span><br>Elf64_Half e_shstrndx;  <span class="hljs-comment">/* 节名称字符串表对应的节头下标 */</span><br><br>Elf64_Word sh_name;   <span class="hljs-comment">/* 节名在节名称字符串表（.shstrtab）中的偏移,切记是在字符串表中的偏移 */</span><br>Elf64_Xword sh_size;  <span class="hljs-comment">/* 节总大小 */</span><br>Elf64_Off sh_offset;  <span class="hljs-comment">/* 该节在文件中的偏移 */</span><br></code></pre></td></tr></table></figure><p>解题步骤如下：</p><ol><li>首先我们要设置好文件指针<code>const ELF64_Ehdr *ehdr = (ELF64_Ehdr*)binary;</code></li><li>获取节头表的地址<code>const ELF64_Shdr *sh_table = (ELF64_Shdr*)((char*)binary + ehdr-&gt;e_shoff);</code>。这里一定要注意以下指针类型的转换，e_shoff是<strong>步数</strong>，**(char*)**是步幅，只有在正确的步幅下步数才有意义。</li><li>获取<strong>节名称字符串表的节头</strong>， <code>const ELF64_Shdr *shstrShdr = (const ELF64_Shdr*)(sh_table + ehdr-&gt;e_shstrndx);</code>这里步幅是<strong>节头表项的大小</strong>，步数是节名称字符串表对应的节头下标.</li><li>获取<strong>节名称字符串表的内容字符串</strong>，<code>const char* shstrtab = (char*)binary + shstrShdr-&gt;sh_offset;</code>这里步幅是<strong>字节</strong>，步数是节名称字符串表在文件中的偏移。</li></ol><p>走到这里工作已经完成一大半了，我们总结以下之前提到的易于混淆的概念:</p><table><thead><tr><th>名称</th><th>类比</th><th>计算方式</th></tr></thead><tbody><tr><td>节头表</td><td>目录索引</td><td>文件首地址+节头表在文件中的偏移</td></tr><tr><td>节名称字符串表</td><td>特定页码</td><td>节头表地址+节名称字符串表对应的节头下标*节头表项的大小</td></tr><tr><td>节名称字符串表的内容字符串</td><td>某一页的具体文字</td><td>文件首地址+节名称字符串表在文件中的偏移（从求得的节名称字符串表获取）</td></tr></tbody></table><ol start="5"><li>获取<strong>节名称</strong>，遍历节头表，获取每个节名称在节名称字符串表中的偏移<code>sh_name</code>，并通过偏移获取节名称字符串表中的节名称<code>shstrtab + sh_name</code>，判断是否为<code>.symtab</code>，如果是则将<strong>matchSymtab</strong>标记为1.这里注意一下<strong>节名称字符串表</strong>的形式是<code>&quot;\0.text\0.data\0.bss\0.symtab\0.strtab\0.shstrtab\0...&quot;</code>所以在比较的时候可以放心strcmp.</li></ol><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; section_count; i++) &#123;<br>section_name = shstrtab + sh_table[i].sh_name;<br><span class="hljs-type">int</span> matchSymtab = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">if</span> (<span class="hljs-built_in">strcmp</span>(<span class="hljs-string">&quot;.symtab&quot;</span>, section_name) == <span class="hljs-number">0</span>) &#123;<br>matchSymtab = <span class="hljs-number">1</span>;<br>&#125;<br><span class="hljs-keyword">if</span> (matchSymtab == <span class="hljs-number">1</span>) &#123;<br>symtabIndex = i;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>至此，本题基本结束。难点在于概念的理解和区分，以及指针类型转换的正确使用。还是挺容易把自己绕进去的。</p><h4 id="lab1-extra"><a href="#lab1-extra" class="headerlink" title="lab1-extra"></a>lab1-extra</h4><p>题目要求实现 <strong>scanf</strong>的功能，类比 <strong>printf</strong>实现。</p><p>改动涉及的文件有 <code>include/print.h</code>, <code>include/printk.h</code>, <code>kernel/printk.c</code> ,<code>lib/print.c</code>。我们重点放在<code>lib/print.c</code>上，其他文件的改动主要是函数申明和照抄。</p><p>考察形式主要是代码补全，课程组还贴心地为我们提供了思路！</p><h4 id="c"><a href="#c" class="headerlink" title="%c"></a>%c</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">scan_c</span><span class="hljs-params">(<span class="hljs-type">scan_callback_t</span> in, <span class="hljs-type">void</span> *data, <span class="hljs-type">char</span> *ch, <span class="hljs-type">int</span> *ch_valid, <span class="hljs-type">char</span> *cp)</span> &#123;<br>    <span class="hljs-comment">/* ---------------------------------- */</span><br>    <span class="hljs-comment">/* Lab 1-extra: Your code here. (2/4)  */</span><br>    <span class="hljs-comment">/* 提示：</span><br><span class="hljs-comment">       1. 使用辅助函数确保缓冲区里有字符（注意：%c 绝不能跳过空白符！）</span><br><span class="hljs-comment">       2. 将读取到的字符存入 cp 指向的内存。</span><br><span class="hljs-comment">       3. 核心：因为该字符已被 %c 实体吃掉，必须将 缓存标志位 置为无效(值为0)。</span><br><span class="hljs-comment">    */</span><br>    <span class="hljs-comment">/* ---------------------------------- */</span><br>    ensure_char(in, data, ch, ch_valid);<br>    *cp = *ch;                <br>    *ch_valid = <span class="hljs-number">0</span>; <span class="hljs-comment">// 标志位置为无效</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这里重点是知道<strong>ch</strong>和<strong>ch_valid</strong>分别是什么，只要你不懵逼，做这个题手拿把掐。<strong>ch</strong>是读取到的字符，<strong>ch_valid</strong>是标志位。</p><h4 id="s"><a href="#s" class="headerlink" title="%s"></a>%s</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">scan_s</span><span class="hljs-params">(<span class="hljs-type">scan_callback_t</span> in, <span class="hljs-type">void</span> *data, <span class="hljs-type">char</span> *ch, <span class="hljs-type">int</span> *ch_valid, <span class="hljs-type">char</span> *cp)</span> &#123;<br>    <span class="hljs-comment">/* ---------------------------------- */</span><br>    <span class="hljs-comment">/* Lab 1-extra: Your code here. (3/4)  */</span><br>    <span class="hljs-comment">/* 提示：</span><br><span class="hljs-comment">       1. 使用辅助函数跳过所有的前导空白符。</span><br><span class="hljs-comment">       2. 连续读取非空白字符并存入 cp(可以在循环内部使用 *cp++ = *ch 进行赋值)。</span><br><span class="hljs-comment">       3. 遇到空白符或 &#x27;\0&#x27; 时停止读取。</span><br><span class="hljs-comment">       4. 别忘了在字符串末尾添加 &#x27;\0&#x27; 封口。</span><br><span class="hljs-comment">       5. 停下时：ch 里必须正好握着导致停止的“空白符”，且保持 缓存标志位 置为有效(值为1)。</span><br><span class="hljs-comment">    */</span><br>    <span class="hljs-comment">/* ---------------------------------- */</span><br>    skip_whitespace(in, data, ch, ch_valid);<br>    <span class="hljs-keyword">while</span>(*ch != <span class="hljs-string">&#x27; &#x27;</span> &amp;&amp; *ch != <span class="hljs-string">&#x27;\t&#x27;</span> &amp;&amp; *ch != <span class="hljs-string">&#x27;\n&#x27;</span> &amp;&amp; *ch != <span class="hljs-string">&#x27;\r&#x27;</span> &amp;&amp; *ch != <span class="hljs-string">&#x27;\0&#x27;</span>) &#123;<br>        *cp++ = *ch;<br>        in(data, ch, <span class="hljs-number">1</span>); <span class="hljs-comment">// 继续读取下一个字符</span><br>    &#125;<br>    *cp = <span class="hljs-string">&#x27;\0&#x27;</span>; <span class="hljs-comment">// 添加字符串结束符</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这里我们要对<strong>in</strong>函数有一定理解，<strong>in</strong>是回调函数，作为<code>vscanfmt</code>的参数，它的作用是读取下一个字符并更新<strong>ch</strong>,但并不会改变<strong>ch_valid</strong>，所以我们在循环内部调用它来继续读取下一个字符。并且不需要画蛇添足地去令<strong>ch_valid &#x3D; 1</strong>，因为题目已经明确了当我们遇到空白符或’\0’时停止读取，此时<strong>ch</strong>里正好握着导致停止的“空白符”，且保持缓存标志位置为有效(值为1)，所以我们只需要关注<strong>ch</strong>的值即可。</p><h4 id="u"><a href="#u" class="headerlink" title="%u"></a>%u</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">scan_u</span><span class="hljs-params">(<span class="hljs-type">scan_callback_t</span> in, <span class="hljs-type">void</span> *data, <span class="hljs-type">char</span> *ch, <span class="hljs-type">int</span> *ch_valid, <span class="hljs-type">int</span> *ip)</span> &#123;<br>    <span class="hljs-comment">/* ---------------------------------- */</span><br>    <span class="hljs-comment">/* Lab 1-extra: Your code here. (4/4)  */</span><br>    <span class="hljs-comment">/* 提示：</span><br><span class="hljs-comment">       1. 定义变量用于累加数字计算结果。</span><br><span class="hljs-comment">       2. 使用辅助函数跳过所有的前导空白符。</span><br><span class="hljs-comment">       3. 兼容可选的前导 &#x27;+&#x27; 号（至多1个）（如果有，调用 in() 越过它）。</span><br><span class="hljs-comment">       4. 如果遇到的第一个非空白字符就不是数字或 `+`，直接赋值为 `0` 并结束。</span><br><span class="hljs-comment">       5. 连续读取数字字符（&#x27;0&#x27;-&#x27;9&#x27;），并计算十进制数值。</span><br><span class="hljs-comment">       6. 遇到非数字字符时停止读取，并将结果存入ip。</span><br><span class="hljs-comment">       7. 停下时：ch里必须正好握着导致停止的“非数字字符”，且保持缓存标志位置为有效(值为1)。</span><br><span class="hljs-comment">    */</span><br>    <span class="hljs-comment">/* ---------------------------------- */</span><br>    <span class="hljs-type">int</span> result = <span class="hljs-number">0</span>;<br>    skip_whitespace(in, data, ch, ch_valid);<br>    <span class="hljs-keyword">if</span> (*ch == <span class="hljs-string">&#x27;+&#x27;</span>) &#123;<br>        in(data, ch, <span class="hljs-number">1</span>); <span class="hljs-comment">// 越过 &#x27;+&#x27;</span><br>    &#125;<br>    <span class="hljs-keyword">if</span> (*ch &lt; <span class="hljs-string">&#x27;0&#x27;</span> || *ch &gt; <span class="hljs-string">&#x27;9&#x27;</span>) &#123;<br>        *ip = <span class="hljs-number">0</span>; <span class="hljs-comment">// 第一个非空白字符不是数字或 &#x27;+&#x27;</span><br>        <span class="hljs-keyword">return</span>;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-keyword">while</span> (*ch &gt;= <span class="hljs-string">&#x27;0&#x27;</span> &amp;&amp; *ch &lt;= <span class="hljs-string">&#x27;9&#x27;</span>) &#123;<br>            result = result * <span class="hljs-number">10</span> + (*ch - <span class="hljs-string">&#x27;0&#x27;</span>);<br>            in(data, ch, <span class="hljs-number">1</span>); <span class="hljs-comment">// 继续读取下一个字符</span><br>        &#125;<br>        *ip = result;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里没什么好说的，str转int是程序设计的基本功了。</p><h2 id="lab-2"><a href="#lab-2" class="headerlink" title="lab 2"></a>lab 2</h2><p>这里当时没来得及写，大家脑补一下吧(bushi😥</p><h2 id="lab3"><a href="#lab3" class="headerlink" title="lab3"></a>lab3</h2><p>lab3的核心问题是进程创建和进程调度，我们以这两个问题为主线来学习lab3的内容。</p><h3 id="进程创建"><a href="#进程创建" class="headerlink" title="进程创建"></a>进程创建</h3><p>进程创建发生在内核态，因此我们的主要工作集中在<code>kern/env.c</code>文件中。</p><h4 id="env-init-全局环境初始化"><a href="#env-init-全局环境初始化" class="headerlink" title="env_init():全局环境初始化"></a>env_init():全局环境初始化</h4><p><code>env_init()</code>函数的主要任务是搭建整个进程管理的全局数据结构。包括初始化链表(env_free_list, env_sched_list),填充空闲链表。唯一不太好理解的在于<strong>构建模板页目录</strong>。通过初始化一个全局的模板页目录，并将操作系统中的(物理页管理数组pages和进程控制块数组evns)映射到模板页目录中，后续在创建新进程时可以直接复制这个模板页目录，避免了每次创建新进程都要重新构建页目录的开销。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 分配并初始化页目录本身</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Page</span> *<span class="hljs-title">p</span>;</span><br>panic_on(page_alloc(&amp;p));<br>p-&gt;pp_ref++;<br>base_pgdir = (Pde *)page2kva(p);<br><span class="hljs-comment">// 映射pages数组和envs数组到模板页目录中</span><br>map_segment(base_pgdir, <span class="hljs-number">0</span>, PADDR(pages), UPAGES,<br>        ROUND(npage * <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">struct</span> Page), PAGE_SIZE), PTE_G);<br>map_segment(base_pgdir, <span class="hljs-number">0</span>, PADDR(envs), UENVS, ROUND(NENV * <span class="hljs-keyword">sizeof</span>(<span class="hljs-keyword">struct</span> Env), PAGE_SIZE),<br>        PTE_G);<br></code></pre></td></tr></table></figure><h4 id="env-alloc-分配并初始化进程块"><a href="#env-alloc-分配并初始化进程块" class="headerlink" title="env_alloc():分配并初始化进程块"></a>env_alloc():分配并初始化进程块</h4><ol><li>分配PCB空槽：从env_free_list中分配一个空槽，若无空槽则返回错误。</li><li>初始化地址空间(env_setup_vm):为新进程分配页目录，并将内核空间映射到新进程的地址空间中。</li><li>分配标识符(mkenvid, asid_alloc(),):为新进程分配一个唯一的标识符,同时申请一个唯一的ASID，用于TLB管理。</li><li>配置初始上下文：设置cp0_status, 初始化栈指针寄存器</li></ol><h4 id="load-icode-加载ELF可执行程序"><a href="#load-icode-加载ELF可执行程序" class="headerlink" title="load_icode():加载ELF可执行程序"></a>load_icode():加载ELF可执行程序</h4><p>有了进程的躯壳和独立地址空间之后，需要把存放在内存中的ELF格式的应用程序可执行代码加载进去。</p><ol><li>解析ELF头</li><li>遍历段并分配物理内存(elf_load_seg)：扫描ELF的程序头表，针对每个可加载的段，利用系统提供的内存分配函数为其分配物理内存，并将ELF文件中的代码和数据通过page_insert()映射到新进程的地址空间中。</li><li>设置入口点：将ELF头中的入口地址设置到新进程的程序计数器寄存器中，以便进程启动时能够正确执行。</li></ol><p>**env_create()**：统筹调用env_alloc()和load_icode()，完成一个新进程的创建，并将其加入就绪队列中，等待调度器调度执行。</p><h3 id="进程调度"><a href="#进程调度" class="headerlink" title="进程调度"></a>进程调度</h3><p>进程调度也发生在内核态，关键在于选择下一个要运行的进程，并切换到该进程的上下文，我们主要关注<code>kern/sched.c</code>和<code>kern/env.c</code>中的相关函数。</p><p>首先要明确调度不会凭空发生，必须通过异常或中断陷入内核态才能触发，最常见的有两种：时钟中断（被动），主动让出（主动）。在当前实验中，我们主要通过时钟中断来触发调度。</p><h4 id="时钟中断发生，内核保存现场并调用sched-yield"><a href="#时钟中断发生，内核保存现场并调用sched-yield" class="headerlink" title="时钟中断发生，内核保存现场并调用sched_yield()"></a>时钟中断发生，内核保存现场并调用sched_yield()</h4><p>当时钟中断发生时，内核会自动保存当前进程的上下文（寄存器状态等），然后调用<code>sched_yield()</code>函数来选择下一个要运行的进程。</p><h4 id="schedule-选择下一个要运行的进程"><a href="#schedule-选择下一个要运行的进程" class="headerlink" title="schedule():选择下一个要运行的进程"></a>schedule():选择下一个要运行的进程</h4><p>将刚刚被中断的进程（如果还活着）放入就绪队列，从就绪队列中选出下一个状态为<code>ENV_RUNNABLE</code>的进程作为新进程。</p><h4 id="env-run-切换到新进程"><a href="#env-run-切换到新进程" class="headerlink" title="env_run():切换到新进程"></a>env_run():切换到新进程</h4><ol><li>将全局指针<code>curenv</code>指向新进程。</li><li>更新新进程的状态为<code>ENV_RUNNING</code>，并增加运行次数。</li><li>切换页目录：通知CPU使用新进程的页目录，以便新进程能够访问自己的地址空间。</li><li>切换上下文并退出内核：通过<code>env_pop_tf()</code>函数将新进程的寄存器状态恢复到CPU寄存器中，最终跳转到新进程的入口地址开始执行。</li></ol><h3 id="2025-exam"><a href="#2025-exam" class="headerlink" title="2025-exam"></a>2025-exam</h3><p>exam主要考察的是新的调度算法的实现。重点修改<code>kern/sched.c/schedule</code>和<code>kern/env.c/env_create_**</code>文件即可。这里给大家提个小tip：<strong>不用看调度算法的说明，直接照着代码提示实现即可</strong></p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// env_create_edf</span><br><span class="hljs-keyword">struct</span> Env *<span class="hljs-title function_">env_create_edf</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">void</span> *binary, <span class="hljs-type">size_t</span> size, <span class="hljs-type">int</span> runtime, <span class="hljs-type">int</span> period)</span> &#123;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Env</span> *<span class="hljs-title">e</span>;</span><br><br>    env_alloc(&amp;e, <span class="hljs-number">0</span>);<br>    e-&gt;env_edf_runtime = runtime;<br>    e-&gt;env_edf_period = period;<br>    e-&gt;env_period_deadline = <span class="hljs-number">0</span>; <span class="hljs-comment">// 初始化为 0，使进程在首次调用 schedule 函数时触发条件判断，进入首个运行周期</span><br>    e-&gt;env_status = ENV_RUNNABLE;<br>    <span class="hljs-comment">// !!!一定要记得初始化相关字段要全面，并且初值正确！务必务必！</span><br><br>    load_icode(e, binary, size);<br>    LIST_INSERT_HEAD(&amp;env_edf_sched_list, e, env_edf_sched_link);<br>    <span class="hljs-keyword">return</span> e;<br>&#125;<br><span class="hljs-type">void</span> <span class="hljs-title function_">schedule</span><span class="hljs-params">(<span class="hljs-type">int</span> yield)</span> &#123;<br>    <span class="hljs-type">static</span> <span class="hljs-type">int</span> clock = <span class="hljs-number">-1</span>; <span class="hljs-comment">// 当前时间片，从 0 开始计数</span><br>    clock++;<br>    <span class="hljs-type">static</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Env</span> *<span class="hljs-title">last_env</span> =</span> <span class="hljs-literal">NULL</span>; <span class="hljs-comment">// 上一个被调度的进程</span><br><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Env</span> *<span class="hljs-title">e</span>;</span><br>    LIST_FOREACH(env, &amp;env_edf_sched_list, env_edf_sched_link) &#123;<br>        <span class="hljs-keyword">if</span> (clock == e-&gt;env_period_deadline) &#123;<br>            e-&gt;env_period_deadline += e-&gt;env_edf_period; <span class="hljs-comment">// 更新下一个周期的截止时间</span><br>            e-&gt;env_runtime_left = e-&gt;env_edf_runtime; <span class="hljs-comment">// 重置剩余运行时间</span><br>        &#125;<br>    &#125;<br><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Env</span> *<span class="hljs-title">selected_env</span> =</span> <span class="hljs-literal">NULL</span>;<br>    LIST_FOREACH(e, &amp;env_edf_sched_list, env_edf_sched_link) &#123;<br>        <span class="hljs-keyword">if</span> (e-&gt;env_runtime_left &gt; <span class="hljs-number">0</span>) &#123; <span class="hljs-comment">// 只考虑剩余运行时间大于 0 的进程</span><br>            <span class="hljs-keyword">if</span> (selected_env == <span class="hljs-literal">NULL</span> || <br>                e-&gt;env_period_deadline &lt; selected_env-&gt;env_period_deadline || <br>                (e-&gt;env_period_deadline == selected_env-&gt;env_period_deadline &amp;&amp; e-&gt;env_id &lt; selected_env-&gt;env_id)) &#123;<br>                selected_env = e; <span class="hljs-comment">// 更新选中的进程</span><br>            &#125;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (selected_env != <span class="hljs-literal">NULL</span>) &#123;<br>        selected_env-&gt;env_runtime_left--; <span class="hljs-comment">// 调度前先减少剩余运行时间</span><br>        env_run(selected_env); <span class="hljs-comment">// 调度选中的进程</span><br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-type">static</span> <span class="hljs-type">int</span> count = <span class="hljs-number">0</span>; <span class="hljs-comment">// remaining time slices of current env</span><br>        <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Env</span> *<span class="hljs-title">e</span> =</span> last_env; <span class="hljs-comment">// 请根据提示修改这行代码</span><br><br>        <span class="hljs-keyword">if</span> (yield || count == <span class="hljs-number">0</span> || e == <span class="hljs-literal">NULL</span> || e-&gt;env_status != ENV_RUNNABLE) &#123;<br>            <span class="hljs-comment">// 当前进程还活着并且仍然可以运行</span><br>            <span class="hljs-keyword">if</span> (e &amp;&amp; e-&gt;env_status == ENV_RUNNABLE) &#123;<br>                TAILQ_REMOVE(&amp;env_sched_list, e, env_sched_link);<br>                TAILQ_INSERT_TAIL(&amp;env_sched_list, e, env_sched_link);<br>            &#125;<br>            e = TAILQ_FIRST(&amp;env_sched_list);<br>            <span class="hljs-keyword">if</span> (e == <span class="hljs-literal">NULL</span>) &#123;<br>                panic(<span class="hljs-string">&quot;schedule: no runnable envs\n&quot;</span>);<br>            &#125;<br>            count = e-&gt;env_pri; <span class="hljs-comment">// 时间片重置</span><br>        &#125;<br>        count--;<br>        last_env = e;<br>        env_run(e);<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>exam几乎都是这么个模式，先根据提示实现新的调度算法的核心逻辑，然后在最后调用之前实现的RR算法。<br>需要注意: </p><ol><li><em><em>struct Env <em>e &#x3D; last_env;</em></em> :这里不用<code>curenv</code>的原因是RR调度器要维护自己的历史状态。EDF进程运行时不能污染RR的</em>*last_env**。只有真正进入RR分支并调度了RR进程，才更新last_env。（这一点在题目中有说明）。</li><li>**selected_env-&gt;env_runtime_left–;<strong>和</strong>count–;**都要在env_run()之前执行，只不过是两个调度器各自维护自己的时间片计数器，互不干扰。</li><li>**static struct Env *last_env &#x3D; NULL;**需要初始化为NULL，并且加上static修饰。</li><li>**static int count &#x3D; 0;**需要初始化为0，并且加上<code>static</code>修饰。</li></ol><p><code>static</code>修饰的变量只在函数第一次被调用时初始化一次，<code>count</code>要保存<strong>当前正在运行的进程还剩几个时间片</strong>，而<code>last_env</code>要保存<strong>当前进程的上一个被调度的进程</strong>。如果不加static修饰，这两个变量在每次进程被调度时都会被重新初始化，从而无法正确维护状态。</p><h2 id="lab4"><a href="#lab4" class="headerlink" title="lab4"></a>lab4</h2><p>lab4主要涉及三个模块：系统调用，IPC通信，fork。</p><h3 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h3><p>我们以<code>sys_mem_alloc</code>为例，说明它从用户态进入内核、分发、执行、返回用户态的完整流程。在这过程中大家要持续关注当前所处的是用户态还是内核态，这对理解操作系统的运行机制有很大帮助。</p><h4 id="用户态封装"><a href="#用户态封装" class="headerlink" title="用户态封装"></a>用户态封装</h4><p>首先我们明确用户程序不会直接调用内核函数<code>sys_mem_alloc</code>,而是调用用户库里(<code>user/lib/syscall_lib.c</code>)的封装函数:</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">syscall_mem_alloc</span><span class="hljs-params">(u_int envid, <span class="hljs-type">void</span> *va, u_int perm)</span> &#123;<br>    <span class="hljs-keyword">return</span> msyscall(SYS_mem_alloc, envid, va, perm);<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="执行syscall指令"><a href="#执行syscall指令" class="headerlink" title="执行syscall指令"></a>执行syscall指令</h4><p>其中<code>SYS_mem_alloc</code>表示系统调用号，定义在<code>include/syscall.h</code>中。<code>msyscall</code>是一个汇编函数，在<code>user/lib/syscall_wrap.S</code>中实现：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs assembly">syscall<br>jr ra<br></code></pre></td></tr></table></figure><p>按照MIPS的约定，在进入<code>msyscall</code>之前，系统调用号和参数已经被放在了寄存器<code>a0</code>到<code>a3</code>中。具体效果图如下：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">$a0 = SYS_mem_alloc<br>$a1 = envid<br>$a2 = va<br>$a3 = perm<br></code></pre></td></tr></table></figure><p>如果参数超过四个，那么剩余的参数会被放在用户栈上。<code>*((int *)sp + n)</code> 表示第n个参数。<br><code>syscall</code>指令触发MIPS异常，CPU从用户态切换到内核态，将CP0中的<code>Cause</code>寄存器的<code>ExcCode</code>设置为8(系统调用异常)，同时CPU强制将PC(程序计数器)跳转到操作系统的异常统一处理入口<code>0x80000180</code>，而我们在<code>kernel.lds</code>设置了：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs assembly">. = 0x80000180;<br>.exc_gen_entry : &#123;<br>    *(.text.exc_gen_entry)<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="异常入口保存现场"><a href="#异常入口保存现场" class="headerlink" title="异常入口保存现场"></a>异常入口保存现场</h4><p>这意味着此时的内核入口不是<code>sys_mem_alloc</code>，而是通用异常处理入口<code>exc_gen_entry</code>，它位于<code>kern/entry.S</code>中：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs assembly">exc_gen_entry:<br>    SAVE_ALL<br>    mfc0    t0, CP0_STATUS<br>    and     t0, t0, ~(STATUS_UM | STATUS_EXL | STATUS_IE)<br>    mtc0    t0, CP0_STATUS<br>    mfc0    t0, CP0_CAUSE<br>    andi    t0, 0x7c<br>    lw      t0, exception_handlers(t0)<br>    jr      t0    <br></code></pre></td></tr></table></figure><p><code>SAVE_ALL</code>把用户态的寄存器保存到内核栈上，防止内核态的函数调用修改了用户态的寄存器值。接下来我们把<code>CP0_STATUS</code>寄存器的<code>UM</code>、<code>EXL</code>、<code>IE</code>位清零，进入内核态后禁止中断。然后从<code>CP0_CAUSE</code>寄存器中获取异常代码，内核根据异常代码选择处理函数。<code>exception_handlers</code>是一个数组，存储了所有异常类型对应的处理函数地址,位于<code>kern/traps.c</code>中。对于系统调用异常，<code>ExcCode</code>为8，所以我们会跳转到<code>exception_handlers[8]</code>，也就是<code>handle_sys</code>函数：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> (*exception_handlers[<span class="hljs-number">32</span>])(<span class="hljs-type">void</span>) = &#123;<br>    [<span class="hljs-number">0</span> ... <span class="hljs-number">31</span>] = handle_reserved,<br>    [<span class="hljs-number">0</span>] = handle_int,<br>    [<span class="hljs-number">2</span> ... <span class="hljs-number">3</span>] = handle_tlb,<br><span class="hljs-meta">#<span class="hljs-keyword">if</span> !defined(LAB) || LAB &gt;= 4</span><br>    [<span class="hljs-number">1</span>] = handle_mod,<br>    [<span class="hljs-number">8</span>] = handle_sys,<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br>&#125;;<br></code></pre></td></tr></table></figure><h4 id="handle-sys调用do-syscall"><a href="#handle-sys调用do-syscall" class="headerlink" title="handle_sys调用do syscall"></a>handle_sys调用do syscall</h4><p><code>handle_sys</code>位于<code>kern/genex.c</code>中：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">.macro BUILD_HANDLER exception handler<br><span class="hljs-title function_">NESTED</span><span class="hljs-params">(handle_\exception, TF_SIZE + <span class="hljs-number">8</span>, zero)</span><br>    move    a0, sp<br>    addiu   sp, sp, -8<br>    jal     \handler<br>    addiu   sp, sp, 8<br>    j       ret_from_exception<br><span class="hljs-title function_">END</span><span class="hljs-params">(handle_\exception)</span><br>.endm<br><br>BUILD_HANDLER sys do_syscall<br></code></pre></td></tr></table></figure><p>把当前<code>Trapframe</code>指针放入<code>a0</code>寄存器中(刚刚保存好的用户现场)，执行<code>do_syscall</code>函数，处理系统调用。再处理完后执行<code>ret_from_exception</code>函数返回用户态：</p><p>其中<code>do_syscall</code>位于<code>kern/syscall_all.c</code>中：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">do_syscall</span><span class="hljs-params">(<span class="hljs-keyword">struct</span> Trapframe *tf)</span> &#123;<br>    <span class="hljs-type">int</span> (*func)(u_int, u_int, u_int, u_int, u_int);<br>    <span class="hljs-type">int</span> sysno = tf-&gt;regs[<span class="hljs-number">4</span>];     <span class="hljs-comment">// $a0</span><br>    <span class="hljs-keyword">if</span> (sysno &lt; <span class="hljs-number">0</span> || sysno &gt;= MAX_SYSNO) &#123;<br>        tf-&gt;regs[<span class="hljs-number">2</span>] = -E_NO_SYS;     <span class="hljs-comment">// $v0</span><br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br>    tf -&gt; cp0_epc += <span class="hljs-number">4</span>;<br>    func = syscall_table[sysno];<br>    u_int arg1 = tf-&gt;regs[<span class="hljs-number">5</span>];<br>    u_int arg2 = tf-&gt;regs[<span class="hljs-number">6</span>];<br>    u_int arg3 = tf-&gt;regs[<span class="hljs-number">7</span>];<br><br>    u_int arg4, arg5;<br>    arg4 = *((u_int *)tf -&gt; regs[<span class="hljs-number">29</span>] + <span class="hljs-number">4</span>); <span class="hljs-comment">// $sp + 16</span><br>    arg5 = *((u_int *)tf -&gt; regs[<span class="hljs-number">29</span>] + <span class="hljs-number">5</span>); <span class="hljs-comment">// $sp + 20</span><br>    tf-&gt;regs[<span class="hljs-number">2</span>] = func(arg1, arg2, arg3, arg4, arg5);<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="恢复用户态"><a href="#恢复用户态" class="headerlink" title="恢复用户态"></a>恢复用户态</h4><p>在<code>tf-&gt;regs[2]</code>写入返回值之后，汇编入口执行以下命令返回用户态：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs assembly">FEXPORT(ret_from_exception)<br>    RESTORE_ALL<br>    eret<br></code></pre></td></tr></table></figure><p><code>RESTORE_ALL</code> 从 <code>Trapframe</code> 恢复寄存器，<code>eret</code> 根据 <code>cp0_epc</code> 返回用户态。<br>至此，系统调用全流程已经实现。</p><h3 id="IPC通信"><a href="#IPC通信" class="headerlink" title="IPC通信"></a>IPC通信</h3><p>IPC是一种同步消息传递机制，发送者和接收者必须同时存在才能完成通信。接收方首先调用<code>ipc_recv</code>进入等待状态，发送方调用<code>ipc_send</code>发送消息，并把接收方变成可运行。接下来我们具体分析。</p><p>在<code>user/lib/ipc.c</code>中，用户程序调用<code>ipc_recv</code>函数：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">u_int <span class="hljs-title function_">ipc_recv</span><span class="hljs-params">(u_int *whom, <span class="hljs-type">void</span> *dstva, u_int *perm)</span> &#123;<br>    <span class="hljs-type">int</span> r = syscall_ipc_recv(dstva);<br>    <span class="hljs-keyword">if</span> (r != <span class="hljs-number">0</span>) &#123;<br>        user_panic(<span class="hljs-string">&quot;syscall_ipc_recv err: %d&quot;</span>, r);<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (whom) &#123;<br>        *whom = env-&gt;env_ipc_from;<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (perm) &#123;<br>        *perm = env-&gt;env_ipc_perm;<br>    &#125;<br><br>    <span class="hljs-keyword">return</span> env-&gt;env_ipc_value;<br>&#125;<br></code></pre></td></tr></table></figure><p>在用户态调用<code>syscall_ipc_recv</code>，进入内核态后调用<code>sys_ipc_recv</code>。这正是之前提到的系统调用模块的内容。</p><p>依旧在<code>user/lib/ipc.c</code>中，发送方调用<code>ipc_send</code>函数，在进入内核之后调用<code>sys_ipc_try_send</code>。</p><p>当<code>sys_ipc_try_send</code>函数返回0之后，接收方被放回调度队列，等待被调度器调度执行。接收方被调度器选中执行时会从阻塞的<code>sys_ipc_recv</code>函数中返回。</p><p>这里有一个很有意思的事情：</p><blockquote><p>由于 <strong>sys_ipc_recv</strong> 阻塞前已经设置 **((struct Trapframe *)KSTACKTOP - 1)-&gt;regs[2] &#x3D; 0;<strong>。所以接收方用户看到的是 <strong>syscall_ipc_recv(dstva) &#x3D;&#x3D; 0</strong>,于是</strong>ipc_recv**函数继续执行，返回发送方的envid、权限和消息值。</p></blockquote><h3 id="fork"><a href="#fork" class="headerlink" title="fork"></a>fork</h3><p><code>fork()</code>要创建一个子进程，使他看起来和父进程一样拥有相同的地址空间，寄存器状态等。我们在<code>user/lib/fork.c</code>中实现了<code>fork()</code>函数：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">fork</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span> &#123;<br>    u_int child;<br>    u_int i;<br><br>    <span class="hljs-keyword">if</span> (env-&gt;env_user_tlb_mod_entry != (u_int)cow_entry) &#123;<br>        try(syscall_set_tlb_mod_entry(<span class="hljs-number">0</span>, cow_entry));<br>    &#125;<br><br>    child = syscall_exofork();<br>    <span class="hljs-keyword">if</span> (child == <span class="hljs-number">0</span>) &#123;<br>        env = envs + ENVX(syscall_getenvid());<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    &#125;<br>    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; PDX(UXSTACKTOP); i++) &#123;<br>        <span class="hljs-keyword">if</span> (vpd[i] &amp; PTE_V) &#123;<br>            <span class="hljs-keyword">for</span> (u_int j = <span class="hljs-number">0</span>; j &lt; PAGE_SIZE / <span class="hljs-keyword">sizeof</span>(Pte); j++) &#123;<br>                u_long va = (i * (PAGE_SIZE / <span class="hljs-keyword">sizeof</span>(Pte)) + j) &lt;&lt; PGSHIFT;<br>                <span class="hljs-keyword">if</span> (va &gt;= USTACKTOP) &#123;<br>                    <span class="hljs-keyword">break</span>;<br>                &#125;<br>                <span class="hljs-keyword">if</span> (vpt[VPN(va)] &amp; PTE_V) &#123;<br>     duppage(child, VPN(va));<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>    syscall_set_tlb_mod_entry(child, cow_entry);<br>    syscall_set_env_status(child, ENV_RUNNABLE);<br><br>    <span class="hljs-keyword">return</span> child;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="注册TLB-Mod-异常处理函数"><a href="#注册TLB-Mod-异常处理函数" class="headerlink" title="注册TLB Mod 异常处理函数"></a>注册TLB Mod 异常处理函数</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">if</span> (env-&gt;env_user_tlb_mod_entry != (u_int)cow_entry) &#123;<br>        try(syscall_set_tlb_mod_entry(<span class="hljs-number">0</span>, cow_entry));<br>    &#125;<br></code></pre></td></tr></table></figure><p><code>fork()</code>首先调用了<code>syscall_set_tlb_mod_entry</code>,这个函数主要作用是为指定的进程(envid)注册一个<strong>用户态</strong>的TLB Mod异常处理程序的入口地址。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">sys_set_tlb_mod_entry</span><span class="hljs-params">(u_int envid, u_int func)</span> &#123;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Env</span> *<span class="hljs-title">env</span>;</span><br>    try(envid2env(envid, &amp;env, <span class="hljs-number">1</span>));<br>    env-&gt;env_user_tlb_mod_entry = func;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>异常处理程序入口<code>cow_entry</code>位于<code>user/lib/fork.c</code>，是用户态下的写时复制异常处理程序。内核的 <code>do_tlb_mod</code> 会把异常现场放到用户异常栈上，然后跳到该函数，</p><h4 id="创建子进程"><a href="#创建子进程" class="headerlink" title="创建子进程"></a>创建子进程</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">child = syscall_exofork();<br>    <span class="hljs-keyword">if</span> (child == <span class="hljs-number">0</span>) &#123;<br>        env = envs + ENVX(syscall_getenvid());<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    &#125;<br></code></pre></td></tr></table></figure><p>用户态下调用<code>syscall_exofork</code>,内核态调用<code>sys_exofork</code>,这一步只做<code>进程控制块和寄存器状态</code>的复制，不复制内存。并且遵循以下原则：</p><blockquote><p>父进程中 fork 返回 child envid;子进程中 fork 返回 0。</p></blockquote><p>由于子进程刚创建时，用户态的 env 变量还是从父进程地址空间继承来的，需要修正为指向自己的 Env 结构。</p><h4 id="复制地址空间映射"><a href="#复制地址空间映射" class="headerlink" title="复制地址空间映射"></a>复制地址空间映射</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; PDX(UXSTACKTOP); i++) &#123;<br>    <span class="hljs-keyword">if</span> (vpd[i] &amp; PTE_V) &#123;<br>        <span class="hljs-keyword">for</span> (u_int j = <span class="hljs-number">0</span>; j &lt; PAGE_SIZE / <span class="hljs-keyword">sizeof</span>(Pte); j++) &#123;<br>            u_long va = (i * (PAGE_SIZE / <span class="hljs-keyword">sizeof</span>(Pte)) + j) &lt;&lt; PGSHIFT;<br>            <span class="hljs-keyword">if</span> (va &gt;= USTACKTOP) &#123;<br>                <span class="hljs-keyword">break</span>;<br>            &#125;<br>            <span class="hljs-keyword">if</span> (vpt[VPN(va)] &amp; PTE_V) &#123;<br>                duppage(child, VPN(va));<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>父进程遍历自己的页表，将虚拟页映射到子进程中。</p><h4 id="设置子进程异常入口并让他可运行"><a href="#设置子进程异常入口并让他可运行" class="headerlink" title="设置子进程异常入口并让他可运行"></a>设置子进程异常入口并让他可运行</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">syscall_set_tlb_mod_entry(child, cow_entry);<br>syscall_set_env_status(child, ENV_RUNNABLE);<br></code></pre></td></tr></table></figure><h4 id="总流程图如下"><a href="#总流程图如下" class="headerlink" title="总流程图如下"></a>总流程图如下</h4><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">父进程调用 fork()<br>        |<br>        v<br>检查父进程是否已注册 cow_entry<br>        |<br>        | 未注册<br>        v<br>syscall_set_tlb_mod_entry(0, cow_entry)<br>        |<br>        v<br>syscall_exofork()<br>        |<br>        v<br>内核 sys_exofork<br>        |<br>        | env_alloc 创建子 Env<br>        | 复制父进程 Trapframe 到子进程<br>        | 设置子进程 $v0 = 0<br>        | 设置子进程 ENV_NOT_RUNNABLE<br>        v<br>父进程从 syscall_exofork 返回 child envid<br>        |<br>        v<br>父进程遍历 vpd / vpt<br>        |<br>        v<br>对每个有效用户页调用 duppage(child, vpn)<br>        |<br>        v<br>判断页面权限<br>        |<br>        | 普通可写页<br>        |   去掉 PTE_D，加 PTE_COW<br>        |   映射给子进程<br>        |   重映射父进程为 COW<br>        |<br>        | 不可写页 / PTE_LIBRARY / 已是 COW<br>        |   按原权限映射给子进程<br>        v<br>syscall_set_tlb_mod_entry(child, cow_entry)<br>        |<br>        v<br>syscall_set_env_status(child, ENV_RUNNABLE)<br>        |<br>        v<br>父进程 fork 返回 child envid<br>子进程之后被调度<br>        |<br>        v<br>从复制来的 Trapframe 恢复执行<br>        |<br>        v<br>因为子进程 $v0 = 0，所以 child == 0<br>        |<br>        v<br>修正用户态 env 指针<br>        |<br>        v<br>子进程 fork 返回 0<br>父子任一方写 COW 页<br>        |<br>        v<br>页无 PTE_D，触发 TLB Mod 异常<br>        |<br>        v<br>内核 do_tlb_mod 跳转到用户态 cow_entry<br>        |<br>        v<br>cow_entry 检查 PTE_COW<br>        |<br>        v<br>在 UCOW 分配新页<br>        |<br>        v<br>复制原页内容到 UCOW<br>        |<br>        v<br>把 UCOW 映射回触发异常的 va，权限改为可写<br>        |<br>        v<br>解除 UCOW 临时映射<br>        |<br>        v<br>syscall_set_trapframe 恢复异常现场<br>        |<br>        v<br>重新执行刚才的写指令，写入成功<br></code></pre></td></tr></table></figure><h2 id="2025-extra"><a href="#2025-extra" class="headerlink" title="2025-extra"></a>2025-extra</h2><p>这里提供 2025 年的extra供大家上机参考。extra主要考察的还是<strong>新增系统调用</strong>这方面的一些知识，只不过extra的难度在于<code>c语言</code>实现。我们的主要战场在<code>kern/syscall_all.c</code>。一些函数的申明在此不多赘述，照着题目提示复制粘贴即可。我们直接来看<code>sys_***</code>函数的实现：</p><ol><li><code>sys_shm_new</code>：申请共享内存</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">// int sys_shm_new(u_int npage)<br><br>申请总大小为 npage 个页面的共享内存。在申请时，首先找到一个编号最小的，未被分配的（open = 0）的共享内存，并使用 page_alloc 函数申请 npage 个页面，进行必要的操作后，记录在 pages 数组中。<br><br>如果找不到这样可用的共享内存，返回 -E_SHM_INVALID 而无需申请页面。如果在申请页面时，空闲页面不足，返回 -E_NO_MEM。在这个过程中，请避免造成内存泄漏（也即，当发现分配失败时，你需要释放已经分配的页面）。<br><br>正确执行后，返回共享内存的编号（数组下标）。<br></code></pre></td></tr></table></figure><p>做这个题的关键在于知道page_allloc是干啥的，并且理解本题的结构体的概念：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Shm</span> &#123;</span><br>    u_int npage;     <span class="hljs-comment">// 共享内存的页面数量</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Page</span> *<span class="hljs-title">pages</span>[<span class="hljs-title">N_SHM_PAGE</span>];</span>   <span class="hljs-comment">// 存储共享内存页面的数组</span><br>    u_int open;          <span class="hljs-comment">// 标志共享内存是否被分配，0表示未分配，1表示已分配</span><br>&#125;;<br><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Shm</span> <span class="hljs-title">shm_pool</span>[<span class="hljs-title">N_SHM</span>];</span><br></code></pre></td></tr></table></figure><p>拆解题目，我们需要干的主体就是分配内存，如果分配失败清理已经分配的内存，最后返回共享内存的编号。题目还有如下提示：</p><blockquote><p>对于某个共享内存的页面，可能被绑定多个进程，然后被全部解除绑定，此时，你应该保证这些物理页面仍可以进行新的绑定操作。提示：你可以通过在创建共享内存时，增加页面的 pp_ref 记录，并在销毁共享内存时使用 page_decref 函数释放页面。</p></blockquote><p>所以代码也就呼之欲出了：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">sys_shm_new</span><span class="hljs-params">(u_int npage)</span> &#123;<br>    <span class="hljs-keyword">if</span> (npage == <span class="hljs-number">0</span> || npage &gt; N_SHM_PAGE) &#123;<br>        <span class="hljs-keyword">return</span> -E_SHM_INVALID;<br>    &#125;<br><br>    <span class="hljs-comment">// Lab4-Extra: Your code here. (5/8)</span><br>    <span class="hljs-comment">// 1. 找到一个编号最小的，未被分配的共享内存</span><br>    <span class="hljs-type">int</span> i;<br>    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; N_SHM; i++) &#123;<br>        <span class="hljs-keyword">if</span> (shm_pool[i].open == <span class="hljs-number">0</span>) &#123;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">if</span> (i == N_SHM) &#123;<br>        <span class="hljs-keyword">return</span> -E_SHM_INVALID;<br>    &#125;<br>    <span class="hljs-comment">// 2. 申请 npage 个页面，成功则记录在 pages 数组中，失败则清理已经分配的页面并返回 -E_NO_MEM</span><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; npage; j++) &#123;<br>        <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Page</span> *<span class="hljs-title">pp</span>;</span><br>        <span class="hljs-type">int</span> r = page_alloc(&amp;pp);   <span class="hljs-comment">// 记得page_alloc怎么用吗</span><br>        <span class="hljs-keyword">if</span> (r == <span class="hljs-number">0</span>) &#123;<br>            shm_pool[i].pages[j] = pp;<br>            pp-&gt;pp_ref++;<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">0</span>; k &lt; j; k++) &#123;<br>                <span class="hljs-keyword">struct</span> Page *p = shm_pool[i].pages[k];<br>                page_decref(p);<br>                shm_pool[i].pages[k] = <span class="hljs-literal">NULL</span>;     <span class="hljs-comment">// 清理已经分配的页面指针，保持好习惯</span><br>            &#125;<br>            <span class="hljs-keyword">return</span> -E_NO_MEM;<br>        &#125;<br>    &#125;<br>    <span class="hljs-comment">// 3. 记录共享内存的页面数量和状态，千万别忘了这一步</span><br>    shm_pool[i].npage = npage;<br>    shm_pool[i].open = <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">return</span> i;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="2"><li>sys_shm_bind：绑定共享内存</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">将虚拟地址 va（保证按页对齐） 作为共享内存的起始地址，映射到编号为 key 的共享内存中。你需要将虚拟地址范围 [va, va + npage * PAGE_SIZE) 依次映射到共享内存的 npage 个物理页面上。<br><br>如果对应的共享内存未被分配（open = 0），则返回 -E_SHM_NOT_OPEN。否则，返回 0 即可。<br></code></pre></td></tr></table></figure><p>这个题目关键在于记得映射虚拟地址和物理页面的函数是什么。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">sys_shm_bind</span><span class="hljs-params">(<span class="hljs-type">int</span> key, u_int va, u_int perm)</span> &#123;<br>    <span class="hljs-keyword">if</span> (key &lt; <span class="hljs-number">0</span> || key &gt;= N_SHM) &#123;<br>        <span class="hljs-keyword">return</span> -E_SHM_INVALID;<br>    &#125;<br><br>    <span class="hljs-comment">// Lab4-Extra: Your code here. (6/8)</span><br>    <span class="hljs-keyword">if</span> (shm_pool[key].open == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">return</span> -E_SHM_NOT_OPEN;<br>    &#125;<br><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; shm_pool[key].npage; i++) &#123;<br>        u_int nowva = va + i * PAGE_SIZE;<br>        page_insert(curenv-&gt;env_pgdir, curenv-&gt;env_asid, shm_pool[key].pages[i], nowva, perm);<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="3"><li>sys_shm_unbind：解除绑定</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">将虚拟地址范围 [va, va + npage * PAGE_SIZE) 解除映射（va 保证按页对齐）。简单起见，这里的实现不需要关心这些虚拟地址是否真的被映射到共享内存之中，使用 page_remove 函数移除映射即可。<br><br>如果 key 对应的共享内存未被分配（open = 0），返回 -E_SHM_NOT_OPEN。否则，返回 0 即可。<br></code></pre></td></tr></table></figure><p>顾名思义，本题的关键在于记得解除映射的函数是什么,代码片段如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; shm_pool[key].npage; i++) &#123;<br>    u_int nowva = va + i * PAGE_SIZE;<br>    page_remove(curenv-&gt;env_pgdir, curenv-&gt;env_asid, nowva);<br>&#125;<br></code></pre></td></tr></table></figure><ol start="4"><li>sys_shm_free：销毁共享内存</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">将 key 对应的共享内存注销（将 open 设为 0），并对页面进行必要的操作，将它们释放。<br><br>如果该共享内存原本未被分配，返回 -E_SHM_NOT_OPEN。否则，返回 0 即可。<br><br>测试数据保证在调用 sys_shm_free 前，全部的映射都已经被 unbind 了。<br></code></pre></td></tr></table></figure><p>最后仿照第一个函数释放页面的形式实现即可，别忘了清理已经分配页面的指针<code>shm_pool[key].pages[i] = NULL;</code>。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">sys_shm_free</span><span class="hljs-params">(<span class="hljs-type">int</span> key)</span> &#123;<br>    <span class="hljs-keyword">if</span> (key &lt; <span class="hljs-number">0</span> || key &gt;= N_SHM) &#123;<br>        <span class="hljs-keyword">return</span> -E_SHM_INVALID;<br>    &#125;<br><br>    <span class="hljs-comment">// Lab4-Extra: Your code here. (8/8)</span><br>    <span class="hljs-keyword">if</span> (shm_pool[key].open == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">return</span> -E_SHM_NOT_OPEN;<br>    &#125;<br><br>    shm_pool[key].open = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; shm_pool[key].npage; i++) &#123;<br>        <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Page</span> *<span class="hljs-title">p</span> =</span> shm_pool[key].pages[i];<br>        page_decref(p);<br>        shm_pool[key].pages[i] = <span class="hljs-literal">NULL</span>;<br>    &#125;<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="lab5"><a href="#lab5" class="headerlink" title="lab5"></a>lab5</h2><p>在开始lab5实验之前，建议大家提前学习了文件管理的相关理论知识。同时在学习过程中可以多和自己现实中的文件管理过程进行类比，有助于理解这个复杂的单元。</p><p>lab5主题是搭建一个文件管理系统，那么我们先来了解一下<strong>什么是文件管理系统</strong>。</p><p>在我看来，所谓文件管理系统，就是一个可以让用户方便地管理文件的系统。它可以让用户创建、删除、移动、复制文件和文件夹。那么为了实现这一系列功能，我们就需要设计一套合理的流程，我们可以把这个流程分为三个层次：</p><ol><li><strong>底层</strong>：怎么读写磁盘</li><li><strong>中层</strong>：怎么组织磁盘数据</li><li><strong>上层</strong>：怎么让用户正常使用文件（用户接口）</li></ol><p>最终的完整流程如下：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">用户程序调用 open/read/write/close<br>        ↓<br>用户态 fd 层找到对应设备 devfile<br>        ↓<br>通过 IPC 请求文件系统服务 fs server<br>        ↓<br>fs server 根据路径找到 struct File<br>        ↓<br>根据 f_direct / f_indirect 找到文件数据块<br>        ↓<br>通过块缓存读写磁盘块<br>        ↓<br>必要时通过 ide_read / ide_write 操作 IDE<br>        ↓<br>IDE 通过寄存器访问磁盘扇区<br>        ↓<br>完成真正的数据读写<br></code></pre></td></tr></table></figure><p>这篇博客将从下往上逐级介绍实现细节，对于代码细节不一定会深究，重在框架搭建和流程理解。（在懂了流程之后代码细节都好说）</p><h3 id="底层，磁盘读写"><a href="#底层，磁盘读写" class="headerlink" title="底层，磁盘读写"></a>底层，磁盘读写</h3><h4 id="用户态读写IDE设备寄存器"><a href="#用户态读写IDE设备寄存器" class="headerlink" title="用户态读写IDE设备寄存器"></a>用户态读写IDE设备寄存器</h4><p>这一层解决了一个问题：<strong>用户态文件系统服务如何访问磁盘设备</strong>。我们现在内核文件<code>kern/syscall_all.c</code>中实现两个系统调用：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">sys_read_dev(va, pa, len)<br>sys_write_dev(va, pa, len)<br></code></pre></td></tr></table></figure><p>他们的作用不是直接读写磁盘文件，而是允许用户态程序读写指定的设备物理地址<code>pa</code>，也就是IDE的MMIO寄存器。<br>这里解释一下，<code>MMIO(Memory-Mapped I/O)</code>就是把硬件设备的控制寄存器伪装成普通的内存地址，这样用户态程序就可以通过普通的内存访问指令来读写设备寄存器了。</p><h4 id="设备寄存器读写封装成IDE扇区读写"><a href="#设备寄存器读写封装成IDE扇区读写" class="headerlink" title="设备寄存器读写封装成IDE扇区读写"></a>设备寄存器读写封装成IDE扇区读写</h4><p>有了<code>sys_read_dev/sys_write_dev</code>之后，还不能直接读写文件，因为它们只是读写寄存器。接下来要在<code>fs/ide.c</code>中实现：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// diskno: 磁盘号</span><br><span class="hljs-comment">// secno: 起始逻辑扇区号</span><br><span class="hljs-comment">// nsecs: 要读取的扇区数</span><br><span class="hljs-comment">// dst: 目标内存地址指针</span><br>ide_read(diskno, secno, dst, nsecs)<br>ide_write(diskno, secno, src, nsecs)<br></code></pre></td></tr></table></figure><p>这一层的核心思想是：<strong>按照IDE协议配置LBA寄存器，然后通过DATA寄存器传输数据</strong>。DATA寄存器只是数据传输的窗口，真正决定读写磁盘的哪里是LBA寄存器。LBA寄存器由四个寄存器组成，分别是：</p><ul><li>LBA[7:0]：存储目标扇区号的低8位</li><li>LBA[15:8]：存储目标扇区号的中间8位</li><li>LBA[23:16]：存储目标扇区号的高8位</li><li>DEVICE寄存器：存储目标磁盘号和LBA模式等信息,以及目标扇区号的[24:27]位</li></ul><p>函数整体流程如下：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">等待 IDE 就绪<br>        ↓<br>设置要操作的扇区数量<br>        ↓<br>设置目标扇区号 secno 的 LBA[7:0], LBA[15:8], LBA[23:16]<br>        ↓<br>设置目标磁盘 diskno , LBA 模式和 secno 的 [24:27] ,在 DEVICE 寄存器<br>        ↓<br>写入读/写命令<br>        ↓<br>通过 DATA 寄存器读出或写入一个扇区的数据<br></code></pre></td></tr></table></figure><h3 id="中层，磁盘数据组织"><a href="#中层，磁盘数据组织" class="headerlink" title="中层，磁盘数据组织"></a>中层，磁盘数据组织</h3><h4 id="用fsformat构造文件系统镜像"><a href="#用fsformat构造文件系统镜像" class="headerlink" title="用fsformat构造文件系统镜像"></a>用fsformat构造文件系统镜像</h4><p>这一部分主要解决磁盘里一开始应该放什么，我们引入<code>tools/fsformat.c</code>。这是一个运行在宿主机上的工具，不是MOS内部运行的程序，它负责把<code>tests/lab5_x/rootfs</code>目录下的文件按照MOS文件系统的格式写入到磁盘镜像<code>fs.img</code>中。</p><p>在理清楚<code>fsformat</code>的流程之前，我们介绍一下文件系统的磁盘布局：</p><p><a href="./%E7%A3%81%E7%9B%98%E5%B8%83%E5%B1%80.png">!磁盘布局</a></p><p>代码文件中会涉及几个重要的结构体：Block(磁盘块), Super(文件系统超级块), File(文件控制块)</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Block</span> &#123;</span><br>    <span class="hljs-type">uint8_t</span> data[BLOCK_SIZE];<br>    <span class="hljs-type">uint32_t</span> type;<br>&#125; disk[NBLOCK];<br></code></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>data[BLOCK_SIZE]</td><td>这个磁盘块真正保存的数据内容。一个块 4096 字节，可能存普通文件内容，也可能存目录项、位图、间接索引块等。</td></tr><tr><td>type</td><td>标记这个块的类型。例如这个块是数据块、文件控制块块、索引块、位图块等。主要用于 fsformat 构造镜像时管理磁盘布局。</td></tr><tr><td>disk[NBLOCK]</td><td>这是一个全局数组，模拟了磁盘上所有的块。我们在 fsformat 里直接操作这个数组来构造文件系统镜像。disk[i]表示第i个磁盘块。</td></tr></tbody></table><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Super</span> &#123;</span><br>    <span class="hljs-type">uint32_t</span> s_magic;<br>    <span class="hljs-type">uint32_t</span> s_nblocks;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">File</span> <span class="hljs-title">s_root</span>;</span><br>&#125;;<br></code></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>s_magic</td><td>文件系统魔数，用于标识这是一个合法的文件系统。</td></tr><tr><td>s_nblocks</td><td>磁盘块总数，告诉文件系统有多少块可用。</td></tr><tr><td>s_root</td><td>根目录的文件控制块，包含根目录的元信息和索引关系。</td></tr></tbody></table><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">File</span> &#123;</span><br>    <span class="hljs-type">char</span> f_name[MAXNAMELEN];<br>    <span class="hljs-type">uint32_t</span> f_size;<br>    <span class="hljs-type">uint32_t</span> f_type;<br>    <span class="hljs-type">uint32_t</span> f_direct[NDIRECT];<br>    <span class="hljs-type">uint32_t</span> f_indirect;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">File</span> *<span class="hljs-title">f_dir</span>;</span><br>    <span class="hljs-type">char</span> f_pad[FILE_STRUCT_SIZE - MAXNAMELEN - (<span class="hljs-number">3</span> + NDIRECT) * <span class="hljs-number">4</span> - <span class="hljs-keyword">sizeof</span>(<span class="hljs-type">void</span> *)];<br>&#125; __attribute__((aligned(<span class="hljs-number">4</span>), packed));<br></code></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>f_name</td><td>文件名，存储这个文件的名字。</td></tr><tr><td>f_size</td><td>文件大小，单位是字节。对于普通文件，表示文件内容大小；对于目录，表示目录占用的数据块总大小。</td></tr><tr><td>f_type</td><td>文件类型。常见值有 FTYPE_REG 和 FTYPE_DIR，分别表示普通文件和目录。</td></tr><tr><td>f_direct[NDIRECT]</td><td>直接索引块数组，最多可以直接索引 NDIRECT 个数据块。</td></tr><tr><td>f_indirect</td><td>间接索引块号，如果文件需要更多数据块，用 f_indirect 指向一个索引块，这个索引块里面再保存更多数据块号。</td></tr><tr><td>f_dir</td><td>指向父目录的指针，方便路径解析时回溯。</td></tr></tbody></table><p><strong>PS:当f_type是FTYPE_DIR时，f_direct和f_indirect指向的块里存的不是文件内容，而是struct File，也就是目录块。</strong></p><p><code>fsformat</code>主要做几件事：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">init_disk()<br>        ↓<br>初始化磁盘块、超级块、位图<br><br>遍历传入的文件/目录<br>        ↓<br>如果是普通文件，调用 write_file<br>如果是目录，调用 write_directory<br><br>写入过程中：<br>        ↓<br>为文件/目录分配 struct File<br>为文件内容分配数据块<br>维护 f_direct / f_indirect 索引关系<br><br>flush_bitmap()<br>        ↓<br>把磁盘块占用情况写入位图<br><br>finish_fs()<br>        ↓<br>把内存中的 disk[] 写入 fs.img<br></code></pre></td></tr></table></figure><p>通过以上步骤，我们提前在磁盘里创建好了一个简单文件系统，包括超级块，根目录，位图，文件控制块以及文件数据块。</p><h4 id="把磁盘块映射到用户态缓存"><a href="#把磁盘块映射到用户态缓存" class="headerlink" title="把磁盘块映射到用户态缓存"></a>把磁盘块映射到用户态缓存</h4><p>文件系统进程是用户态服务。它不能每次访问文件都直接读磁盘，因为磁盘 I&#x2F;O 很慢。所以引入了块缓存block cache。</p><p>在<code>fs/fs.c</code>中实现把磁盘块抽象成用户态虚拟内存中的一页缓存，涉及到的核心函数有</p><ul><li>diskaddr(blockno)：计算磁盘块号对应的缓存<strong>虚拟地址</strong></li><li>block_is_mapped(blockno)：检查磁盘块是否已经映射到缓存</li><li>read_block(blockno)：把磁盘块读入缓存</li><li>write_block(blockno)：把缓存中的块写回磁盘</li><li>map_block(blockno)：把磁盘块映射到缓存，如果没有就分配物理页并从磁盘读入</li><li>unmap_block(blockno)：解除磁盘块和缓存的映射</li></ul><p>具体流程如下：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">磁盘块号 blockno<br>        ↓<br>计算对应的缓存虚拟地址<br>        ↓<br>如果没映射，就分配物理页并从磁盘读入<br>        ↓<br>如果缓存被修改，标记 dirty<br>        ↓<br>必要时 write_block 写回磁盘<br></code></pre></td></tr></table></figure><h4 id="磁盘空间管理，实现块的分配和释放"><a href="#磁盘空间管理，实现块的分配和释放" class="headerlink" title="磁盘空间管理，实现块的分配和释放"></a>磁盘空间管理，实现块的分配和释放</h4><p>文件系统还需要知道哪些磁盘块空闲，哪些已经被占用。这部分依赖 bitmap 位图,涉及的函数有：</p><ul><li>alloc_block()：分配一个空闲块，返回块号</li><li>free_block(blockno)：释放一个块，把它标记为可用</li><li>block_is_free(blockno)：检查一个块是否空闲</li></ul><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">alloc_block:<br>    扫描 bitmap<br>        ↓<br>    找到值为 1 的空闲块<br>        ↓<br>    将其置为 0，表示已占用<br>        ↓<br>    把 bitmap 所在块写回磁盘<br>        ↓<br>    为这个 block 建立缓存映射<br>        ↓<br>    返回 blockno<br><br>free_block:<br>    将 bitmap 对应 bit 置为 1<br>        ↓<br>    回写 bitmap<br>        ↓<br>    如果该块在缓存中，解除映射<br></code></pre></td></tr></table></figure><p>如此，文件系统就可以像管理物理页一样管理磁盘块了</p><h4 id="文件块管理，把文件逻辑块号映射到磁盘块号"><a href="#文件块管理，把文件逻辑块号映射到磁盘块号" class="headerlink" title="文件块管理，把文件逻辑块号映射到磁盘块号"></a>文件块管理，把文件逻辑块号映射到磁盘块号</h4><p>这部分逻辑类似lab2内存管理里的<code>pgdir_walk</code>，主要处理<strong>文件内部逻辑块和真实磁盘块之间的关系</strong>，说白了就是<strong>某个文件的第 filebno 个数据块，实际存在哪个磁盘块里</strong>的问题，涉及到的函数有：</p><ul><li><code>file_block_walk(struct File *f, u_int filebno, int alloc)</code>：根据文件 f 和文件块号 filebno，找到对应的磁盘块号，如果 alloc 为真且不存在则分配新块,返回槽位的地址。</li><li><code>file_map_block(struct File *f, u_int filebno, int alloc)</code>：根据文件 f 和文件块号 filebno，找到对应的磁盘块号，如果 alloc 为真且不存在则分配新块,返回磁盘块号。</li><li><code>file_clear_block(struct File *f, u_int filebno)</code>：根据文件 f 和文件块号 filebno，找到对应的磁盘块号并释放它。</li><li><code>file_get_block(struct File *f, u_int filebno)</code>：根据文件 f 和文件块号 filebno，找到该逻辑块在<strong>内存</strong>中的指针地址，封装了<strong>逻辑块 -&gt; 物理块 -&gt; 内存地址</strong>的转换过程。并将其保存在blk中。</li></ul><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">给定 File + 文件内部块号 filebno<br>        ↓<br>如果 filebno 在直接索引范围内<br>        ↓<br>    找 f_direct[filebno]<br><br>否则<br>        ↓<br>    找 f_indirect 指向的间接索引块<br>        ↓<br>    在间接索引块中找到对应磁盘块号<br></code></pre></td></tr></table></figure><h4 id="目录管理"><a href="#目录管理" class="headerlink" title="目录管理"></a>目录管理</h4><p>普通文件的数据块中存放的是文件内容，目录的数据块中存放的是多个struct file，也就是子文件的FCB。目录管理的核心函数有：</p><ul><li>dir_lookup(struct File *dir, char *name, struct File **file)：在目录 dir 中查找名字为 name 的子文件，如果找到就把它的 struct File 通过 file 参数返回。</li><li>dir_alloc_file(struct File *dir, struct File **file)：在目录中找到空闲的struct file（FCB），如果没有空闲则扩展一个，通过 file 参数返回。</li></ul><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">dir_lookup:<br>    遍历目录的所有数据块<br>        ↓<br>    每个数据块里有多个 struct File<br>        ↓<br>    比较 f_name<br>        ↓<br>    找到对应文件<br><br>dir_alloc_file:<br>    遍历目录中的 struct File<br>        ↓<br>    找到空闲 FCB<br>        ↓<br>    如果没有空闲 FCB，就给目录扩展一个新数据块<br></code></pre></td></tr></table></figure><h4 id="路径解析，从根目录找到目标文件"><a href="#路径解析，从根目录找到目标文件" class="headerlink" title="路径解析，从根目录找到目标文件"></a>路径解析，从根目录找到目标文件</h4><p>这是一个逐级调用dir_lookup的过程，核心函数是<code>walk_path</code>,这个函数至关重要，是后面<code>file_open</code>,<code>file_create</code>,<code>file_remove</code>的基础。</p><h4 id="文件级操作"><a href="#文件级操作" class="headerlink" title="文件级操作"></a>文件级操作</h4><p>在路径解析和文件块管理的基础上，我们可以实现真正的文件操作，比如：</p><ul><li>file_open() 打开一个文件，返回 struct File 结构体</li><li>file_create() 创建一个新文件，返回 struct File 结构体</li><li>file_remove() 删除一个文件</li><li>file_truncate() 清空一个文件的内容</li><li>….</li></ul><h3 id="上层，用户接口"><a href="#上层，用户接口" class="headerlink" title="上层，用户接口"></a>上层，用户接口</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">用户程序<br>  |<br>  | open/read/write/close/stat/remove<br>  v<br>+-------------------------+<br>| fd.c                    |<br>| 通用 fd 管理与设备分发   |<br>+-------------------------+<br>  |<br>  | 如果 fd 是普通文件<br>  v<br>+-------------------------+<br>| file.c                  |<br>| 普通文件设备接口         |<br>| open/file_read/write... |<br>+-------------------------+<br>  |<br>  | fsipc_open/map/dirty...<br>  v<br>+-------------------------+<br>| fsipc.c                 |<br>| IPC 请求封装层           |<br>+-------------------------+<br>  |<br>  | IPC<br>  v<br>+-------------------------+<br>| serv.c                 |<br>| 文件系统服务端           |<br>| serve_open/map/dirty... |<br>+-------------------------+<br>  |<br>  | 调用真正文件系统函数<br>  v<br>+-------------------------+<br>| fs.c / ide.c / disk.c   |<br>| 文件、目录、块缓存、磁盘 |<br>+-------------------------+<br></code></pre></td></tr></table></figure><h4 id="fd-c涉及的数据结构有："><a href="#fd-c涉及的数据结构有：" class="headerlink" title="fd.c涉及的数据结构有："></a>fd.c涉及的数据结构有：</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Fd</span> &#123;</span><br>    u_int fd_dev_id;<br>    u_int fd_offset;<br>    u_int fd_omode;<br>&#125;;<br></code></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>fd_dev_id</td><td>设备ID，标识这个文件描述符对应哪个设备。对于普通文件设备，这个值是0。</td></tr><tr><td>fd_offset</td><td>文件偏移量，表示当前读写位置在文件中的字节位置。每次读写操作后，这个值会更新。</td></tr><tr><td>fd_omode</td><td>打开模式，表示这个文件描述符是以什么模式打开的，比如只读、只写、读写等。</td></tr></tbody></table><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Dev</span> &#123;</span><br>    <span class="hljs-type">int</span> dev_id;<br>    <span class="hljs-type">char</span> *dev_name;<br>    <span class="hljs-type">int</span> (*dev_read)(<span class="hljs-keyword">struct</span> Fd *, <span class="hljs-type">void</span> *, u_int, u_int);<br>    <span class="hljs-type">int</span> (*dev_write)(<span class="hljs-keyword">struct</span> Fd *, <span class="hljs-type">const</span> <span class="hljs-type">void</span> *, u_int, u_int);<br>    <span class="hljs-type">int</span> (*dev_close)(<span class="hljs-keyword">struct</span> Fd *);<br>    <span class="hljs-type">int</span> (*dev_stat)(<span class="hljs-keyword">struct</span> Fd *, <span class="hljs-keyword">struct</span> Stat *);<br>    <span class="hljs-type">int</span> (*dev_seek)(<span class="hljs-keyword">struct</span> Fd *, u_int);<br>&#125;;<br><span class="hljs-comment">// 如果fd是普通文件，就可以变形</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Dev</span> <span class="hljs-title">devfile</span> =</span> &#123;<br>    .dev_id = <span class="hljs-string">&#x27;f&#x27;</span>,<br>    .dev_name = <span class="hljs-string">&quot;file&quot;</span>,<br>    .dev_read = file_read,<br>    .dev_write = file_write,<br>    .dev_close = file_close,<br>    .dev_stat = file_stat,<br>&#125;;<br></code></pre></td></tr></table></figure><h4 id="file-c涉及的数据结构有："><a href="#file-c涉及的数据结构有：" class="headerlink" title="file.c涉及的数据结构有："></a>file.c涉及的数据结构有：</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Filefd</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Fd</span> <span class="hljs-title">f_fd</span>;</span><br>    u_int f_fileid;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">File</span> <span class="hljs-title">f_file</span>;</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>这是是普通文件设备专用的<code>fd</code>结构，它在<code>Fd</code>的基础上额外保存<code>fileid</code>和<code>struct File</code>的副本。</p><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>f_fd</td><td>这是一个通用的文件描述符结构，包含了文件描述符的基本信息，比如类型、权限等。</td></tr><tr><td>f_fileid</td><td>这是一个唯一标识符，用于区分不同的文件实例。它可以用来在文件系统服务端和用户态之间进行文件的引用和管理。</td></tr><tr><td>f_file</td><td>这是一个 struct File 结构体，是当前文件的struct File副本。包含了文件的元信息和索引关系。它是从文件系统服务端获取的，代表了用户态打开的文件在文件系统中的状态。</td></tr></tbody></table><h4 id="fsipc-c涉及的数据结构有："><a href="#fsipc-c涉及的数据结构有：" class="headerlink" title="fsipc.c涉及的数据结构有："></a>fsipc.c涉及的数据结构有：</h4><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Fsreq_</span>* &#123;</span><br>        <span class="hljs-comment">// 用户进程发给文件服务进程的 IPC 请求参数,位于 user/lib/fsreq.h</span><br>&#125;<br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Open</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">File</span> *<span class="hljs-title">o_file</span>;</span><br>    u_int o_fileid;<br>    <span class="hljs-type">int</span> o_mode;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Filefd</span> *<span class="hljs-title">o_ff</span>;</span><br>&#125;;<br></code></pre></td></tr></table></figure><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>o_file</td><td>指向服务端文件系统中的 struct File。也就是这个打开对象真正对应的文件。</td></tr><tr><td>o_fileid</td><td>服务端给这个打开对象分配的编号。用户端 Filefd.f_fileid 保存的就是这个值。</td></tr><tr><td>o_mode</td><td>这是一个整数，表示打开文件的模式，比如只读、只写、读写等。</td></tr><tr><td>o_ff</td><td>这指向服务端为这个打开对象准备的 struct Filefd 页。serve_open 会把这个页映射给用户进程。</td></tr></tbody></table><h2 id="lab6"><a href="#lab6" class="headerlink" title="lab6"></a>lab6</h2><p>lab6主要内容是实现简单的shell，以及其中涉及管道的部分功能。先来看管道部分。</p><h3 id="pipe"><a href="#pipe" class="headerlink" title="pipe"></a>pipe</h3><p>管道负责提供跨进程共享内存的环形队列缓冲区，读写机制以及关闭检测来保障<code>shell</code>在用户态下可以实现多个进程之间通过<code>|</code>进行流式管道数据传输。具体实现代码在<code>user/lib/pipe.c</code>中。</p><h4 id="int-pipe-int-pfd-2"><a href="#int-pipe-int-pfd-2" class="headerlink" title="int pipe(int pfd[2])"></a>int pipe(int pfd[2])</h4><p>该函数主要功能是分配并映射用于管道通信的资源。具体代码逻辑如下：  </p><ol><li>分配文件描述符</li></ol><p>分配两个描述符控制页fd0(读端)和fd1(写端)。并设置权限为<strong>PTE_LIBRARY</strong>表示共享，以便在<code>fork</code>后父子进程能同时看到状态修改。  </p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">    <span class="hljs-keyword">if</span> ((r = fd_alloc(&amp;fd0)) &lt; <span class="hljs-number">0</span> || (r = syscall_mem_alloc(<span class="hljs-number">0</span>, fd0, PTE_D | PTE_LIBRARY)) &lt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">goto</span> err;<br>    &#125;<br>    <span class="hljs-keyword">if</span> ((r = fd_alloc(&amp;fd1)) &lt; <span class="hljs-number">0</span> || (r = syscall_mem_alloc(<span class="hljs-number">0</span>, fd1, PTE_D | PTE_LIBRARY)) &lt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">goto</span> err1;<br>    &#125;<br></code></pre></td></tr></table></figure><ol start="2"><li>分配共享数据页</li></ol><p>为管道申请物理内存页并映射给读端</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">    va = fd2data(fd0);<br>    <span class="hljs-keyword">if</span> ((r = syscall_mem_alloc(<span class="hljs-number">0</span>, (<span class="hljs-type">void</span> *)va, PTE_D | PTE_LIBRARY)) &lt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">goto</span> err2;<br>    &#125;<br></code></pre></td></tr></table></figure><ol start="3"><li>实现共享映射</li></ol><p>将同一个物理内存页映射给写端</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">if</span> ((r = syscall_mem_map(<span class="hljs-number">0</span>, (<span class="hljs-type">void</span> *)va, <span class="hljs-number">0</span>, (<span class="hljs-type">void</span> *)fd2data(fd1), PTE_D | PTE_LIBRARY)) &lt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">goto</span> err3;<br>    &#125;<br></code></pre></td></tr></table></figure><ol start="4"><li>将分配的文件描述符编号写入pfd[0]和pfd[1]</li></ol><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">pfd[<span class="hljs-number">0</span>] = fd2num(fd0);<br>pfd[<span class="hljs-number">1</span>] = fd2num(fd1);<br></code></pre></td></tr></table></figure><h4 id="pipe-read"><a href="#pipe-read" class="headerlink" title="pipe_read"></a>pipe_read</h4><p>该函数实现了从管道文件描述符中读取最多n字节数据到用户缓冲区<code>vbuf</code>中。核心逻辑为缓冲区读空时的处理：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">while</span> (p-&gt;p_rpos &gt;= p-&gt;p_wpos) &#123;<br>    <span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span> || _pipe_is_closed(fd, p)) &#123;<br>        <span class="hljs-keyword">return</span> i;<br>    &#125;<br>    syscall_yield();<br>&#125;<br></code></pre></td></tr></table></figure><p>如果<code>i&gt;0</code>说明之前已经成功读取了至少1个字节，应当立即返回已经读取的字节数；如果写端已经全部关闭了，说明以后不会有新的数据写入，可以立即返回。如果以上条件都不满足，主动让出CPU，等待写进程写入数据。</p><p><strong>写进程同理，这里就不赘述</strong></p><h4 id="pipe-is-closed"><a href="#pipe-is-closed" class="headerlink" title="_pipe_is_closed()"></a>_pipe_is_closed()</h4><p>该函数用于检测通道是否关闭，通过物理页引用计数的比较来实现。</p><ul><li><p>pageref(p):该管道共享数据页的总引用计数</p></li><li><p>pageref(fd):当前持有的这一端描述符的引用计数。</p></li></ul><p>显然，当<code>fd_ref == page_ref</code>时,说明只剩下当前这一端还映射这这个管道，另一端已无进程打开，即该管道已关闭。</p><p>但是在处理过程中我们要考虑并发安全：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">do</span> &#123;<br>        runs = env-&gt;env_runs;<br>        fd_ref = pageref(fd);<br>        pipe_ref = pageref(p);<br>    &#125; <span class="hljs-keyword">while</span> (runs != env-&gt;env_runs);<br><br>    <span class="hljs-keyword">return</span> fd_ref == pipe_ref;<br></code></pre></td></tr></table></figure><p>如果在读取了<code>fd_ref</code>之后，读取<code>pipe_ref</code>之前，发生时钟中断，操作系统将CPU切换给了管道的另一端进程，而另一端进程执行了<code>close()</code>操作，导致<code>pipe_ref</code>的值减1。此时，比较对象一个时关闭前的数据，一个时关闭后的数据，压根不具备可比性。所以我们利用<code>env-&gt;env_runs</code>来监控调度，确保fd_ref和pipe_ref是在同一个没有被中断打扰的时间片内读出。</p><h3 id="shell"><a href="#shell" class="headerlink" title="shell"></a>shell</h3><h4 id="sh-c"><a href="#sh-c" class="headerlink" title="sh.c"></a>sh.c</h4><p><code>sh.c</code>负责接受用户输入的指令，理解其意图。并调度操作系统内核和其他用户程序来执行这些命令。主要用到的是<strong>语法分析</strong>和<strong>词法分析</strong>的知识。</p><p>目前shell具备识别以下符号的能力：</p><ul><li><p>w:普通单词</p></li><li><p>&lt;:输入重定向</p></li><li><p>&gt;:输出重定向</p></li><li><p>|:管道</p></li></ul><h4 id="spawn-c"><a href="#spawn-c" class="headerlink" title="spawn.c"></a>spawn.c</h4><p>在传统的Unix操作系统中，创建并运行新程序需要组合使用<code>fork</code>和<code>exec</code>。然而，MOS采用了类似Windows的<code>spawn</code>机制，把解析和加载程序的任务移到了用户态，简化了微内核的设计。</p><p>控制核心为<code>spawn</code>函数，拆解分析如下：</p><ol><li>打开ELF可执行文件</li></ol><p>根据传入的程序路径prog，以只读模式打开对应的ELF格式可执行文件。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> fd;<br>    <span class="hljs-keyword">if</span> ((fd = open(prog, O_RDONLY)) &lt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">return</span> fd;<br>    &#125;<br></code></pre></td></tr></table></figure><ol start="2"><li>读取并校验ELF头部</li></ol><p>从文件中读取<code>sizeof(Elf32_Ehdr)</code>大小的数据到临时缓冲区<code>elfbuf</code>。并调用<code>elf_from</code>函数进行ELF格式校验。最后将程序的入口地址保存在<code>entrypoint</code>中，后续用于初始化PC寄存器。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> r;<br>u_char elfbuf[<span class="hljs-number">512</span>];<br><span class="hljs-keyword">if</span> ((r = readn(fd, elfbuf, <span class="hljs-keyword">sizeof</span>(Elf32_Ehdr))) != <span class="hljs-keyword">sizeof</span>(Elf32_Ehdr)) &#123;<br>    <span class="hljs-keyword">goto</span> err;<br>&#125;<br><span class="hljs-type">const</span> Elf32_Ehdr *ehdr = elf_from(elfbuf, <span class="hljs-keyword">sizeof</span>(Elf32_Ehdr));<br><span class="hljs-keyword">if</span> (!ehdr) &#123;<br>    r = -E_NOT_EXEC;<br>    <span class="hljs-keyword">goto</span> err;<br>&#125;<br>u_long entrypoint = ehdr-&gt;e_entry;<br></code></pre></td></tr></table></figure><ol start="3"><li>创建一个空白子进程</li></ol><p>利用<code>syscall_exofork()</code>创建一个新的进程控制块，并复制父进程的寄存器状态，但不会拷贝任何内存页面。此时子进程处于挂起状态，<strong>需要注意的是</strong>：<code>syscall_exofork()</code>返回值是子进程的envid（环境id），如果执行失败则返回负数。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">u_int child;<br>child = syscall_exofork();<br><span class="hljs-keyword">if</span> (child &lt; <span class="hljs-number">0</span>) &#123;<br>    r = child;<br>    <span class="hljs-keyword">goto</span> err;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="4"><li>初始化子进程的用户栈</li></ol><p>调用<code>init_stack</code>申请一个物理页，将其映射到子进程的栈顶处。并向栈中压入<code>argc</code>、<code>argv</code>等参数，最终计算出子进程启动时sp寄存器应指向的虚拟地址，写入sp变量中。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c">u_int sp;<br><span class="hljs-keyword">if</span> ((r = init_stack(child, argv, &amp;sp)) &lt; <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-keyword">goto</span> err1;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="5"><li>将ELF程序段加载到子进程内存中</li></ol><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-type">size_t</span> ph_off;<br>ELF_FOREACH_PHDR_OFF (ph_off, ehdr) &#123;<br>    <span class="hljs-keyword">if</span> ((r = seek(fd, ph_off)) &lt; <span class="hljs-number">0</span>) &#123; <span class="hljs-keyword">goto</span> err1; &#125;<br>    <span class="hljs-keyword">if</span> ((r = readn(fd, elfbuf, ehdr-&gt;e_phentsize)) != ehdr-&gt;e_phentsize) &#123; <span class="hljs-keyword">goto</span> err1; &#125;<br>    Elf32_Phdr *ph = (Elf32_Phdr *)elfbuf;<br>    <span class="hljs-keyword">if</span> (ph-&gt;p_type == PT_LOAD) &#123;<br>        <span class="hljs-type">void</span> *bin;<br>        <span class="hljs-comment">// 1. 将文件中的段数据读入父进程的临时缓冲区</span><br>        r = read_map(fd, ph-&gt;p_offset, &amp;bin);<br>        <span class="hljs-keyword">if</span> (r != <span class="hljs-number">0</span>) &#123; <span class="hljs-keyword">goto</span> err1; &#125;<br>        <span class="hljs-comment">// 2. 将段数据映射并加载到子进程的虚拟内存空间中</span><br>        r = elf_load_seg(ph, bin, spawn_mapper, &amp;child);<br>        <span class="hljs-keyword">if</span> (r != <span class="hljs-number">0</span>) &#123; <span class="hljs-keyword">goto</span> err1; &#125;<br>    &#125;<br>&#125;<br><br>close(fd);<br></code></pre></td></tr></table></figure><ol start="6"><li>设置子进程的寄存器上下文</li></ol><p>初始化执行状态，子进程被fork出来时寄存器与父进程一致，此时我们需要修正他的两个关键寄存器的值：</p><ul><li><p>cp0_epc：修改为ELF入口地址entrypoint,</p></li><li><p>regs[29]:修改为刚刚初始化完argv参数的栈顶指针sp</p></li></ul><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Trapframe</span> <span class="hljs-title">tf</span> =</span> envs[ENVX(child)].env_tf;<br>tf.cp0_epc = entrypoint; <span class="hljs-comment">// 设置 PC 寄存器为程序入口地址</span><br>tf.regs[<span class="hljs-number">29</span>] = sp;        <span class="hljs-comment">// 设置 SP 寄存器为第四步计算出的栈指针</span><br><span class="hljs-keyword">if</span> ((r = syscall_set_trapframe(child, &amp;tf)) != <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-keyword">goto</span> err2;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="7"><li>继承父进程的共享页面</li></ol><p>扫描当前父进程页表中所有有效的虚拟页面，如果页面的权限标志中包含PTE_LIBRARY,说明该页面需要跨进程共享，通过<code>syscall_mem_map</code>将这些页面以相同的虚拟地址和权限直接映射到子进程中。这也是子进程能够直接使用父进程打开的管道和重定向文件的根本原因。</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 扫描整个用户空间页表直到 USTACKTOP</span><br><span class="hljs-keyword">for</span> (u_int pdeno = <span class="hljs-number">0</span>; pdeno &lt;= PDX(USTACKTOP); pdeno++) &#123;<br>    <span class="hljs-keyword">if</span> (!(vpd[pdeno] &amp; PTE_V)) &#123; <span class="hljs-keyword">continue</span>; &#125;<br>    <span class="hljs-keyword">for</span> (u_int pteno = <span class="hljs-number">0</span>; pteno &lt;= PTX(~<span class="hljs-number">0</span>); pteno++) &#123;<br>        u_int pn = (pdeno &lt;&lt; <span class="hljs-number">10</span>) + pteno;<br>        u_int perm = vpt[pn] &amp; ((<span class="hljs-number">1</span> &lt;&lt; PGSHIFT) - <span class="hljs-number">1</span>);<br>        <span class="hljs-keyword">if</span> ((perm &amp; PTE_V) &amp;&amp; (perm &amp; PTE_LIBRARY)) &#123;<br>            <span class="hljs-type">void</span> *va = (<span class="hljs-type">void</span> *)(pn &lt;&lt; PGSHIFT);<br>            <span class="hljs-comment">// 共享映射到子进程对应虚拟地址</span><br>            <span class="hljs-keyword">if</span> ((r = syscall_mem_map(<span class="hljs-number">0</span>, va, child, va, perm)) &lt; <span class="hljs-number">0</span>) &#123;<br>                <span class="hljs-keyword">goto</span> err2;<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="8"><li>使能并启用子进程</li></ol><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">if</span> ((r = syscall_set_env_status(child, ENV_RUNNABLE)) &lt; <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-keyword">goto</span> err2;<br>&#125;<br><span class="hljs-keyword">return</span> child;<br></code></pre></td></tr></table></figure><p>将子进程的状态从挂起设为<code>ENV_RUNNABLE</code>即可。</p><h2 id="再见OS"><a href="#再见OS" class="headerlink" title="再见OS"></a>再见OS</h2><p>至此，本学期的OS实验要向大家说再见了，希望大家在提交lab6之后能带着从容和自信，为这段学习旅程写下一个圆满的句号。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">阳光总在风雨后<br>乌云上有晴空<br>珍惜所有的感动<br>每一份希望在你手中<br>阳光总在风雨后<br>请相信有彩虹<br>风风雨雨都接受<br>我一直会在你的左右<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> BUAA </tag>
            
            <tag> OS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>批判性思维与贝叶斯定理</title>
      <link href="/2026/06/22/%E4%BF%A1%E5%8F%B7%E5%A4%84%E7%90%86%E4%B8%8E%E4%BF%A1%E6%81%AF%E6%8E%A8%E6%96%AD/"/>
      <url>/2026/06/22/%E4%BF%A1%E5%8F%B7%E5%A4%84%E7%90%86%E4%B8%8E%E4%BF%A1%E6%81%AF%E6%8E%A8%E6%96%AD/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>写这篇博客的灵感来源于刘雪峰老师主讲的课程《信号处理与信息推断》。这门课程的主要内容来自刘老师的著作《生活不是掷骰子》。从中不仅可以领略到了贝叶斯公式在生活中无处不在，也学会了以批判的眼光看问题，以理性的思维做决策。结合课上所学和书中所讲，我感触颇多，遂投诸文字，希望能帮助各位读者透过现象看清本质，活得从容，活得自由。</p><h2 id="信息推断"><a href="#信息推断" class="headerlink" title="信息推断"></a>信息推断</h2><p>所谓信息推断，就是由观察到的现象推断其背后的信息。这是一个由外而内的过程，而这个过程往往比由内而外的解释要困难的多。比如通过一个人感染了流感病毒我们可以推出他出现发烧，咳嗽，乏力等症状。这是由内而外，由原因得到结果的过程，相对容易；但是通过一个人出现发烧，咳嗽等症状，我们很难准确推断除他感染了流感病毒。这是由外而内，由结果推断原因的过程，相对困难。</p><p>在生活中我们常见两种推断方式：</p><ul><li>黑白思维</li><li>概率思维</li></ul><p>黑白思维的思维方式:</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">看到一个现象 -&gt; 找到一个原因 -&gt; 不断收集证据来加强这个原因 -&gt; 一开始想到的原因就是最后的结论<br></code></pre></td></tr></table></figure><p>概率思维的思维方式：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">看到一个现象 -&gt; 找到多个可能的原因 -&gt; 收集证据来给每个原因概率 -&gt; 找到可能性最大的原因<br></code></pre></td></tr></table></figure><h2 id="贝叶斯公式"><a href="#贝叶斯公式" class="headerlink" title="贝叶斯公式"></a>贝叶斯公式</h2><p>$$<br>P(A \mid B) &#x3D; P(A)\frac{P(B \mid A)}{P(B)}<br>$$</p><p>A表示原因，B表示观察到的现象。</p><ul><li>$P(B \mid A)$ ：似然概率，表示原因A对现象B的解释程度</li><li>$P(A)$：先验概率，表示原因A出现的概率</li><li>$P(A \mid B)$：后验概率，表示发生现象B是因为原因A的概率</li></ul><p>显然，<strong>贝叶斯定理选出的是既本身常见，又能在一定程度上解释该观测的原因，二者缺一不可</strong></p><h2 id="贝叶斯定理与生活"><a href="#贝叶斯定理与生活" class="headerlink" title="贝叶斯定理与生活"></a>贝叶斯定理与生活</h2><h3 id="奥卡姆剃刀"><a href="#奥卡姆剃刀" class="headerlink" title="奥卡姆剃刀"></a>奥卡姆剃刀</h3><blockquote><p>如非必须，勿增实体。</p></blockquote><p>最简单的原因，往往就是先验概率最大的原因。在日常生活中很多情况下$P(B \mid A_1) &#x3D; P(B \mid A_2) &#x3D; 1$ ,此时后验概率的大小主要取决于先验概率。这也就是为什么顶尖守门员扑点球的时候并不是根据球员的动作，而是根据球员的踢点球习惯猜一个方向。</p><h3 id="汉隆剃刀"><a href="#汉隆剃刀" class="headerlink" title="汉隆剃刀"></a>汉隆剃刀</h3><p>能用愚蠢解决，不要解释为恶意。因为愚蠢的先验概率最大。</p><h3 id="扭转先验"><a href="#扭转先验" class="headerlink" title="扭转先验"></a>扭转先验</h3><p>无论是奥卡姆剃刀还是汉隆剃刀，都说明了先验概率的重要性。那么我们如何<strong>扭转先验</strong>呢，当然要从似然概率上做文章。</p><p>当你想要降低某个观点的可信度时，不仅要找到该观点不能解释的证据，还要找到其他观点可以解释的证据。就像书中特别形象的一个比喻：</p><blockquote><p>当你想要贬低一个人的时候，最有效的方法往往不是直接贬低他，而是在贬低他的同时抬高另一个人</p></blockquote><p>这就是所谓的<strong>信息量</strong>大。信息量大主要有两种情况：<br>$$<br>\frac{P(A \mid \overline{H})}{P(A \mid H)} \approx 0<br>$$<br><strong>强烈支持H</strong></p><p>$$<br>\frac{P(A \mid H)}{P(A \mid \overline{H})} \approx 0<br>$$<br><strong>强烈反对H</strong></p><p>其中隐含着一条原则：<strong>找到排他性证据的难度，远远大于解释的难度</strong>。所以，当我们在生活中要支持某个原因时，一个排他性证据的说服力远大于一万句解释的说服力。</p><h3 id="条件独立"><a href="#条件独立" class="headerlink" title="条件独立"></a>条件独立</h3><p>若事件A，B关于事件C条件独立，有<br>$$<br>\left{<br>\begin{array}{l}<br>P(A,B \mid C) &#x3D; P(A \mid C) \cdot P(B \mid C) \<br>P(A \mid B,C) &#x3D; P(A \mid C) \<br>P(B \mid A,C) &#x3D; P(B \mid C)<br>\end{array}<br>\right.<br>$$<br>在C发生的情况下，A&#x2F;B的信息不能帮助我们更好地推断B&#x2F;A发生地概率。这说明相关性并不等于因果性，统计规律也不能说明两个事件之间的因果关联。</p><p>例如：统计规律发现，伦敦司机穿夹克时事故发生率升高，但是实际原因是:天气下雨，会导致司机穿夹克，并且天气下雨也会导致事故率升高。也就是说穿夹克和事故率高是关于下雨条件独立的。</p><h3 id="在线贝叶斯"><a href="#在线贝叶斯" class="headerlink" title="在线贝叶斯"></a>在线贝叶斯</h3><p>在多数情况下，多个观测并不是同时到来的，我们希望每来一个观测，我们就根据当前观测得到一个估计。而在更新时我们有两个选择：</p><ul><li>每来一个观测就全部重新计算</li><li>没来一个观测就在原来的基础上更新</li></ul><p>在线贝叶斯主要考虑的时第二种情况。让当前时刻的后验概率充当下一时刻的先验概率。</p><h3 id="分层描述法"><a href="#分层描述法" class="headerlink" title="分层描述法"></a>分层描述法</h3><p>我们常常会把很多重要的信息聚合在一起导致样本数量过少，无法获得统计数据。所以，可以适当降低条件数量，增大样本，从而找到我们想要的概率。颗粒度越粗，样本数量越多。例如毕业生找工作问题：</p><blockquote><p>北航计算机学院本科生找到好工作的概率</p></blockquote><p>可以按如下粒度划分：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">高校本科毕业生<br>一本高校本科毕业生<br>985高校本科毕业生<br>北航本科毕业生<br>北航计算机学院本科毕业生<br></code></pre></td></tr></table></figure><p>通过不同粒度的划分，我们可以逼近到最小粒度的概率，从而获得较为精确的结论。</p><h3 id="用事实操纵真相"><a href="#用事实操纵真相" class="headerlink" title="用事实操纵真相"></a>用事实操纵真相</h3><p>我们在日常生活中会听到各种各样的<strong>事实</strong>，当然有些<strong>所谓的事实</strong>纯是捏造谣言博取眼球，那真正的事实就一定可信吗？我们来看看这个清末海关的例子：</p><blockquote><p> 版本一：第二次鸦片战争后，各通商口岸的关税管理权落入西方殖民者手中。</p></blockquote><p>从第一个版本的事实中我们得出结论：<strong>外国人干涉中国主权，这是殖民主义者的严重罪行</strong></p><blockquote><p> 版本二：清末海关贪腐横行，由洋人代管后海关税收有了立竿见影的提高，海关税收成为了清政府重要的财政收入来源。同时，英国人赫德接任海关总税务司后，极力建议清政府学习西方，干好理财，练兵，劝学三件大事。在八国联军侵华后，他也曾力劝八国使团不要推翻清政府。他还建议清政府提高海军战斗力，对抗日本。</p></blockquote><p>从第二个版本的事实中我们得出结论：<strong>西方人不管在道义上还是能力上都比当时中国人强，并且真的有一心一意对我们好的外国人，简直是旧时代的白求恩。</strong></p><blockquote><p>版本三：赫德力劝八国使团不要推翻清政府是为了保留清政府这个会下蛋的母鸡，从而每年支付巨额赔款；他建议清政府提高海军力量对抗日本是为了防止日本侵害英国在华的既得利益，同时让清政府从英国购买军舰。</p></blockquote><p>从第三个版本的事实中我们得出结论：虽然赫德看起来为中国做了不少好事，但是本质上还是为了英国在中国的利益服务</p><p>显然，我们可以通过讲事实来给别人构造出截然不同的故事，从而引导受众的思维进入我们预先设计好的思维圈套中，逐渐丧失自己的判断能力。互联网很多观点就是这样，通过片面的事实来输出价值导向，误导观众。如果你不具备基本的批判性思维，那很可能会被一些人牵着鼻子走还觉得自己的理解高人一等，属实可悲。</p><p>所以，请记住，以批判的眼光看待你所看到的所有事实，<strong>我们要的是事实，而且是全部的事实</strong>。</p><h2 id="贝叶斯的启示"><a href="#贝叶斯的启示" class="headerlink" title="贝叶斯的启示"></a>贝叶斯的启示</h2><p>《生活不是掷骰子》这本书以质朴的语言和形象的举例，给我们讲解了一个深刻的道理：全面看问题，理性做决策。当我们在生活中遇到某个观点时，不妨问问自己以下几个问题：</p><ul><li>用来佐证观点的证据可靠吗</li><li>这些证据量化了吗</li><li>是有偏采样得到的吗</li><li>有没有该观点之外的原因可以解释这些证据</li><li>这个原因的先验概率高吗，如果不高的话，这些证据足以扭转先验吗</li></ul><p>当你能以这样的态度面对生活中的问题时，你就具有了批判性思维。当你不断了解不同的证据，不断根据正确的判断来修正你的认知的时候，你就具有了成长型思维。</p><p>最后，引用刘老师课上所讲：<strong>毋意，毋必，毋固，毋我</strong></p><p>不要主观臆断，要主动寻找证据；不要认定某事百分百正确或错误，要用概率思维看待问题；不要固执己见，要勇于使自己的观点随着事实的改变而改变；不要永远从内部视角看问题，当你跳出来时你会发现不一样的风景。</p><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>当我们具备理性思维之后，我们会发现身边的很多人其实沉浸在自己的感性世界里。起初我们会笑他们的愚蠢，甚至会觉得他们可怜。但这种世人皆醉我独醒的优越感往往会成为压垮生活的最后一根稻草。当你看清太多事情的本质时，你会发现幸福感在一点点流失，你无法与这个世界共情，活在一种虚幻的孤独里。所以，我们不必成为极端的理性主义者，做一个具备理性思维的人足矣。特别喜欢刘老师在课程结束时的赠言：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">你可以看穿规则，但不必时时揭穿它<br>你可以戳穿幻觉，但也别着急粉碎他人的梦<br>你可以不说，但在关键时刻，你要展示你的勇气和决断<br>我们清醒的活着，不是为了审判世界，而是理解世界后，既拥有内心持久的宁静，也依然充满热爱生活的能力<br></code></pre></td></tr></table></figure><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p>刘雪峰. 生活不是掷骰子：理性决策的贝叶斯思维[M]. 北京: 电子工业出版社, 2024.</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">莫听穿林打叶声，何妨吟啸且徐行。<br>竹杖芒鞋轻胜马，谁怕？一蓑烟雨任平生。<br>料峭春风吹酒醒，微冷，山头斜照却相迎。<br>回首向来萧瑟处，归去，也无风雨也无晴。<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 数学与生活 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 贝叶斯定理 </tag>
            
            <tag> 信息推断 </tag>
            
            <tag> 理性决策 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>AI 微电影制作</title>
      <link href="/2026/04/06/AI%E5%88%B6%E5%9B%BE%E6%8F%90%E7%A4%BA%E8%AF%8D/"/>
      <url>/2026/04/06/AI%E5%88%B6%E5%9B%BE%E6%8F%90%E7%A4%BA%E8%AF%8D/</url>
      
        <content type="html"><![CDATA[<h2 id="序"><a href="#序" class="headerlink" title="序"></a>序</h2><p>在这个 AI 技术飞速发展的时代，AI 微电影的制作已经成为了一个热门话题。这个方向确实值得我们挖掘，因为微电影制作过程中的每一个环节都可以丰富我们的技能包，都可以对其他领域产生意想不到的帮助。AI 微电影的制作主要分为三步，写脚本，生成图片，视频剪辑。每一步都有细节和技巧，下面我们开始吧。</p><h2 id="工具准备"><a href="#工具准备" class="headerlink" title="工具准备"></a>工具准备</h2><p>豆包，Gemini Pro &#x2F; GPT Plus（推荐 Gemini Pro），剪映</p><h2 id="写脚本"><a href="#写脚本" class="headerlink" title="写脚本"></a>写脚本</h2><p>写脚本要用豆包来实现，因为豆包的文字功底在国产 AI 中那是独一份的存在。<strong>豆包使用思考模式，Gemini Pro 使用图片制作，Pro 模式。</strong></p><ol><li>首先我们需要针对自己的主题给出一个大概的脚本设想，明确一共有几个部分，每个部分大概包括什么内容。(这部分可以使用 AI 辅助实现，但是整体框架还是建议由你来构思)</li><li>新开一个对话，给出如下提示词：</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">你是一个资深的动画编剧，全球顶级。我在进行[时长][主题]视频的制作，上面是我已经做好了初步的故事脚本，请你修改这个脚本，包含镜头，旁白，运镜，持续时间，使得这个故事脚本更加合理。<br></code></pre></td></tr></table></figure><ol start="3"><li>在当前对话中对格式做出微小的修正，例如给出如下提示词：</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">分镜脚本更详细一点，要有|镜号|画面描述|镜头运动|时长|对白音效<br></code></pre></td></tr></table></figure><ol start="4"><li>当你觉得脚本包含的信息较为完善之后，就可以开始图片提示词的生成。为了使 AI 生成的图片风格一致，我们需要在提示词方面下功夫。我们可以给出如下提示词：</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">提示词生成参考，具有以下七个内容：风格限定，视角构图，主体描述，背景设定，细节修饰，光影色调，质量词。请结合分镜脚本场景，按照示例风格拆解不同镜头生成提示词。请一定保证整个剧本画面风格的一致性，你来为我选定一个合适的风格<br></code></pre></td></tr></table></figure><ol start="5"><li>由于 AI 初次生成的提示词包含的脚本镜头可能过少，我们可以通过将某句话发送并让他根据上述格式要求生成提示词，例如：</li></ol><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">这正是现代金融风控的核心进化方向：从静态的单因子认证，走向动态的多维一致性验证，为我们筑牢了可信的技术屏障，让金融安全真正跟上数字时代的步伐。把这张图片按照上述要求生成提示词<br></code></pre></td></tr></table></figure><h2 id="生成图片"><a href="#生成图片" class="headerlink" title="生成图片"></a>生成图片</h2><p>生成图片使用的是 Gemini Pro，这里再说一下工具要求，<strong>Pro 模式</strong>、<strong>图片制作</strong>。</p><ol><li>上传脚本文件，说明<code>这是我的一个脚本，我之后会给你上传提示词，请你按照提示词来给我生成图片</code></li><li>将提示词复制粘贴到对话中，等待生成图片。</li></ol><p><strong>特别注意一下</strong>：到了后期，AI 比较容易出现幻觉，这个时候重开一个对话重复上述步骤即可。对于需要微调的图片建议新开一个对话进行调整，即保证当前对话的纯净度。</p><h2 id="视频剪辑"><a href="#视频剪辑" class="headerlink" title="视频剪辑"></a>视频剪辑</h2><p>这个我教不了大家了[哭脸]</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>虽然名为 AI 微电影，但是这篇博客的核心在于图像生成，尤其是风格统一的图像生成，这在日常生活中的 PPT 制作、给导师当牛马的过程中都非常好用，希望能对各位牛马同仁有所帮助。最后祝大家都能做出自己满意的作品。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">我走在长街中       盼郎君几撩纱<br>听戏子唱京城       夜泊借宿酒家<br>人杂乱戏小丑       君载着黑骏马<br>叶黄退入长秋       威风凛凛寻她<br>悠悠的古城中       我本一醉天涯<br>听美人奏琴声       游走京惜繁华<br>朗朗夜色星空       不舍笑声离她<br>望孩童放花灯       ...<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> prompt </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AI </tag>
            
            <tag> prompt </tag>
            
            <tag> 微电影 </tag>
            
            <tag> 制图 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-OO」 二三事</title>
      <link href="/2026/03/28/OO%E4%BA%8C%E4%B8%89%E4%BA%8B/"/>
      <url>/2026/03/28/OO%E4%BA%8C%E4%B8%89%E4%BA%8B/</url>
      
        <content type="html"><![CDATA[<h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><p>OO 全称 <strong>Objective Oriented</strong>，所谓面向对象，最重要的当然得有对象了，对象都没有，你去哪面向对象呢？所以趁着大二，听劝，赶紧找一个哈！</p><p>好了，言归正传，我们先说明一下什么是面向对象，所谓面向对象，就是把现实世界中的事务抽象成一个个的对象，然后通过对这些对象的操作来实现我们想要的功能。每个对象有它自己的属性和方法，属性就是对象的特征，方法就是对象的行为。比如说我们有一个学生对象，它有名字、年龄、性别等属性，还有吃饭、睡觉、学习等方法。</p><p>值得注意的是，面向对象只是一种设计思想，它并不是某个语言的专属。大家想想我们大一学习的 C 语言，难道它无法实现面向对象编程吗，答案当然是否定的，类可以是结构体，方法可以是函数指针，然后问题就迎刃而解了。那么有同学又要说了，这也太难了，从来都没写过函数指针啊！没错，写不出来是你菜，可不是人家 C 语言实现不了哦😥。课程组之所以为我们选择 Java 来实现面向对象编程主要是因为 Java 提供了很多现成的库和工具，让我们可以更方便地实现面向对象编程，同时也可以让我们更好地理解面向对象的思想。</p><p>关于面向对象的思想，个人认为在 OOpre 的博客中已经讲得较为详尽，大家具体可以参照这篇 Blog: <a href="https://cordial-kid.github.io/2026/03/04/OOPre%E9%97%AF%E5%85%B3%E6%9C%89%E6%84%9F/">OOpre闯关有感</a>。当然，在正课中如果遇到了新的有用的思想我会在这里跟大家补充分享的。</p><p>本着前人栽树，后人乘凉的原则，笔者希望可以将自己每单元较为成熟的架构和大家分享，帮助大家省去重构的麻烦。同时分享几个容易被hack的点，帮助大家在编程过程中规避bug。(如果大家的作业和2026年的很像的话希望可以帮助到大家)😁</p><h2 id="Unit1"><a href="#Unit1" class="headerlink" title="Unit1"></a>Unit1</h2><h3 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h3><p>本单元主要是表达式计算，表达式的计算主要分为三大步：<code>词法分析</code>,<code>语法分析</code>和<code>计算结果</code>。词法分析主要是将输入解析成token，语法分析主要是将token解析成抽象语法树，计算结果主要是通过访问抽象语法树来计算结果。具体的实现细节请看笔者的架构图。</p><p>这里是Unit1的设计的类：<br><img src="/images%5COOUnit1%E7%B1%BB%E5%9B%BE.png" alt="Unit1类图"></p><p>这里是Unit1架构简图：<br><img src="/images%5COOUnit1%E6%9E%B6%E6%9E%84%E7%AE%80%E5%9B%BE.png" alt="Unit1架构简图"></p><p>Mainclass是程序的入口，负责调用各个类来实现客户要求的功能。在Mainclass内部首先要调用的就是<code>input</code>类，负责从标准输入中读取表达式(<strong>这里强烈建议实现Input类</strong>)，然后将输入的表达式传递给<code>lexer</code>类进行词法分析，得到一个token列表，然后将token列表传递给<code>parser</code>类进行语法分析，得到一个抽象语法树，最后将抽象语法树调用<code>Poly</code>类进行计算，得到最终的结果并调用output类进行输出和优化(这里依旧强烈建议实现Output类)。</p><p>在上述过程中，无论是<code>选择表达式</code>、<code>求导</code>还是<code>函数</code>等等新增的功能都算作是 <strong>Factor</strong>。架构如此清晰，你怎么能错呢，牢弟？</p><h3 id="Unit1-Hack点"><a href="#Unit1-Hack点" class="headerlink" title="Unit1-Hack点"></a>Unit1-Hack点</h3><p>如果你有一个清晰的架构，你几乎不可能出现<code>WA</code>的问题，大多是TLE问题，但是这个问题真的很恶心，因为评测机几乎测不出来这种边界bug这就需要大家从往届学长留下的测试点中找点灵感了。</p><h4 id="hack点1：选择表达式"><a href="#hack点1：选择表达式" class="headerlink" title="hack点1：选择表达式"></a>hack点1：选择表达式</h4><p>我问你，<code>[(1==1) ? 1 : 一坨]</code>,后面这一坨你是算还是不算？算了的话那你几乎强测就会被挂掉了，所以我们只需要条件命中时计算，条件未命中时跳过就好，像这样：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"> <span class="hljs-type">Polynomial</span> <span class="hljs-variable">polyA</span> <span class="hljs-operator">=</span> leftCond.toPoly();<br> <span class="hljs-type">Polynomial</span> <span class="hljs-variable">polyB</span> <span class="hljs-operator">=</span> rightCond.toPoly();<br> <span class="hljs-type">Polynomial</span> <span class="hljs-variable">tmp</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Polynomial</span>();<br><br><span class="hljs-keyword">if</span> (polyA.equals(polyB)) &#123;<br>     tmp = trueBranch.toPoly();<br> &#125; <span class="hljs-keyword">else</span> &#123;<br>     tmp = falseBranch.toPoly();<br> &#125;<br></code></pre></td></tr></table></figure><h4 id="hack点2：递推函数"><a href="#hack点2：递推函数" class="headerlink" title="hack点2：递推函数"></a>hack点2：递推函数</h4><p>这里其实课程组并没有那么苛刻，提前存储之类确实是好办法，但是其实只要大家不用那种最笨的递归都可以过。完全可以用到再算啊，没问题的，只要你在算的过程中使用中间变量稍微存一下就行了，没那么多所谓的限制。</p><p>举个简单的例子：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">tmp = <span class="hljs-number">3</span>*a + <span class="hljs-number">4</span>*b<br>a=b<br>b=tmp<br></code></pre></td></tr></table></figure><p>It’s okay，完全没有问题的。</p><h4 id="hack点3：提公因式优化"><a href="#hack点3：提公因式优化" class="headerlink" title="hack点3：提公因式优化"></a>hack点3：提公因式优化</h4><p>提公因式其实是一个较为简单的优化，大家还是有必要实现的，但是在实现的过程中由于exp内部本质也是一个Polynomial，所以在实现化简过程中会反复调用optimizePoly和optimizeExp函数，导致递归过深，带来超时的问题。解决这个问题其实也很简单，引入一个memoMap就行，只要我们在化简过程中算出来过，就把他存下来，再用到的时候直接取值返回，就可以避免重复计算了。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> String <span class="hljs-title function_">optimizePoly</span><span class="hljs-params">(Polynomial polynomial)</span> &#123;<br>        Map&lt;Polynomial, String&gt; memo = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;();<br>        <span class="hljs-keyword">return</span> optimizePolyInternel(polynomial, memo);<br>    &#125;<br></code></pre></td></tr></table></figure><p>后续只要将momoMap传递下去就行了，具体的实现可以见笔者的个人仓库.这个memoMap的思想还是有必要学习一下的，毕竟在很多递归的场景下都可以用到，能够有效的减少重复计算，提高效率。</p><h3 id="关于优化"><a href="#关于优化" class="headerlink" title="关于优化"></a>关于优化</h3><p>这也是本单元我想讲的核心。我希望诸位在优化之前问自己几个问题：我为什么要优化？我要怎么优化？我的优化需要花掉我多少时间？如果你在思考了三个问题之后一拍桌子，“没问题，我就这么干了！”那你就去，遵从内心就好。</p><p>对于第一单元来说，我为什么优化？因为优化可以挣性能分，可以提升成绩。这是实打实的，咱不扯别的，做优化就是为了卷。那我要怎么优化呢，首先最简单的合并同类项，把系数为正的项提前。这是最好实现并且优化效果最明显的两步。接下来对 <code>exp</code> 的提公因式可以进一步优化某些表达式，最后还可以将exp的内容拆开分别提公因式，但是这个操作及其极其复杂，需要花费我大量的时间去实现。于是我选择了放弃最后一步优化，腾出时间来趁着春光正好出去走走，毕竟大学就四年，一直卷也没意义。当然这只是我的个人观点，可能有些同学实现最后一步优化就一顿饭的功夫，那你是大哥，你放开干；可能有些同学不拿这个性能分心里非常难受，那你也去干，总之遵从内心，在看到OO成绩时问心无愧就好。</p><p>所以，要不要优化，要优化到什么程度，还请诸位三思🫡。</p><h2 id="Unit2"><a href="#Unit2" class="headerlink" title="Unit2"></a>Unit2</h2><p>很多人觉得电梯月是魔鬼月哈，但是从个人角度来讲，我真的没觉得他和前一单元有什么区别。虽然我也不知道为什么，可能是对多线程的理解每个人不一样，抑或是我没有实现影子电梯？总之电梯没什么好怕的，<strong>Just Brave it!</strong>.</p><h3 id="三次作业的迭代要求"><a href="#三次作业的迭代要求" class="headerlink" title="三次作业的迭代要求"></a>三次作业的迭代要求</h3><p>第二单元三次作业的演进：第一次先建立基本线程模型，第二次把调度器和状态机做完整，第三次再把系统推进到双轿厢和换乘场景。</p><p>第一次作业中，我的主结构是 <code>InputThread -&gt; 全局 RequestQueue -&gt; DispatchThread -&gt; 各电梯局部 RequestQueue -&gt; Elevator</code>。这一版使用生产者消费者模型，把输入、分配、执行三层职责拆开，让每个电梯线程只关心自己的局部队列和轿厢内乘客。架构图如下<br><img src="/images/OO%E4%BA%8C%E4%B8%89%E4%BA%8B2.1%E6%9E%B6%E6%9E%84%E5%9B%BE.png" alt="2.1次作业架构图"></p><p>第二次作业加入维修请求后，系统不再只是“多部电梯并行跑”，因此我引入了 <code>ElevState</code>，把 <code>NORMAL</code>、<code>REP_ACCEPT</code>、<code>REPAIR</code>、<code>TEST</code> 等状态。与此同时，<code>DispatchThread</code> 开始真正承担“全局调度器”的角色，而不只是转发请求架构图如下：<br><img src="/images/OO%E4%BA%8C%E4%B8%89%E4%BA%8B2.2%E6%9E%B6%E6%9E%84%E5%9B%BE.png" alt="2.2次作业架构图"></p><p>第三次作业加入双轿厢升级和回收后，并发难点进一步上升。此时新增的 <code>ShaftContext</code>、<code>ShaftState</code>、<code>PassengerRegistry</code> 和 <code>isBusyMap</code> 让系统从“多线程执行”变成了“多线程共享资源并协同执行”。我觉得这也是二单元最核心的训练：不仅要会写线程，还要会给共享状态建模。最终的架构如下:<br><img src="/images/OO%E4%BA%8C%E4%B8%89%E4%BA%8B2.3%E6%9E%B6%E6%9E%84%E5%9B%BE.png" alt="2.3次作业架构图"></p><p>总体架构：<br><img src="/images%5COO%E4%BA%8C%E4%B8%89%E4%BA%8BUnit2%E6%9E%B6%E6%9E%84%E7%AE%80%E5%9B%BE.png" alt="Unit2架构简图"></p><h3 id="Unit2-Hack点"><a href="#Unit2-Hack点" class="headerlink" title="Unit2-Hack点"></a>Unit2-Hack点</h3><p>到了第二单元Hack点就变得多样化起来，不仅有TLE，还有各种各样的WA，每个人都有自己的原因，我把我出现的bug整理如下，如果能帮诸位在强测抢一点分那最好不过了。<strong>这里再说一句，强测很好过，恶心的数据点大多数来自互测，所以大家没必要对自己的强测太过担心</strong></p><h4 id="hack点1：电梯维修时输入乘客"><a href="#hack点1：电梯维修时输入乘客" class="headerlink" title="hack点1：电梯维修时输入乘客"></a>hack点1：电梯维修时输入乘客</h4><p>设想一种情况，假设输入数据的限制时间是50s以内，一共有6部电梯，在49.5s时候维修5部，50s的时候放入一堆从底层到顶层的请求。那么在这种情况下你的乘客会被分配给谁呢？如果全部分给了那部不在维修中的电梯那你不就超时了吗。</p><p>所以我们在电梯维修状态下也需要为其分配乘客，即向电梯的等待队列里面添加元素，但是要<strong>注意RECEIVE</strong>输出的时机，要符合题目的要求。</p><h4 id="hack点2：电梯队列的RECEIVE信息和增加元素信息的顺序"><a href="#hack点2：电梯队列的RECEIVE信息和增加元素信息的顺序" class="headerlink" title="hack点2：电梯队列的RECEIVE信息和增加元素信息的顺序"></a>hack点2：电梯队列的RECEIVE信息和增加元素信息的顺序</h4><p>如果先放入队列再输出RECEIVE信息像这样：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">queueMap.get(elevatorId).offer(request);<br>TimableOutput.println(<span class="hljs-string">&quot;RECEIVE-&quot;</span> + ((PersonRequest) request).getPersonId() + <span class="hljs-string">&quot;-&quot;</span> + elevatorId);<br></code></pre></td></tr></table></figure><p>在放入队列后电梯进程会立刻开始处理，这就意味着可能存在乘客已经被从1楼运送到2楼了，程序才慢吞吞地输出<code>我收到乘客请求了</code>的信息，显然这个不科学的。</p><h4 id="hack点3：双轿厢电梯碰撞"><a href="#hack点3：双轿厢电梯碰撞" class="headerlink" title="hack点3：双轿厢电梯碰撞"></a>hack点3：双轿厢电梯碰撞</h4><p>评测机判断双轿厢电梯是否碰撞主要看的是<strong>输出信息</strong>而不是访问电梯的状态，所以保证电梯不碰撞的模块必须是原子操作，尤其不能漏掉输出。示例代码如下：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> String <span class="hljs-title function_">arrive</span><span class="hljs-params">(<span class="hljs-type">int</span> elevatorId, <span class="hljs-type">int</span> targetFloor)</span> &#123;<br>    <span class="hljs-keyword">while</span> (!canArrive(elevatorId, targetFloor)) &#123;<br>        <span class="hljs-comment">// wait</span><br>    &#125;<br>    <span class="hljs-comment">// 更新楼层</span><br>    <span class="hljs-keyword">if</span> (elevatorId == mainId) &#123;<br>        <span class="hljs-built_in">this</span>.mainFloor = targetFloor;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-built_in">this</span>.backupFloor = targetFloor;<br>    &#125;<br>    notifyAll();<br>    <span class="hljs-comment">// TODO 输出Arrive信息，一定要保证输出也包含在里面</span><br>    <span class="hljs-keyword">return</span> Floor.toString(targetFloor);<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="hack点4：电梯wait无法醒来导致死循环"><a href="#hack点4：电梯wait无法醒来导致死循环" class="headerlink" title="hack点4：电梯wait无法醒来导致死循环"></a>hack点4：电梯wait无法醒来导致死循环</h4><p>我的程序的wait逻辑是在电梯等待队列为空并且电梯等待队列没有达到<code>end</code>状态时才会进入<code>wait</code>，然后等待requestqueue的notify来唤醒。这个看起来非常合理，但是我们可以看一下这个数据点：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">[1.0]UPDATE-1<br>[1.0]UPDATE-2<br>[1.0]UPDATE-3<br>[1.0]UPDATE-4<br>[1.0]UPDATE-5<br>[1.0]UPDATE-6<br>[2.0]1-WEI-60-FROM-F1-TO-F7<br>[15.0]RECYCLE-7<br>[15.0]RECYCLE-8<br>[15.0]RECYCLE-9<br>[15.0]RECYCLE-10<br>[15.0]RECYCLE-11<br>[15.0]RECYCLE-12<br>[15.5]2-WEI-60-FROM-F1-TO-F7<br></code></pre></td></tr></table></figure><p>在15秒的时候，只有电梯<code>1,2,3,4,5,6</code>可以被分配乘客请求，但是电梯处于<code>DOUBLE</code>状态，无法到达<code>F1</code>接客，所以在我的<code>Dispatch</code>算法中不会被分配乘客请求，导致电梯线程一直在wait，无法被唤醒，无法唤醒就无法切换状态，无法切换状态就无法被分配乘客请求，形成死循环。</p><p><img src="/images%5COO%E4%BA%8C%E4%B8%89%E4%BA%8B%E6%AD%BB%E5%BE%AA%E7%8E%AF.png" alt="死循环"></p><p>我的切换状态函数只在电梯<code>run</code>的情况下才可以执行：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>        syncStateWithShaft();     <span class="hljs-comment">// 状态切换</span><br>        <span class="hljs-comment">// 其他逻辑</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>解决方法比较简单的就是让电梯在wait的时候不要无限等待被唤醒，而是每隔一段时间就自己醒来检查一下状态<code>wait(20)</code>，这样就可以避免死循环了。</p><h4 id="hack点5：判断条件缺失导致Dispatch提前终止"><a href="#hack点5：判断条件缺失导致Dispatch提前终止" class="headerlink" title="hack点5：判断条件缺失导致Dispatch提前终止"></a>hack点5：判断条件缺失导致Dispatch提前终止</h4><p>在判断Dispatch结束的条件时我考虑了三个情况：</p><ul><li>全局队列为空</li><li>输入结束</li><li>没有电梯处于busy(特殊)状态</li></ul><p>细细想来有一个致命的漏洞，当全局队列为空并且电梯为double态时，是不是满足上述条件呢？当然，此时<code>Dispatch</code>终止，如果电梯在F2放下了换乘乘客，那这个乘客就永远无法被调度了。顺理成章，我们加入两个判断条件：</p><ul><li>电梯里面有人 <code>!elevator.isEmpty()</code></li><li>电梯的等候队列有人 <code>!queueMap.get(elevatorId).isEmpty()</code></li></ul><p>到这里似乎上述问题已经得到了解决，更阴间的还在后面。。。（真的是卡bug届领域的大神啊，我真的服了）</p><p>乘客从电梯等候队列移出，还没有放到电梯里面，这个空档期就是Dispatch停下来的绝佳时期。显然，这一顿修改下来无异于是在堆屎山，但是因为这个最后一次作业了，我选择继续打补丁，新增<code>handlingPassenger</code>变量来管理上下客：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">open</span><span class="hljs-params">()</span> &#123;<br>    handlingPassenger = <span class="hljs-literal">true</span>;   <br>    <span class="hljs-comment">// 上客逻辑</span><br>    handlingPassenger = <span class="hljs-literal">false</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>只有在<code>handlingPassenger</code>为<code>false</code>时才能停下<code>Dispatch</code></p><h4 id="hack点6：CPU等待时轮询导致CPU运行超时"><a href="#hack点6：CPU等待时轮询导致CPU运行超时" class="headerlink" title="hack点6：CPU等待时轮询导致CPU运行超时"></a>hack点6：CPU等待时轮询导致CPU运行超时</h4><p>在<code>Dispatch</code>逻辑中，如果全局队列空了错误写法是这样的：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Request</span> <span class="hljs-variable">request</span> <span class="hljs-operator">=</span> requests.poll();<br><span class="hljs-keyword">if</span> (request == <span class="hljs-literal">null</span>) &#123;<br>    <span class="hljs-keyword">continue</span>;<br>&#125;<br>dispatch(request);<br></code></pre></td></tr></table></figure><p>发现<code>null</code>立刻轮询，导致CPU一直在运行，最终被评测机挂掉了。正确的写法应该是：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Request</span> <span class="hljs-variable">request</span> <span class="hljs-operator">=</span> requests.poll();<br><span class="hljs-keyword">if</span> (request == <span class="hljs-literal">null</span>)  &#123;<br>    sleepBriefly();<br>    <span class="hljs-keyword">continue</span>;<br>&#125;<br>dispatch(request);<br></code></pre></td></tr></table></figure><p>以上就是第二单元笔者认为比较容易挂的几个互测数据点了，希望能对大家的开发过程有点帮助吧。最后还是那句话，强测很好过，恶心的数据点大多数来自互测，所以大家没必要对自己的基础分数太过担心，<strong>just do it！</strong></p><h2 id="Unit3"><a href="#Unit3" class="headerlink" title="Unit3"></a>Unit3</h2><p>第三单元的主题是<code>JML</code>与规格驱动开发，总体来说没什么难点，只需要照着课程组给出的<code>JML</code>来翻译成代码即可。其实你甚至不需要知道整个项目是什么意思，像个<strong>傻子</strong>一样翻译就行。</p><h2 id="Unit4"><a href="#Unit4" class="headerlink" title="Unit4"></a>Unit4</h2><p>第四单元考察的是UML建模思想。依次学习了类图，状态图，顺序图的建模。为了加深大家对这三类UML图的理解，我在这里简单的陈述一下具体的知识点。</p><h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><p>UML类图主要用于展示类，类的属性，类的方法，以及类之间的静态关系。类的表示方法为三层矩形框：</p><ul><li>第一层为类名</li><li>第二层为类的属性</li><li>第三层为类的方法</li></ul><p>常用的标示符：</p><ul><li>+public</li><li>-private</li><li>#protected</li></ul><p>示例如下：</p><p><img src="/images%5CUML%E7%B1%BB%E5%9B%BE.png" alt="UML类图"></p><h4 id="类与类之间的6种关系"><a href="#类与类之间的6种关系" class="headerlink" title="类与类之间的6种关系"></a>类与类之间的6种关系</h4><ul><li>依赖（Dependency）：一个类临时用到另一个类。比如A类的某一个方法把B类作为参数传入。</li><li>关联（Association）：类和类有长期的服务关系。比如A类把B类作为全局变量。</li><li>聚合（Aggregation）:整体和部分的关系，部分可以离开整体单独存在。比如A类包含B类，但是B类的生命周期不归A类管（汽车和轮胎）。</li><li>组合（Composition）：整体和部分的关系，部分不能离开整体而存在，整体对部分强拥有。比如A类包含B类，并且B随着A的创建而创建，销毁而销毁（人和大脑）。</li><li>实现（Implementation）：类实现接口。等于Java中的<code>impements</code></li><li>继承（Generalization）：子类继承父类,A类继承B类。等于Java中的<code>extends</code></li></ul><p><img src="/images%5C%E7%B1%BB%E7%9A%84%E5%85%B3%E7%B3%BB.png" alt="类图的6种关系"></p><h3 id="状态图"><a href="#状态图" class="headerlink" title="状态图"></a>状态图</h3><p>UML状态图用于描述一个特定的对象在其生命周期中所经历的各种状态。主要包括以下四个元素：</p><ul><li>初始状态</li><li>状态</li><li>转换</li><li>终止状态</li></ul><p>状态转换三要素：</p><ul><li>Trigger（必填）：触发状态改变的函数。</li><li>Guard（非必填）：只有当bool值为真时转换才能发生。</li><li>Action（非必填）：状态转换时顺便执行什么操作。</li></ul><h3 id="顺序图"><a href="#顺序图" class="headerlink" title="顺序图"></a>顺序图</h3><p>顺序图按照时间顺序，展示了对象之间互相发送消息，协同完成某个任务的流程，主要包括以下x个要素：</p><ul><li>对象</li><li>消息</li><li>时间轴</li></ul><p>不同对象之间传递消息，并且时间自上而下地流逝，越靠下的消息发生的时间越晚。</p><p>其中消息类型有以下四种：</p><ul><li>同步消息：发送方发送请求后，必须等待接收方返回数据才能继续进行</li><li>异步消息：发送方发送请求后，无需等待接收方返回数据就能继续进行</li><li>返回消息：接受方处理完同步消息后，将结果返还给发送方。</li><li>自身调用：对象自己调用自己内部的方法。</li></ul><p><strong>实际上画的时候用Message让人看懂就行了</strong>😁</p><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>行文至此，本学期的OO博客要跟大家说再见了，我们从递归下降法，闯关到多线程，最后归结到JML规格开发和UML类图设计。OO的核心目的并不是把我们练成coding大神，而是通过课程的学习掌握面向对象设计的思想，培养工程能力，为日后走向工作岗位打下坚实的基础。希望以上的知识能带给大家些许帮助，抑或是繁忙的学习生活中的一点慰藉。Cordial-Kid与你同在🫡。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">你会翻过山看到万丈晴天<br>飞鸟正越过海面<br>你会迎着风放着胆唱着歌<br>把风景都看遍<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> OO </category>
          
      </categories>
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> BUAA </tag>
            
            <tag> OO </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-OO」&quot;浅谈&quot;OOPre</title>
      <link href="/2026/03/04/OOPre%E9%97%AF%E5%85%B3%E6%9C%89%E6%84%9F/"/>
      <url>/2026/03/04/OOPre%E9%97%AF%E5%85%B3%E6%9C%89%E6%84%9F/</url>
      
        <content type="html"><![CDATA[<p>OOPre 作为具有北航计算机学院特色的 Pre 系列课程，以其不俗的码量和难以捉摸的 JUnit 让每个 6 系学子爱之深而恨之切。我们在 OOPre 课程中的学习重点不在于 Java 复杂语法的掌握，而在于面向对象编程思想的初步建立，从而为我们之后的 OO 正课学习打下基础。个人感觉对于 Java 小白来说还是受益匪浅。</p><p>本文希望通过阐述面向对象编程常用的四大特性：<strong>封装</strong>、<strong>继承</strong>、<strong>多态</strong>和<strong>抽象</strong>，以及<strong>设计模式</strong>、<strong>递归下降法</strong>等内容来帮助大家理解 OOPre 课程的核心内容。由于大家时间有限，本文不会大量堆砌 OOPre 课程代码，欲知 2025 年秋季学期 OOPre 课程完整代码可以看笔者的个人 <a href="https://github.com/Cordial-Kid/BUAA-OO">GitHub 仓库</a>。</p><p>同时本文也适合想要入门 Java 的同学，最后的 <strong>Java 项目编程规范</strong>可以帮助大家写出更优雅的代码。</p><h2 id="面向对象编程课程内容简介"><a href="#面向对象编程课程内容简介" class="headerlink" title="面向对象编程课程内容简介"></a>面向对象编程课程内容简介</h2><h3 id="四大特性"><a href="#四大特性" class="headerlink" title="四大特性"></a>四大特性</h3><h4 id="封装"><a href="#封装" class="headerlink" title="封装"></a>封装</h4><p>封装是面向对象编程的基本特性之一，相对易于理解，它将数据和操作数据的方法绑定在一起，隐藏了对象的内部实现细节。通过封装，我们可以保护对象的状态不被外部直接访问，从而提高代码的安全性和可维护性。在Java中，封装通常通过访问修饰符（如private、protected、public）来实现。<strong>一句话</strong>：封装就是把属性私有化，通过公共方法来访问和修改属性。</p><p>看代码：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">String</span> <span class="hljs-variable">name</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;JACK&quot;</span>;   <span class="hljs-comment">//name不可以在Person类外部访问</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span> &#123;       <span class="hljs-comment">//可以通过public get方法在外部访问</span><br>        <span class="hljs-keyword">return</span> name;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h4><p>继承是面向对象编程的另一个重要特性，它允许我们创建一个新类（子类）来继承一个已有类（父类）的<strong>属性</strong>和<strong>方法</strong>。同时子类也可以<strong>重写</strong>(Override)父类的方法或者定义子类特有的方法。通过继承，我们可以实现代码的重用和扩展，从而提高开发效率。在Java中，继承通过关键字<code>extends</code>来实现。<strong>一句话</strong>：继承就是子类自动拥有父类的属性和方法。</p><p>看代码：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Animal</span> &#123;<br>    String food ;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Animal</span><span class="hljs-params">(String food)</span> &#123;<br>        <span class="hljs-built_in">this</span>.food = food;<br>    &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">eat</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;I like eat &quot;</span> + food);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Dog</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Animal</span> &#123;<br>    <span class="hljs-keyword">private</span> String gender;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Dog</span><span class="hljs-params">(String food,String gender)</span> &#123;<br>        <span class="hljs-comment">//调用父类的构造方法，必须放在第一行</span><br>        <span class="hljs-built_in">super</span>(food); <br>        <span class="hljs-built_in">this</span>.gender = gender;<br>    &#125;<br>    <span class="hljs-comment">//重写父类的方法</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">eat</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;Dog like eat &quot;</span> + food);<br>    &#125;<br>    <span class="hljs-comment">//定义自己方法</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">showGender</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;My gender is &quot;</span> + gender);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在Main函数中使用时，编译器会优先调用子类的方法，如果子类没有重写父类方法会调用父类方法，但是如果父类也没有这个方法就会喜提<code>Cannot resolve method</code>。</p><h4 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h4><p>多态允许我们通过父类的引用来指向子类的对象，从而实现同一操作在不同对象上的不同表现形式。多态的实现方式主要有两种<code>运行时多态</code>和<code>编译时多态</code>。</p><p><code>运行时多态</code>:通过方法重写实现,即子类重写父类的方法，在运行时根据对象的实际类型来决定调用哪个方法。</p><p>看代码：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">speak</span><span class="hljs-params">()</span>&#123;<br>        System.out.println(<span class="hljs-string">&quot;Hello I am a person&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Person</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">speak</span><span class="hljs-params">()</span>&#123;<br>        System.out.println(<span class="hljs-string">&quot;Hello I am a student&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Main</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">Person</span> <span class="hljs-variable">p1</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Person</span>();<br>        <span class="hljs-type">Person</span> <span class="hljs-variable">p2</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Student</span>(); <span class="hljs-comment">//父类引用指向子类对象</span><br>        p1.speak(); <span class="hljs-comment">//输出: Hello I am a person</span><br>        p2.speak(); <span class="hljs-comment">//输出: Hello I am a student</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>编译时多态</code>:通过方法重载实现,即在同一个类中定义多个同名但参数不同的方法，编译器根据传入参数的类型和数量来决定调用哪个方法。</p><p>看代码：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MathUtils</span> &#123;<br>    <span class="hljs-comment">//方法重载</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span> &#123;<br>        <span class="hljs-keyword">return</span> a + b;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">double</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">double</span> a, <span class="hljs-type">double</span> b)</span> &#123;<br>        <span class="hljs-keyword">return</span> a + b;<br>    &#125;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Main</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">MathUtils</span> <span class="hljs-variable">mu</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MathUtils</span>();<br>        System.out.println(mu.add(<span class="hljs-number">2</span>, <span class="hljs-number">3</span>)); <span class="hljs-comment">//调用int版本，输出: 5</span><br>        System.out.println(mu.add(<span class="hljs-number">2.5</span>, <span class="hljs-number">3.5</span>)); <span class="hljs-comment">//调用double版本，输出: 6.0</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="抽象"><a href="#抽象" class="headerlink" title="抽象"></a>抽象</h4><p>抽象即隐藏具体的实现细节，只向外暴露清晰的使用接口。实现抽象主要有两种方法：<strong>抽象类</strong>和<strong>接口</strong>。</p><p><code>抽象类</code>：<br>当我们发现一些类之间有共同的行为，但是这些行为在每个类中的具体实现方式不同，我们可以将这些共同的行为提取到一个抽象类中，并定义为抽象方法。</p><blockquote><p>抽象类不能被实例化，只能被继承。<br>抽象类中可以包含抽象方法和具体方法。<br>抽象方法必须被子类重写。除非子类也是抽象类。</p></blockquote><p>看代码:</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Animal</span> &#123;<br>    <span class="hljs-comment">//具体方法</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">breathe</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;Animal is breathing&quot;</span>);<br>    &#125;<br>    <span class="hljs-comment">//抽象方法:所有动物都会叫但是叫声不同，这里只申明不实现</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">makeSound</span><span class="hljs-params">()</span>;<br>&#125; <br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Cat</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Animal</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">makeSound</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;Meow&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>注意，抽象类只是不能被实例化，剩下的和普通类没什么区别，可以有构造方法、属性、具体方法等。</p></blockquote><p><code>接口</code>：<br>接口是一种更纯粹的抽象形式，他只包含方法的声明，没有任何实现。一句话：接口定义了一系列方法，这个方法必须被实现接口的类所实现。</p><blockquote><p>接口不可以被实例化,只能被实现。<br>但是接口可以实例化他的实现类。</p></blockquote><p>看代码:</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Flyable</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">fly</span><span class="hljs-params">()</span>;<br>&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Bird</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Flyable</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">fly</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;Bird is flying&quot;</span>);<br>    &#125;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Main</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">Flyable</span> <span class="hljs-variable">f</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Bird</span>(); <span class="hljs-comment">//接口引用指向实现类对象</span><br>        f.fly(); <span class="hljs-comment">//输出: Bird is flying</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>注意，这里解释一下<code>Flyable f = new Bird()</code>,很多同学可能会有疑问，为什么不定义成<code>Bird f = new Bird()</code>呢？答案式为了实现代码解耦，提高扩展性。下面我们看一下两种实现方式的区别：</p><p>&#x2F;&#x2F;方式一:接口引用指向实现类对象<br>如果我们未来想把 <code>f</code>换成一个<code>Plane</code>类的对象，只要<code>Plane</code>实现了<code>Flyable</code>接口，我们只需要直接将<code>new Bird()</code>改成<code>new Plane()</code>即可，而不需要修改其他代码。</p><p>&#x2F;&#x2F;方式二:具体类引用<br>我们需要修改所有用到<code>f</code>的地方，把<code>Bird</code>全改成<code>Plane</code>,改着改着就错了😥。<del>不信可以试试</del></p><h3 id="设计模式"><a href="#设计模式" class="headerlink" title="设计模式"></a>设计模式</h3><h4 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h4><p>单例模式确保一个类只能有一个实例，并提供一个全局访问点。简单来说就是<strong>利用唯一性来优化资源利用，保证数据一致</strong>。这也就决定了单例模式的几个特性：</p><ol><li>私有构造方法：防止外部通过new关键字创建多个实例。</li><li>私有静态变量：用于保存唯一的实例。</li><li>公有静态方法，提供一个全局访问点来获取实例。</li></ol><p>单例模式的实现方式根据<strong>单例实例化时机</strong>的不同分为<code>饿汉式</code>和<code>懒汉式</code>。</p><p><code>饿汉式</code>: 在类加载时就创建实例，不管是否会用到，优点是线程安全，缺点是可能造成资源浪费。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Stone</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Stone</span> <span class="hljs-variable">stone</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Stone</span>();<br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">Stone</span><span class="hljs-params">()</span> &#123;&#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Stone <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> stone;<br>    &#125;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Main</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">Stone</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> Stone.getInstance();<br>    &#125; <br>&#125;<br></code></pre></td></tr></table></figure><p><code>懒汉式</code>: 在第一次调用获取实例的方法时才创建实例，优点是节省资源，缺点是需要处理线程安全问题。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Stone</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Stone stone;<br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">Stone</span><span class="hljs-params">()</span> &#123;&#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Stone <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">if</span> (stone == <span class="hljs-literal">null</span>) &#123;<br>            stone = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Stone</span>();<br>        &#125;<br>        <span class="hljs-keyword">return</span> stone;<br>    &#125;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Main</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">Stone</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> Stone.getInstance();<br>    &#125; <br>&#125;<br></code></pre></td></tr></table></figure><p>这里具体说明一下：</p><blockquote><p>线程安全问题具体指的是如果多个线程同时调用<code>getInstance()</code>方法，可能会创建多个实例，违背了单例模式的初衷。解决方法有很多，比如使用<code>synchronized</code>关键字来锁定方法：<code>public static synchronized Stone getInstance() &#123;...&#125;</code>。大家只做了解即可，现阶段还不会用到。</p></blockquote><h4 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h4><p>工厂模式通过定义一个接口或抽象类来创建对象，而不是直接实例化具体的类。这样可以将对象的创建逻辑与使用逻辑分离，提高代码的灵活性和可维护性。工厂模式主要有三种类型：<code>简单工厂模式</code>、<code>工厂方法模式</code>和<code>抽象工厂模式</code>。核心价值在于解耦和扩展性。</p><p>我们先来看一个没有工厂的情景：假设你需要创建不同类型的手机对象，直接在代码中<code>new</code>实例：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Phone</span> <span class="hljs-variable">Huawei</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HuaweiPhone</span>();<br><span class="hljs-type">Phone</span> <span class="hljs-variable">Apple</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ApplePhone</span>();<br></code></pre></td></tr></table></figure><p>问题很明显：</p><ol><li><strong>耦合严重</strong>：使用者必须知道具体的实现类（HuaweiPhone、Iphone），如果后续替换手机类型（如换成小米），需要修改所有<code>new</code>的地方；</li><li><strong>扩展性差</strong>：新增手机类型时，需要修改所有使用方的代码，违反 “开放 &#x2F; 封闭原则”。</li></ol><p>工厂模式的作用就是解决这些问题,把对象创建的”复杂活”交给工厂，使用者只需要”提需求”。</p><p><code>简单工厂模式</code>(静态工厂模式): 通过一个工厂类根据传入的参数决定创建哪种具体产品类的实例。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">PhoneFactory</span>&#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Phone <span class="hljs-title function_">createPhone</span><span class="hljs-params">(String type)</span>&#123;<br>        <span class="hljs-keyword">if</span> (type.equals(<span class="hljs-string">&quot;huawei&quot;</span>)) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HuaweiPhone</span>();<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (type.equals(<span class="hljs-string">&quot;...&quot;</span>)) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> ...();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>工厂方法模式</code>:为了解决简单工厂的”职责过重”问题，工厂方法模式将”创建不同产品”的逻辑，拆分到多个具体工厂类中，每个工厂只负责创建一种产品。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">PhoneFactory</span> &#123;<br>    Phone <span class="hljs-title function_">createPhone</span><span class="hljs-params">()</span>;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">HuaweiFactory</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">PhoneFactory</span>()&#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Phone <span class="hljs-title function_">createPhone</span><span class="hljs-params">()</span>&#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HuaweiPhone</span>();<br>    &#125;<br>&#125;<br><span class="hljs-comment">//Main函数中要用华为手机</span><br><span class="hljs-type">PhoneFactory</span> <span class="hljs-variable">huaweiFactory</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HuaweiFactory</span>();<br><span class="hljs-type">Phone</span> <span class="hljs-variable">huawei</span> <span class="hljs-operator">=</span> huaweiFactory.createPhone();<br></code></pre></td></tr></table></figure><p><code>抽象工厂模式</code>: 抽象工厂模式进一步将工厂方法模式进行抽象，提供一个接口用于创建一系列相关或相互依赖的对象，而无需指定它们具体的类。一个具体工厂负责创建一组相关产品。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">ElectronicFactory</span> &#123;<br>    Phone <span class="hljs-title function_">createPhone</span><span class="hljs-params">()</span>; <span class="hljs-comment">// 创建手机</span><br>    Earphone <span class="hljs-title function_">createEarphone</span><span class="hljs-params">()</span>; <span class="hljs-comment">// 创建耳机</span><br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">HuaweiFactory</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ElectronicFactory</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Phone <span class="hljs-title function_">createPhone</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HuaweiPhone</span>(); &#125;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Earphone <span class="hljs-title function_">createEarphone</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HuaweiEarphone</span>(); &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h4><p>观察者模式定义了一种<strong>一对多</strong>的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都会得到通知并自动更新。观察者模式主要包含两个角色：<code>主题（Subject）</code>和<code>观察者（Observer）</code>。</p><p>主题对象只需要知道它有哪些观察者，而不需要知道每个观察者的具体实现细节。同样，观察者也只需要关注主题对象的状态变化，而不需要知道主题对象的内部逻辑。这就极大程度地实现了代码的解耦。</p><p>为了清楚的阐释观察者模式的实现方式，我们以气象站为例，温度，湿度，气压显示器为观察者。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Subject</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerObserver</span><span class="hljs-params">(Observer o)</span>;   <span class="hljs-comment">//添加观察者</span><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">removeObserver</span><span class="hljs-params">(Observer o)</span>;     <span class="hljs-comment">//移除观察者</span><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">notifyObservers</span><span class="hljs-params">()</span>;              <span class="hljs-comment">//通知观察者</span><br>&#125;<br><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Observer</span>&#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">updata</span><span class="hljs-params">(<span class="hljs-type">float</span> temperature,<span class="hljs-type">float</span> humidity,<span class="hljs-type">float</span> pressure)</span>;    <span class="hljs-comment">//观察者响应主题</span><br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">WeatherData</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Subject</span> &#123;<br>    <span class="hljs-keyword">private</span> List&lt;Observer&gt; observers;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">float</span> temperature;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">float</span> humidity;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">float</span> pressure;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">WeatherData</span><span class="hljs-params">()</span>&#123;<br>        observers =  <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>    &#125;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerObserver</span><span class="hljs-params">(Observer o)</span> &#123;<br>        observers.add(o);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">removeObserver</span><span class="hljs-params">(Observer o)</span> &#123;<br>        observers.remove(o);<br>    &#125;<br><br>    <span class="hljs-comment">//这里大概率是图的知识，可以参照一下OOpre课程代码的notifyEmployees和notifyAllEmployees函数。</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">notifyObservers</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">for</span> (Observer o : observers) &#123;<br>            o.update(temperature, humidity, pressure);<br>        &#125;<br>    &#125;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Table</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Observer</span>&#123;<br>    <span class="hljs-comment">//在Observer中可以和Subject建立联系(雇佣关系的帮助)，也可以自己执行某些指令</span><br>    <span class="hljs-meta">@Override</span> <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">updata</span><span class="hljs-params">(<span class="hljs-type">float</span> temperature,<span class="hljs-type">float</span> humidity,<span class="hljs-type">float</span> pressure)</span>&#123;<br>        <span class="hljs-comment">//....</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="递归下降法简介"><a href="#递归下降法简介" class="headerlink" title="递归下降法简介"></a>递归下降法简介</h3><p>递归下降法是一种自顶向下的<strong>语法分析</strong>方法，通常用于编译器和解释器中。它通过递归地调用函数来处理输入的符号序列，从而构建语法树。递归下降法的核心思想是将每个非终结符对应一个函数，这些函数根据文法规则来解析输入。<strong>一句话</strong>:递归下降法就是用函数调用模拟语法规则的推导过程。</p><p>这个说法有点抽象，大家可能云里雾里，我们看一个简单的例子：</p><p>这里我们以雇佣关系为例，A(B(E(G),F),C,D)表示，A雇佣B,C,D，B雇佣E,F，E雇佣G。</p><p><strong>形式化表述</strong>：</p><ul><li><p>冒险者 → 标识符 [被雇佣者]</p></li><li><p>被雇佣者 → ‘(‘ 冒险者序列 ‘)’</p></li><li><p>冒险者序列 → 冒险者 {‘,’ 冒险者}</p></li><li><p>标识符 → 数字 | 字母 | ‘_’ [标识符]</p></li><li><p>数字 → ‘0’ | ‘1’ … ‘9’</p></li><li><p>字母 → ‘a’ | ‘b’ … | ‘z’ | ‘A’ | ‘B’ …| ‘Z’ 其中</p></li><li><p>{} 表示允许存在 0 个、1 个或多个。</p></li><li><p>[] 表示允许存在 0 个或 1 个。</p></li></ul><p><strong>变量说明</strong>：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Token:词法单元，是指从源代码中分解出来的、具有独立语法意义的最小单元。<br>Lexer:词法分析器，核心功能是读取字符，输出 Token。<br></code></pre></td></tr></table></figure><p>看代码：</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Lexer</span> &#123;<br>    <span class="hljs-comment">//存储原始字符串</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String input;<br>    <span class="hljs-comment">//标记当前解析到的位置</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">pos</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-comment">//当前识别出的Token</span><br>    <span class="hljs-keyword">private</span> String curToken;<br><br>    <span class="hljs-comment">//辅助方法，删除空格</span><br>    <span class="hljs-keyword">private</span> String <span class="hljs-title function_">removeWhiteSpace</span><span class="hljs-params">(String s)</span> &#123;<br>        <span class="hljs-keyword">return</span> s.replaceAll(<span class="hljs-string">&quot;\\s+&quot;</span>,<span class="hljs-string">&quot;&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-comment">//辅助方法，判断标识符是否是合法字符</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isIdChar</span><span class="hljs-params">(<span class="hljs-type">char</span> c)</span> &#123;<br>        <span class="hljs-keyword">return</span> Character.isLetter(c) || Character.isDigit(c) || c == <span class="hljs-string">&#x27;_&#x27;</span>;<br>    &#125;<br><br>    <span class="hljs-comment">//辅助方法，读取完整标识符</span><br>    <span class="hljs-keyword">private</span> String <span class="hljs-title function_">getId</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">StringBuilder</span> <span class="hljs-variable">sb</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>();   <span class="hljs-comment">//用于拼接标识符</span><br>        <span class="hljs-keyword">while</span>(pos &lt; input.length() &amp;&amp; isIdChar(input.charAt(pos))) &#123;<br>            sb.append(input.charAt(pos));<br>            pos++;<br>        &#125;<br>        <span class="hljs-keyword">return</span> sb.toString();   <span class="hljs-comment">//返回拼接好的完整标识符</span><br>    &#125;<br><br>    <span class="hljs-comment">//核心方法，读取下一个Token</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">next</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">if</span> (pos == input.length()) &#123;<br>            curToken = <span class="hljs-literal">null</span>;<br>            <span class="hljs-keyword">return</span>;<br>        &#125;<br><br>        <span class="hljs-comment">//获取当前指针位置的字符</span><br>        <span class="hljs-type">char</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> input.charAt(pos);<br><br>        <span class="hljs-keyword">if</span> (isIdChar(c)) &#123;<br>            curToken = getId();<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(c == <span class="hljs-string">&#x27;(&#x27;</span> || c == <span class="hljs-string">&#x27;)&#x27;</span> || c == <span class="hljs-string">&#x27;,&#x27;</span>)&#123;<br>            pos += <span class="hljs-number">1</span>;                         <span class="hljs-comment">//这里要保证pos到curToken的下一位</span><br>            curToken = String.valueOf(c);     <span class="hljs-comment">//特殊字符单独作Token</span><br>        &#125;<br>        <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;Unexpected Character&quot;</span> + c);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">//初始化Lexer</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Lexer</span> <span class="hljs-params">(String input)</span> &#123;<br>        <span class="hljs-built_in">this</span>.input = removeWhiteSpace(input);<br>        next();<br>    &#125;<br><br>    <span class="hljs-comment">//查看当前Token，并且不移动指针</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">peek</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> curToken;<br>    &#125;<br><br>    <span class="hljs-comment">//匹配目标方法</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">match</span><span class="hljs-params">(String target)</span> &#123;<br>        <span class="hljs-keyword">if</span> (target.equals(curToken)) &#123;<br>            next();                 <span class="hljs-comment">//如果匹配，就消耗，并准备好下一个Token</span><br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;Excepted token :&quot;</span> + target + <span class="hljs-string">&quot;, but got: &quot;</span>+ curToken);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>以上是<code>Lexer</code>的完整代码，主要功能就是依次拆解出<code>Token</code>，然后应用到<code>Main</code>函数。注意一下<code>match</code>和<code>peek</code>的区别，<code>match</code>自带<code>next</code>方法，<code>peek</code>只监测<code>curToken</code>。同时<code>pos</code>永远指向即将解析的<code>Token</code>。</p><p>在<code>Main</code>中调用时需要写一个<strong>递归函数</strong><code>parseEmployees</code>来调用解析器。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">parseEmployees</span><span class="hljs-params">(Lexer lexer,String employer)</span> &#123;<br>    <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">token</span> <span class="hljs-operator">=</span> lexer.peek();<br>        <span class="hljs-keyword">if</span> (token == <span class="hljs-literal">null</span> || token.equals(<span class="hljs-string">&#x27;)&#x27;</span>)) &#123;<br>            <span class="hljs-keyword">break</span>;     <br>        &#125;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">employee</span> <span class="hljs-operator">=</span> token;<br>        lexer.next();<br><br>        <span class="hljs-comment">//在这里建立employee和employer的关系</span><br><br>        <span class="hljs-keyword">if</span>(<span class="hljs-string">&#x27;(&#x27;</span>.equals(lexer.peek())) &#123;<br>            lexer.match(<span class="hljs-string">&#x27;(&#x27;</span>);<br>            parseEmployees(lexer,employee);<br>            lexer.match(<span class="hljs-string">&#x27;)&#x27;</span>);<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-string">&#x27;,&#x27;</span>.equals(lexer.peek())) &#123;<br>            lexer.match(<span class="hljs-string">&#x27;,&#x27;</span>);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>递归函数退出的时候条件是<code>)</code>,因此我们需要用<code>match</code>方法消耗掉这个<code>token</code>,从而继续向后执行。</p><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">Main:<br>    <span class="hljs-type">Lexer</span> <span class="hljs-variable">lexer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Lexer</span>(input);     <span class="hljs-comment">//初始化时将pos带到了待解析位</span><br>    <span class="hljs-type">String</span> <span class="hljs-variable">topEmployer</span> <span class="hljs-operator">=</span> lexer.peek();<br>    lexer.next();<br>    lexer.match(<span class="hljs-string">&quot;(&quot;</span>);<br>    parseEmployees(lexer, topEmployer);<br>    lexer.match(<span class="hljs-string">&quot;)&quot;</span>); <br></code></pre></td></tr></table></figure><h3 id="面向对象编程总结"><a href="#面向对象编程总结" class="headerlink" title="面向对象编程总结"></a>面向对象编程总结</h3><p>以上就是OOPre课程的核心内容简介，如果你看到这里，那么恭喜你应该可以速通OOPre,面向对象编程的思想方法是我们以后学习编程的基础，希望大家能理解并灵活运用到实际编程中。接下来我们将进入Java项目编程规范的介绍，这里我们采用阿里巴巴Java开发手册作为参考标准。</p><hr><h2 id="Java项目编程规范简介"><a href="#Java项目编程规范简介" class="headerlink" title="Java项目编程规范简介"></a>Java项目编程规范简介</h2><h3 id="命名风格"><a href="#命名风格" class="headerlink" title="命名风格"></a>命名风格</h3><ul><li>类名使用<strong>大驼峰</strong>命名法，例：XmlService。</li><li>方法名，参数名，变量名使用<strong>小驼峰</strong>命名法，例：getUserName。</li><li>常量名使用全大写字母，单词之间用下划线分隔，例：MAX_SIZE。</li><li>中括号是数组定义的一部分，不能与类型分开，例：int[] arr。</li><li>在POJO类中，布尔类型变量不要使用is开头，例：boolean active; 而不是 Boolean isActive;。</li><li>接口类中的方法属性不要任何修饰变量，public也不要加。</li></ul><h3 id="代码格式"><a href="#代码格式" class="headerlink" title="代码格式"></a>代码格式</h3><ul><li>大括号为空写成<code>&#123;&#125;</code>,不换行。大括号非空时写法参考代码示例。</li><li>左&#x2F;右小括号和字符之间不要有空格。</li><li>if&#x2F;for&#x2F;while&#x2F;switch等关键字与左小括号之间要有一个空格。</li><li>任何二目，三目运算符前后都要有空格。</li></ul><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">str</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;Hello World&quot;</span>;    <span class="hljs-comment">// 二目运算符前后有空格</span><br>    <span class="hljs-type">int</span> <span class="hljs-variable">flag</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-comment">// if关键字与左小括号之间有空格,括号内 f 与左括号， 0 与右括号之间没有空格</span><br>    <span class="hljs-keyword">if</span> (flag == <span class="hljs-number">0</span>) &#123;<br>        System.out.println(str);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;Flag is not zero&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><p>单行字符限制不超过120个字符，超过时需要换行。换行规则如下：</p><ol><li>第二行相对第一行缩进4个空格，从第三行开始不再继续缩进。</li><li>运算符与上下文一起换行。</li><li>方法调用的点符号与<strong>下文</strong>一起换行。</li><li>如果多个参数超长，在逗号后换行，逗号与下文一起换行。</li><li>括号前不要换行。</li></ol></li><li><p>当传入多个参数时，每个参数的逗号后面要加空格。</p></li></ul><h3 id="面向对象变成规范"><a href="#面向对象变成规范" class="headerlink" title="面向对象变成规范"></a>面向对象变成规范</h3><ul><li>类内方法定义的顺序是：公有方法或保护方法 &gt; 私有方法 &gt; getter&#x2F;setter方法。</li><li>getter&#x2F;setter方法内不增加业务逻辑，只<code>return this.data</code>。</li></ul><hr><h2 id="Lyrics-Shring"><a href="#Lyrics-Shring" class="headerlink" title="Lyrics Shring"></a>Lyrics Shring</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">我只想要拉住流年<br>好好的说声再见<br>遗憾感谢都回不去昨天<br>我只想铭记这瞬间<br>我们一起走过的光年<br>6月后 光年成纪念<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 面向对象 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> BUAA </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Linux指令集</title>
      <link href="/2026/03/03/Linux%E6%8C%87%E4%BB%A4/"/>
      <url>/2026/03/03/Linux%E6%8C%87%E4%BB%A4/</url>
      
        <content type="html"><![CDATA[<h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><p>Linux 是一种开源的操作系统，在 Linux 系统中，<code>一切皆文件</code>，因此命令行是与系统交互的主要方式。本文档将介绍一些常用的 Linux 指令，帮助你更好地理解和使用 Linux 系统。</p><h2 id="目录操作"><a href="#目录操作" class="headerlink" title="目录操作"></a>目录操作</h2><h3 id="cd"><a href="#cd" class="headerlink" title="cd"></a>cd</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> &lt;目录路径&gt;<br></code></pre></td></tr></table></figure><p>常见指令如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /    // 切换到根目录<br><span class="hljs-built_in">cd</span> ..   // 切换到上一级目录<br><span class="hljs-built_in">cd</span> ~    // 切换到当前用户的主目录<br><span class="hljs-built_in">cd</span> -    // 切换到上一次所在的目录<br></code></pre></td></tr></table></figure><p>同时，<code>cd</code>命令也支持相对路径和绝对路径。例如：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /home/user/Documents   // 绝对路径<br><span class="hljs-built_in">cd</span> ../Downloads           // 相对路径<br></code></pre></td></tr></table></figure><h3 id="ls"><a href="#ls" class="headerlink" title="ls"></a>ls</h3><p><code>ls</code> 用来查看当前目录下的<strong>文件</strong>和<strong>文件夹</strong>。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> &lt;目录路径&gt;<br></code></pre></td></tr></table></figure><p>常见指令如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span>         // 列出当前目录下的文件和文件夹<br><span class="hljs-built_in">ls</span> -1      // 每行一个列出当前目录下的文件和文件夹<br><span class="hljs-built_in">ls</span> -a      // 列出当前目录下的所有文件和文件夹，包括隐藏文件<br></code></pre></td></tr></table></figure><h3 id="mkdir"><a href="#mkdir" class="headerlink" title="mkdir"></a>mkdir</h3><p><code>mkdir</code> 用来创建新的目录。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mkdir</span> &lt;目录名称&gt;<br></code></pre></td></tr></table></figure><h3 id="rmdir"><a href="#rmdir" class="headerlink" title="rmdir"></a>rmdir</h3><p><code>rmdir</code> 用来删除<strong>空</strong>目录。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rmdir</span> &lt;目录名称&gt;<br></code></pre></td></tr></table></figure><h3 id="pwd"><a href="#pwd" class="headerlink" title="pwd"></a>pwd</h3><p><code>pwd</code> 用来显示当前所在的目录<strong>绝对路径</strong>。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">pwd</span><br></code></pre></td></tr></table></figure><p><strong>PS</strong>：所有的目录指令都要注意当前所处的目录位置。</p><h2 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h2><h3 id="touch"><a href="#touch" class="headerlink" title="touch"></a>touch</h3><p><code>touch</code> 用来创建新的文件。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">touch</span> &lt;文件名称&gt;<br></code></pre></td></tr></table></figure><h3 id="rm"><a href="#rm" class="headerlink" title="rm"></a>rm</h3><p><code>rm</code> 用来删除文件。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> &lt;文件名称&gt;<br></code></pre></td></tr></table></figure><p>常见指令如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> -i &lt;文件名称&gt;   // 删除文件前进行确认<br><span class="hljs-built_in">rm</span> -f &lt;文件名称&gt;   // 强制删除文件，不进行确认<br><span class="hljs-built_in">rm</span> -r &lt;目录名称&gt;   // 递归删除目录及其内容<br></code></pre></td></tr></table></figure><h3 id="cp"><a href="#cp" class="headerlink" title="cp"></a>cp</h3><p><code>cp</code> 用来复制文件或目录。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cp</span> &lt;源文件或目录&gt; &lt;目标目录&gt;<br></code></pre></td></tr></table></figure><p>常见指令如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cp</span> &lt;源文件&gt; &lt;目标目录&gt;   // 复制文件到目标目录<br><span class="hljs-built_in">cp</span> -r &lt;源目录&gt; &lt;目标目录&gt; // 递归复制目录及其内容到目标目录<br></code></pre></td></tr></table></figure><h3 id="mv"><a href="#mv" class="headerlink" title="mv"></a>mv</h3><p><code>mv</code> 用来移动或重命名文件或目录。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mv</span> &lt;源文件或目录&gt; &lt;目标目录或新名称&gt;<br></code></pre></td></tr></table></figure><p>常见指令如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mv</span> &lt;源文件&gt; &lt;目标目录&gt;   // 移动文件到目标目录<br><span class="hljs-built_in">mv</span> &lt;源目录&gt; &lt;目标目录&gt;   // 移动目录到目标目录<br><span class="hljs-built_in">mv</span> &lt;源文件或目录&gt; &lt;新名称&gt; // 重命名文件或目录<br></code></pre></td></tr></table></figure><h3 id="diff"><a href="#diff" class="headerlink" title="diff"></a>diff</h3><p><code>diff</code> 用来比较两个文件或目录的内容差异。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">diff &lt;文件或目录1&gt; &lt;文件或目录2&gt;<br></code></pre></td></tr></table></figure><p>相同输出 <code>0</code> ，不同输出 <code>1</code>。</p><h3 id="vim"><a href="#vim" class="headerlink" title="vim"></a>vim</h3><p><code>vim</code> 是一个强大的文本编辑器，用于编辑文件。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">vim &lt;文件名称&gt;  // 打开文件进行编辑<br>I              // 进入插入模式，可以编辑文件内容<br>edit           // 编辑文件内容<br>Esc            // 退出插入模式，回到命令模式<br>Shift + ;      // 进入命令模式，可以执行保存、退出等命令<br>wq             // 保存并退出<br>./&lt;文件名称&gt;    // 文件运行<br></code></pre></td></tr></table></figure><h3 id="cat"><a href="#cat" class="headerlink" title="cat"></a>cat</h3><p><code>cat</code> 用来查看文件内容。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> &lt;文件名称&gt;<br></code></pre></td></tr></table></figure><h2 id="文件查找"><a href="#文件查找" class="headerlink" title="文件查找"></a>文件查找</h2><h3 id="find"><a href="#find" class="headerlink" title="find"></a>find</h3><p><code>find</code> 用于查找某个名字的文件。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">find &lt;路径&gt; -name <span class="hljs-string">&quot;&lt;文件名&gt;&quot;</span><br></code></pre></td></tr></table></figure><h3 id="tree"><a href="#tree" class="headerlink" title="tree"></a>tree</h3><p><code>tree</code> 用来以树状结构显示目录结构。</p><p>常见指令如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">tree       //显示当前目录的树状结构<br>tree &lt;目录&gt;  //显示指定目录的树状结构<br>tree -d      //只显示目录，不显示文件<br>tree -a    //显示所有文件和目录，包括隐藏文件<br>tree -L &lt;层级&gt;  //限制显示的目录层级<br></code></pre></td></tr></table></figure><h2 id="文件编辑"><a href="#文件编辑" class="headerlink" title="文件编辑"></a>文件编辑</h2><p>之前了解到<code>vim</code>可以用来编辑文件，接下来介绍文本编辑常用的三驾马车：<code>grep</code>、<code>sed</code>和<code>awk</code>。</p><h3 id="grep"><a href="#grep" class="headerlink" title="grep"></a>grep</h3><p><code>grep</code> 用于在文本中搜索匹配的字符串。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">grep <span class="hljs-string">&quot;要查找的字符串&quot;</span> &lt;文件名&gt;<br></code></pre></td></tr></table></figure><p>常用指令如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">grep -i <span class="hljs-string">&quot;要查找的字符串&quot;</span> &lt;文件名&gt;   // 忽略大小写进行搜索，输出形式为 匹配行的内容<br>grep -r <span class="hljs-string">&quot;要查找的字符串&quot;</span> &lt;目录&gt;     // 目录递归搜索，输出形式为 文件路径:匹配行的内容<br>grep -n <span class="hljs-string">&quot;要查找的字符串&quot;</span> &lt;文件名&gt;   // 输出形式为 行号:匹配行的内容<br></code></pre></td></tr></table></figure><h3 id="sed"><a href="#sed" class="headerlink" title="sed"></a>sed</h3><p>sed 是一个流编辑器，用于对文本进行替换、删除、插入等操作。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sed [选项] <span class="hljs-string">&#x27;命令&#x27;</span> &lt;文件名&gt;<br></code></pre></td></tr></table></figure><p>选项主要有：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">-i    // 直接修改文件内容，否则不改变文件，只在终端输出<br>-n    // 只输出被修改的行，否则输出整个文件<br></code></pre></td></tr></table></figure><p>常用命令如下，注意一般情况下使用单引号 <code>&#39;</code> 来包裹命令。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sed <span class="hljs-string">&#x27;2a 要插入的文本&#x27;</span> &lt;文件名&gt;   // 在第2行后插入文本(Append)<br>sed <span class="hljs-string">&#x27;2i 要插入的文本&#x27;</span> &lt;文件名&gt;   // 在第2行前插入文本(Insert)<br>sed <span class="hljs-string">&#x27;2c 要替换的文本&#x27;</span> &lt;文件名&gt;   // 替换第2行的文本(Change)<br>sed <span class="hljs-string">&#x27;2d&#x27;</span> &lt;文件名&gt;              // 删除第2行(Delete)<br>sed -n <span class="hljs-string">&#x27;2p&#x27;</span> &lt;文件名&gt;           // 只输出第2行(Print)<br>sed -n <span class="hljs-string">&#x27;2,4p&#x27;</span> &lt;文件名&gt;         // 输出第2行到第4行的内容<br>sed <span class="hljs-string">&#x27;s/要替换的字符串/替换后的字符串/g&#x27;</span> &lt;文件名&gt;  // 替换文件中所有匹配的字符串(Substitute)<br></code></pre></td></tr></table></figure><h3 id="awk"><a href="#awk" class="headerlink" title="awk"></a>awk</h3><p><code>awk</code> 用于对文本进行模式匹配和处理，常用于文本分析和数据处理。常见的使用模板如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">awk <span class="hljs-string">&#x27;匹配模式&#123;执行动作&#125;&#x27;</span> &lt;文件名&gt;<br></code></pre></td></tr></table></figure><p><strong>匹配模式</strong>决定处理哪些行。可以是正则表达式、行号限制等。如果不写，默认处理每一行;<strong>执行动作</strong>决定找到指定的行之后干什么，如果不写，默认打印出找到的行。</p><p><code>awk</code> 最强大的地方在于，每读入一行文本，它就会自动按照<code>空格</code>或<code>Tab</code>键把这行文本<strong>切</strong>成若干块。同时还为我们提供了极其方便的内置变量，见下表：</p><table><thead><tr><th align="left">变量名</th><th align="left">详细解释与用途</th></tr></thead><tbody><tr><td align="left"><code>$0</code></td><td align="left">代表<strong>当前这一整行</strong>的内容。</td></tr><tr><td align="left"><code>$1</code>, <code>$2</code>…</td><td align="left">代表被切割后的<strong>第 1 列、第 2 列</strong>…以此类推。</td></tr><tr><td align="left"><code>NF</code></td><td align="left">Number of Fields：<strong>当前行一共有多少列</strong>。<code>$NF</code> 通常用来表示最后一列。</td></tr><tr><td align="left"><code>NR</code></td><td align="left">Number of Records：<strong>当前的行号</strong>。处理到第 5 行，<code>NR</code> 就是 5。</td></tr><tr><td align="left"><code>FS</code></td><td align="left">Field Separator：<strong>字段分隔符</strong>。默认是空格&#x2F;Tab，可以用 <code>-F</code> 选项修改。</td></tr></tbody></table><p>光看这些可能有点抽象，我们来看一个例子：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">grep -n <span class="hljs-string">&quot;hello&quot;</span> &lt;文件名&gt; | awk -F <span class="hljs-string">&quot;:&quot;</span> <span class="hljs-string">&#x27;&#123;print &quot;第&quot;$1&quot;行的内容是: &quot;$2&#125;&#x27;</span><br></code></pre></td></tr></table></figure><h3 id="tail"><a href="#tail" class="headerlink" title="tail"></a>tail</h3><p><code>tail</code> 用于查看文件的最后几行内容。通常用于输出文件最后几行内容到终端。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">tail</span> -n &lt;行数&gt; &lt;文件名&gt;     // 输出文件最后n行内容<br><span class="hljs-built_in">tail</span> -n +&lt;行数&gt; &lt;文件名&gt;     // 从第n行开始输出文件内容<br></code></pre></td></tr></table></figure><h2 id="文件阅读"><a href="#文件阅读" class="headerlink" title="文件阅读"></a>文件阅读</h2><h3 id="ctags"><a href="#ctags" class="headerlink" title="ctags"></a>ctags</h3><p><code>ctags</code> 用于生成代码标签，帮助开发者快速定位代码中的函数、变量等定义。</p><p>为了跨目录使用，首先打开<code>~/.vimrc</code>文件，添加以下内容：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">set tags = tags<br>set autochdir<br></code></pre></td></tr></table></figure><p>假设hello.c文件中有<code>void test_ctags()</code>函数，我们在 <code>second</code> 目录下新建<code>ctags_test.c</code>文件，输入以下内容：</p><figure class="highlight c"><table><tr><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdio.h&gt;</span></span><br><span class="hljs-type">int</span> <span class="hljs-title function_">main</span><span class="hljs-params">()</span> &#123;<br>    test_ctags();<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>在<strong>当前</strong>目录下使用以下命令生成<code>tags</code>文件：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ctags -R * // 递归生成当前目录及子目录下所有文件的标签<br></code></pre></td></tr></table></figure><p>得到的目录架构为：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">.<br>├── hello.c<br>├── second<br>│   └── test_sctags.c<br>└── tags<br></code></pre></td></tr></table></figure><p>接下来在second目录下进入 <code>ctags_test.c</code> 文件编辑，在test_ctags()函数名上按下 <code>Ctrl + ]</code> 键，vim会自动跳转到 <code>test_ctags()</code> 函数的定义处。再按下 <code>Ctrl + t</code> 键(可能也是 <code>Ctrl + O</code>)，可以返回到之前的位置。</p><h2 id="C语言编译"><a href="#C语言编译" class="headerlink" title="C语言编译"></a>C语言编译</h2><h3 id="gcc"><a href="#gcc" class="headerlink" title="gcc"></a>gcc</h3><p><code>gcc</code> 是GNU编译器套件，用于编译C语言程序。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">gcc -E &lt;源文件.c&gt; -o &lt;预处理文件.i&gt;  // 预处理阶段<br>gcc -S &lt;源文件.c&gt; -o &lt;汇编文件.s&gt;  // 编译阶段<br>gcc -c &lt;源文件.c&gt; -o &lt;目标文件.o&gt;  // 汇编阶段<br>gcc &lt;目标文件.o&gt; -o &lt;可执行文件&gt;  // 链接阶段<br><br>gcc &lt;源文件.c&gt; -o &lt;输出文件&gt;       // 直接编译并链接生成可执行文件<br>./&lt;输出文件&gt;  // 运行编译后的程序<br></code></pre></td></tr></table></figure><p>在编译之前，我们可以通过软链接的方式来简化编译指令，例如：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ln</span> -s <span class="hljs-string">&quot;./codeSet/*.c&quot;</span> ./testfile.c  // 将codeSet目录下的所有.c文件链接到当前目录下的testfile.c<br>gcc testfile.c -o testfile  // 编译testfile.c生成可执行文件<br></code></pre></td></tr></table></figure><h2 id="快捷键操作"><a href="#快捷键操作" class="headerlink" title="快捷键操作"></a>快捷键操作</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">Ctrl + C   // 终止当前正在运行的命令<br>Ctrl + Z   // 暂停当前正在运行的命令<br>Ctrl + D   // 结束输入，通常用于退出终端或结束文件输入<br>Ctrl + A   // 将光标移动到行首<br>Ctrl + E   // 将光标移动到行尾<br>Ctrl + U   // 删除光标前的内容<br>Ctrl + K   // 删除光标后的内容<br>Ctrl + L   // 清屏<br>Up Arrow   // 显示之前输入的命令<br>Down Arrow // 显示之后输入的命令<br>Tab        // 自动补全命令或文件名<br></code></pre></td></tr></table></figure><h2 id="make-makefile"><a href="#make-makefile" class="headerlink" title="make &amp; makefile"></a>make &amp; makefile</h2><p><code>make</code> 是一个自动化构建工具，用于管理和自动化编译过程。<code>makefile</code> 是一个文本文件，定义了构建规则和依赖关系。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs Makefile"><span class="hljs-section">target: dependencies</span><br>    command<br></code></pre></td></tr></table></figure><p>target 分为两类：<code>文件目标</code>和<code>伪目标</code>。文件目标是指生成的文件，伪目标是指不生成文件的目标，通常用于执行特定的命令。例如：<code>clean</code>。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs Makefile"><span class="hljs-meta"><span class="hljs-keyword">.PHONY</span>: clean  // 声明clean为伪目标</span><br><br><span class="hljs-section">my_app: main.o utils.o</span><br>    gcc main.o utils.o -o my_app<br><br><span class="hljs-section">main.o: main.c</span><br>    gcc -c main.c<br><br><span class="hljs-section">utils.o: utils.c</span><br>    gcc -c utils.c<br><br><span class="hljs-section">clean:</span><br>    rm -f *.o my_app<br></code></pre></td></tr></table></figure><p>这里我们来看一个有意思的例子，注意这个例子设计很多知识盲区，建议深入理解一下。如果我们要操作的目录如下：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">.<br>├── code<br>│   ├── Makefile<br>│   ├── fibo.c<br>│   └── main.c<br>├── include<br>│   └── fibo.h<br>└── Makefile<br></code></pre></td></tr></table></figure><p>现在，在根目录和<code>code</code>目录下的 <code>Makefile</code> 中输入内容，从而实现编译 <code>code</code> 目录下的 <code>main.c</code> 和 <code>fibo.c</code> 文件，并且在根目录链接生成可执行文件 <code>fibo</code> ，在 <code>code</code> 目录下生成 <code>*.o</code> 文件。最后在根目录的 <code>Makefile</code> 中添加 <code>clean</code> 功能,删除 <code>code</code> 目录下的 <code>*.o</code> 文件。很多第一次上手的同学都会在 <code>Makefile</code> 中写入 <code>cd</code> <code>mv</code> 等指令来实现目录切换和文件移动，其实大可不必，只要指明操作的路径即可。且看答案如何操作：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs Makefile">./Makefile         //注意这就是个注释<br> <br><span class="hljs-meta"><span class="hljs-keyword">.PHONY</span>: clean      //clean声明为&lt;伪目标&gt;,即执行的动作，注意 &quot;:&quot; 别漏</span><br><br><span class="hljs-section">fibo: code/main.o code/fibo.o</span><br>    gcc code/main.o code/fibo.o -o fibo<br><br>code/main.o code/fibo.o:<br>    make -C code        // 直接调用code目录下的Makefile进行编译<br><br><span class="hljs-section">clean:</span><br>    rm -rf code/*.o (或者 make -C code clean 到 code 目录下的 Makefile 中编辑) <br></code></pre></td></tr></table></figure><figure class="highlight makefile"><table><tr><td class="code"><pre><code class="hljs Makefile">./code/Makefile    //注意这也是个注释<br><br><span class="hljs-section">all: main.o fibo.o</span><br><br><span class="hljs-section">main.o: main.c</span><br>    gcc -I../<span class="hljs-keyword">include</span> -c main.c -o main.o  // 注意这里的-I参数指定了头文件的路径<br><br><span class="hljs-section">fibo.o: fibo.c</span><br>    gcc -I../<span class="hljs-keyword">include</span> -c fibo.c -o fibo.o<br></code></pre></td></tr></table></figure><p>最后，我们说明一下 <code>cd</code> 为何无效，因为每个命令都在一个新的 <code>shell</code> 中执行, 所以无论你怎么 <code>cd</code> ,在下一条命令执行前都会返回到当前目录，等于没用。</p><h2 id="Bash-脚本"><a href="#Bash-脚本" class="headerlink" title="Bash 脚本"></a>Bash 脚本</h2><p>bash 脚本是一种用来自动化执行一系列命令的脚本文件。它以<code>.sh</code>为扩展名，包含了Linux命令和控制结构.</p><p>一个简单的 bash 脚本包括三部分:</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash  // 指定脚本的解释器</span><br><span class="hljs-comment"># 这是一个注释</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Hello, World!&quot;</span>  // 输出文本<br></code></pre></td></tr></table></figure><p>如果在执行 bash 脚本时遇到权限问题，可以使用以下命令赋予执行权限：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> +x &lt;脚本文件.sh&gt;<br></code></pre></td></tr></table></figure><p>如果不想解决权限问题，也可以直接使用以下命令运行脚本：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">bash &lt;脚本文件.sh&gt;<br></code></pre></td></tr></table></figure><p>然后可以通过以下命令运行脚本：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">./&lt;脚本文件.sh&gt;<br></code></pre></td></tr></table></figure><h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><p>在 bash 脚本中，可以使用变量来存储和操作数据。变量不需要声明，直接赋值即可。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br>name=<span class="hljs-string">&quot;Alice&quot;</span>  // 定义变量,注意等号两边不能有空格<br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Hello, <span class="hljs-variable">$name</span>!&quot;</span>  // 使用变量，注意定义变量的时候不需要$符号，使用变量的时候需要加上$符号<br></code></pre></td></tr></table></figure><h3 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h3><p>bash 脚本可以接受命令行参数，这些参数在脚本中通过特殊变量访问。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;第一个参数: <span class="hljs-variable">$1</span>&quot;</span>  // 第一个参数<br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;第二个参数: <span class="hljs-variable">$2</span>&quot;</span>  // 第二个参数<br></code></pre></td></tr></table></figure><p>执行<code>./&lt;脚本文件.sh&gt; arg1 arg2</code>会输出：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">第一个参数: arg1<br>第二个参数: arg2<br></code></pre></td></tr></table></figure><p>两个特殊的参数：</p><p><code>$#</code>：表示传递给脚本的参数个数。<br><code>$@</code>：表示传递给脚本的所有参数，作为一个字符串。</p><h3 id="条件语句"><a href="#条件语句" class="headerlink" title="条件语句"></a>条件语句</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-keyword">if</span> condition<br><span class="hljs-keyword">then</span><br>    <span class="hljs-comment"># 条件为真时执行的命令</span><br><span class="hljs-keyword">elif</span> another_condition<br><span class="hljs-keyword">then</span><br>    <span class="hljs-comment"># 另一个条件为真时执行的命令</span><br><span class="hljs-keyword">else</span><br>    <span class="hljs-comment"># 条件都不满足时执行的命令</span><br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure><p>这里的 <code>condition</code> 的形式为 <code>[ expression ]</code>，其中 <code>expression</code> 可以是关系表达式、字符串比较等。并且 <code>[</code> 和 <code>e</code> 以及 <code>]</code> 和 <code>n</code> 之间必须有空格。</p><p>如果把<strong>单</strong>中括号换成<strong>双</strong>中括号 <code>[[ expression ]]</code>，则可以使用更多的条件表达式，例如：<code>[[ $a -gt 10 &amp;&amp; $b -lt 20 ]]</code>。</p><h3 id="循环语句"><a href="#循环语句" class="headerlink" title="循环语句"></a>循环语句</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-keyword">while</span> condition<br><span class="hljs-keyword">do</span> <br>    <span class="hljs-comment"># 条件为真时执行的命令</span><br><span class="hljs-keyword">done</span><br></code></pre></td></tr></table></figure><p>这里特别注意一下 <code>condition</code> 的形式为 <code>[ expression ]</code>，其中 <code>expression</code> 可以是关系表达式、字符串比较等。并且 <code>[</code> 和 <code>e</code> 以及 <code>]</code> 和 <code>n</code> 之间必须有空格。</p><h3 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-keyword">function</span> <span class="hljs-function"><span class="hljs-title">function_name</span></span>() &#123;<br>    <span class="hljs-comment"># 函数体</span><br>    <span class="hljs-built_in">return</span> int_value  // 可选，返回一个整数值<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>Attention</strong>:函数调用时需要用<code>$?</code>来获取函数的返回值。</p><h3 id="Bash-脚本语法"><a href="#Bash-脚本语法" class="headerlink" title="Bash 脚本语法"></a>Bash 脚本语法</h3><h4 id="关系运算符"><a href="#关系运算符" class="headerlink" title="关系运算符"></a>关系运算符</h4><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">-eq ==  // 等于<br>-ne !=  // 不等于<br>-gt &gt;   // 大于<br>-ge &gt;=  // 大于等于<br>-lt &lt;   // 小于<br>-le &lt;=  // 小于等于<br></code></pre></td></tr></table></figure><h4 id="关于变量"><a href="#关于变量" class="headerlink" title="关于变量"></a>关于变量</h4><ol><li>变量只有在 <code>&quot;</code>中才能展开，<code>&#39;</code> 会阻止展开。例: <code>sed -i &quot;s/$2/$3/g&quot; &quot;$1&quot;</code> 正确，<code>sed -i &#39;s/$2/$3/g&#39; &quot;$1&quot;</code> 错误。</li><li>文件名最好用双引号括起来，以防止文件名中包含空格或特殊字符导致的问题。例同上。</li><li>算数运算常用以下指令:</li></ol><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">let</span> a=a+1<br>(( a++ ))<br>(( a = a + <span class="hljs-number">1</span> ))<br>b=$(( a + <span class="hljs-number">1</span> ))<br></code></pre></td></tr></table></figure><p>我们不难看出双小括号常常用于算数表达式中，且在双小括号中变量不需要加 <code>$</code> 符号。</p><h4 id="关于字符串"><a href="#关于字符串" class="headerlink" title="关于字符串"></a>关于字符串</h4><p>这里首先介绍一下关于字符串的 <code>测试操作符</code> :</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">-z <span class="hljs-string">&quot;<span class="hljs-variable">$str</span>&quot;</span>  // 判断字符串是否为空，返回<span class="hljs-literal">true</span>如果字符串长度为0<br>-n <span class="hljs-string">&quot;<span class="hljs-variable">$str</span>&quot;</span>  // 判断字符串是否非空，返回<span class="hljs-literal">true</span>如果字符串长度不为0<br><span class="hljs-string">&quot;<span class="hljs-variable">$str1</span>&quot;</span> == <span class="hljs-string">&quot;<span class="hljs-variable">$str2</span>&quot;</span>  // 判断两个字符串是否相等，返回<span class="hljs-literal">true</span>如果相等<br><span class="hljs-string">&quot;<span class="hljs-variable">$str1</span>&quot;</span> != <span class="hljs-string">&quot;<span class="hljs-variable">$str2</span>&quot;</span>  // 判断两个字符串是否不相等，返回<span class="hljs-literal">true</span>如果不相等<br><span class="hljs-string">&quot;<span class="hljs-variable">$str1</span>&quot;</span> &gt; <span class="hljs-string">&quot;<span class="hljs-variable">$str2</span>&quot;</span>  // 判断字符串1是否大于字符串2，返回<span class="hljs-literal">true</span>如果字符串1大于字符串2,按照字典顺序比较<br><span class="hljs-string">&quot;<span class="hljs-variable">$str1</span>&quot;</span> &lt; <span class="hljs-string">&quot;<span class="hljs-variable">$str2</span>&quot;</span>  // 判断字符串1是否小于字符串2，返回<span class="hljs-literal">true</span>如果字符串1小于字符串2,按照字典顺序比较<br></code></pre></td></tr></table></figure><p>在 bash 中，如果比较的字符串中包含空格或特殊字符，<code>强烈建议</code>使用双引号括起来，以防止解析错误。</p><p>然后我们了解一下字符串的拼接和切割：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-variable">$&#123;str:begin&#125;</span>  // 获取从begin位置开始的字符串<br><span class="hljs-variable">$&#123;str:begin:length&#125;</span>  // 获取从begin位置开始的长度为length的字符串<br><span class="hljs-variable">$&#123;str:(-2)&#125;</span>   // 获取字符串的最后两个字符<br><span class="hljs-variable">$&#123;str:4:(-2)&#125;</span>  // 获取字符串从第4个字符开始到倒数第二个字符的子串<br></code></pre></td></tr></table></figure><p>我们在 <code>bash</code> 中最常使用字符串的情况之一就是对一个目录下的文件进行遍历操作，并对其内部实现相应的修改。下面我们来看一下一个典型的例子：</p><blockquote><p>读取 .&#x2F;code&#x2F; 文件夹下的所有文件，修改它们的内容（加一行代码、改一个函数名），然后把处理后的结果保存到 .&#x2F;codeSet&#x2F; 文件夹下，并将后缀统一改为 .c</p></blockquote><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><span class="hljs-built_in">mkdir</span> codeSet  // 创建codeSet文件夹<br><span class="hljs-keyword">for</span> file <span class="hljs-keyword">in</span> $(<span class="hljs-built_in">ls</span> ./code/)  // 遍历code文件夹下的所有文件<br><span class="hljs-keyword">do</span> <br>    sed -e <span class="hljs-string">&#x27;1i \#include&quot;include/fibo.h&quot;&#x27;</span> -e <span class="hljs-string">&#x27;s/getInt/getint/g&#x27;</span> ./code/<span class="hljs-variable">$file</span> &gt; ./codeSet/<span class="hljs-variable">$&#123;file:0:(-3)&#125;</span>.c  // 对每个文件进行处理，并保存到codeSet文件夹下，修改后的文件名为原文件名去掉后缀并加上.c<br><span class="hljs-keyword">done</span><br></code></pre></td></tr></table></figure><p>下面是字符串进阶，涉及if相关语法：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">mkdir</span> ./output/<br>&gt; ./testRes.txt<br><br>ak=1<br><span class="hljs-keyword">for</span> file <span class="hljs-keyword">in</span> $(<span class="hljs-built_in">ls</span> ./dataSet/)<br><span class="hljs-keyword">do</span><br>    <span class="hljs-keyword">if</span> [ <span class="hljs-variable">$&#123;file:(-3)&#125;</span> = <span class="hljs-string">&quot;.in&quot;</span> ]; <span class="hljs-keyword">then</span><br>        <span class="hljs-built_in">basename</span>=<span class="hljs-variable">$&#123;file:0:(-3)&#125;</span><br>        ./test.out &lt; <span class="hljs-string">&quot;./dataSet/<span class="hljs-variable">$basename</span>.in&quot;</span> &gt; <span class="hljs-string">&quot;./output/<span class="hljs-variable">$basename</span>.out&quot;</span><br>        <span class="hljs-keyword">if</span> diff <span class="hljs-string">&quot;./output/<span class="hljs-variable">$basename</span>.out&quot;</span> <span class="hljs-string">&quot;./dataSet/<span class="hljs-variable">$basename</span>.out&quot;</span>; <span class="hljs-keyword">then</span><br>            <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;<span class="hljs-variable">$basename</span> 1&quot;</span> &gt;&gt; ./testRes.txt<br>        <span class="hljs-keyword">else</span><br>            <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;<span class="hljs-variable">$basename</span> 0&quot;</span> &gt;&gt; ./testRes.txt<br>            <span class="hljs-built_in">let</span> ak=0<br>        <span class="hljs-keyword">fi</span><br>    <span class="hljs-keyword">fi</span><br><span class="hljs-keyword">done</span><br><br>sed -i <span class="hljs-string">&quot;1i<span class="hljs-variable">$ak</span>&quot;</span> ./testRes.txt<br></code></pre></td></tr></table></figure><h4 id="关于"><a href="#关于" class="headerlink" title="关于$"></a>关于$</h4><p>纸上得来终觉浅，决知此时要躬行。相信大家在实践之后最头疼的当属 <code>$</code> 符号了，下面我们总结一下关于 <code>$</code> 的使用：</p><ol><li>单独使用 <code>$</code> 或者 <code>$&#123;&#125;</code> 时，是为了获取变量的值。例: <code>echo $name</code> 或者 <code>echo $&#123;name&#125;</code>。<code>&#123;&#125;</code> 在这里是可选的，主要用于区分变量名和后续文本。</li><li><code>$(( ))</code> 用于算数运算，里面的变量不需要加 <code>$</code> 符号。例: <code>result=$((a + b))</code>。</li><li><code>$()</code> 用于命令替换，执行括号内的命令并将输出作为结果。例: <code>current_date=$(date)</code>,<code>$(ls)</code>.</li></ol><h4 id="免交互文档"><a href="#免交互文档" class="headerlink" title="免交互文档"></a>免交互文档</h4><p><code>免交互文档</code> 是指在执行命令时不需要用户进行交互输入的文档，多用于将一大段文本原封不动地传递给一段命令。常用的语法如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> &lt;&lt; <span class="hljs-string">&#x27;EOF&#x27;</span> &gt; &lt;文件名&gt;   // 将EOF之间的内容写入文件,并且不解析其中的变量<br>// 这里是要写入文件的内容<br>EOF<br></code></pre></td></tr></table></figure><p>这里的EOF仅仅是自定义分隔符，你当然可以把他换成 <code>APPLE</code>, <code>BANANA</code>, <code>BUAA</code> 等等，只要保证分隔符前后一致即可。<strong>我们注意到</strong>，上述命令中的 <code>EOF</code> 并没有被 <code>&#39;</code> 或 <code>&quot;</code> 包裹，这意味着在 <code>EOF</code> 之间的变量和命令会被shell进行解析，反之则原样输出。</p><p><strong>小贴士</strong>:</p><blockquote><p>脚本文件要善用重定向</p></blockquote><h2 id="重定向"><a href="#重定向" class="headerlink" title="重定向"></a>重定向</h2><p>重定向是指将命令的输入或输出重定向到文件或其他命令。Linux中有三种标准流：标准输入（stdin）、标准输出（stdout）和标准错误（stderr）。重定向可以用来将这些流重定向到文件或其他命令。</p><h3 id="输出重定向"><a href="#输出重定向" class="headerlink" title="输出重定向"></a>输出重定向</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">command</span> &gt; file.txt   // 将命令的输出重定向到文件，覆盖原有内容<br><span class="hljs-built_in">command</span> &gt;&gt; file.txt  // 将命令的输出重定向到文件，追加到原有内容<br><span class="hljs-built_in">command</span> 2&gt; error.txt  // 将命令的错误输出重定向到文件，覆盖原有内容<br><span class="hljs-built_in">command</span> 2&gt;&gt; error.txt // 将命令的错误输出重定向到文件，追加到原有内容<br></code></pre></td></tr></table></figure><p>特别的,如果要将一个文件清空，可以使用 <code>&gt; file.txt</code> 来实现。</p><h3 id="输入重定向"><a href="#输入重定向" class="headerlink" title="输入重定向"></a>输入重定向</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">command</span> &lt; input.txt   // 将文件的内容作为命令的输入<br></code></pre></td></tr></table></figure><p>举个例子：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> &lt; input.txt   // 将input.txt的内容输出到终端<br>grep <span class="hljs-string">&quot;要查找的字符串&quot;</span> &lt; input.txt  // 在input.txt中搜索匹配的字符串<br></code></pre></td></tr></table></figure><h2 id="管道"><a href="#管道" class="headerlink" title="管道"></a>管道</h2><p>管道（|）用于将一个命令的输出作为另一个命令的输入。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">command1 | command2   // 将command1的输出作为command2的输入<br></code></pre></td></tr></table></figure><p>几个明显的例子：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> file.txt | grep <span class="hljs-string">&quot;要查找的字符串&quot;</span>   // 在file.txt中搜索匹配的字符串<br><span class="hljs-built_in">cat</span> a.txt | <span class="hljs-built_in">cat</span> &gt;&gt; b.txt   // 将a.txt的内容追加到b.txt中<br></code></pre></td></tr></table></figure><h2 id="权限操作"><a href="#权限操作" class="headerlink" title="权限操作"></a>权限操作</h2><p><code>chmod</code> 是 <code>Linux</code> 系统中用来改变文件或目录访问权限的指令。权限分为三类：<strong>User</strong>、<strong>Group</strong>和<strong>Other</strong>，每类权限又分为<strong>读（r）</strong>、<strong>写（w）</strong>和<strong>执行（x）</strong>三种。</p><p>添加权限时使用 <code>+</code>，删除权限时使用 <code>-</code>，设置权限时使用 <code>=</code>。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> u+x &lt;文件名&gt;   // 给用户添加执行权限<br><span class="hljs-built_in">chmod</span> g-w &lt;文件名&gt;   // 给用户组删除写权限<br><span class="hljs-built_in">chmod</span> o=r &lt;文件名&gt;   // 给其他用户设置只读权限<br></code></pre></td></tr></table></figure><p>同时，<code>chmod</code> 还支持数字表示法，其中每个权限对应一个数字：读（r）&#x3D;4，写（w）&#x3D;2，执行（x）&#x3D;1。通过将这些数字相加，可以表示不同的权限组合。例如：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> 755 &lt;文件名&gt;   // 设置权限为 rwxr-xr-x<br><span class="hljs-built_in">chmod</span> 644 &lt;文件名&gt;   // 设置权限为 rw-r--r--<br><span class="hljs-built_in">chmod</span> 700 &lt;文件名&gt;   // 设置权限为 rwx------<br></code></pre></td></tr></table></figure><h2 id="Lyrics-Shring"><a href="#Lyrics-Shring" class="headerlink" title="Lyrics Shring"></a>Lyrics Shring</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">一叶轻船<br>一双月对半<br>一帘清梦悠悠醉意阑珊<br>移舟靠岸<br>案前唯剩空盏<br>莫怨良辰短<br>曲终了韵意未完<br></code></pre></td></tr></table></figure><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>本篇博客写于笔者操作系统课程学习期间，内容可能存在不完善之处，如果浪费了您宝贵的时间，笔者深表歉意。后续会继续更新完善，欢迎大家提出宝贵意见和建议。</p><p>Thanks for reading!</p>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Git指令集</title>
      <link href="/2026/01/26/Git/"/>
      <url>/2026/01/26/Git/</url>
      
        <content type="html"><![CDATA[<h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><p>Git 是一个分布式版本控制系统，广泛用于软件开发和其他需要版本控制的项目中。GitHub 和 GitLab 两个计算机领域绕不开的平台，都基于 Git 进行版本控制和协作开发。本文档将介绍一些常用的 Git 指令，帮助你更好地理解和使用 Git。我们以 GitHub 为例进行说明。</p><h2 id="Git配置"><a href="#Git配置" class="headerlink" title="Git配置"></a>Git配置</h2><p>在使用Git之前，我们需要进行一些基本的配置，主要包括设置用户名和邮箱地址。这些信息会被记录在每次提交中，用于标识提交的作者。</p><p>打开Git Bash，执行以下命令进行配置：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git config --global user.name <span class="hljs-string">&quot;你的用户名&quot;</span><br>git config --global user.email <span class="hljs-string">&quot;你的邮箱地址&quot;</span><br></code></pre></td></tr></table></figure><p>如果去掉<code>--global</code>参数，则表示只对当前仓库进行配置。</p><p>如果想查看当前的配置信息，可以执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git config --list<br></code></pre></td></tr></table></figure><h2 id="远程仓库关联"><a href="#远程仓库关联" class="headerlink" title="远程仓库关联"></a>远程仓库关联</h2><p>GitHub 其实就是一个代码托管平台，用户可以在上面创建远程仓库来存储和管理代码。要将本地仓库与远程仓库关联，有以下两种方式：</p><ol><li><p><strong>git clone</strong>：用于从远程仓库克隆代码到本地。<br>例如我们在E:\Projects目录下打开Git Bash，执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> &lt;远程仓库地址&gt;<br></code></pre></td></tr></table></figure></li></ol><p>得到的结果是Git会在E:\Projects目录下创建一个新的文件夹，文件夹名称与远程仓库名称相同，里面包含了远程仓库的所有代码和历史记录。结构图如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">当前目录<br>├─ repo/<br>│  ├─ .git/<br>│  ├─ README.md<br>│  └─ ...<br></code></pre></td></tr></table></figure><ol start="2"><li><p><strong>git init</strong>：在本地创建一个新的Git仓库，然后手动关联到远程仓库。<br>例如我们在E:\Projects目录下打开Git Bash，执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git init<br>git add .<br>git commit -m <span class="hljs-string">&quot;initial commit&quot;</span><br>git remote add origin &lt;远程仓库地址&gt;      //关联远程仓库<br>git branch -M main         //把当前分支强制重命名为main<br>git push -u origin main<br></code></pre></td></tr></table></figure></li></ol><p>Git会在E:\Projects目录下创建一个.git文件夹，表示这是一个Git仓库。结构图如下：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">├─ 当前目录<br>│  ├─ .git/<br>│  ├─ README.md<br>│  └─ ...<br></code></pre></td></tr></table></figure><p><strong>注意以上两种方法均需要在远程仓库平台（如 GitHub）上提前创建好远程仓库。</strong></p><h2 id="文件提交"><a href="#文件提交" class="headerlink" title="文件提交"></a>文件提交</h2><p>在掌握怎么提交文件之前，我们需要先了解Git的工作区、暂存区和本地仓库（版本库）三个概念：</p><h3 id="工作区"><a href="#工作区" class="headerlink" title="工作区"></a>工作区</h3><p>工作区是我们在电脑上看到的文件和文件夹的实际位置。我们在工作区进行文件的编辑、添加和删除等操作。</p><h3 id="暂存区"><a href="#暂存区" class="headerlink" title="暂存区"></a>暂存区</h3><p>暂存区是一个临时存储区域，用于保存我们准备提交到本地仓库的文件快照。当我们执行<code>git add</code>命令时，文件会被添加到暂存区。</p><h3 id="本地仓库"><a href="#本地仓库" class="headerlink" title="本地仓库"></a>本地仓库</h3><p>本地仓库是我们<strong>电脑上</strong>存储版本历史记录的地方。当我们执行<code>git commit</code>命令时，暂存区的文件会被提交到本地仓库，形成一个新的版本，这个版本会有唯一的标识符且不可更改。</p><p>了解了这三个概念后，我们就可以在<code>.git</code>所在的目录下执行相关指令来提交文件了。</p><h3 id="常用指令"><a href="#常用指令" class="headerlink" title="常用指令"></a>常用指令</h3><ol><li><strong>git add</strong> <code>&lt;文件名&gt;</code>：将指定文件添加到暂存区。如果要添加所有修改过的文件，可以使用<code>git add .</code>。</li><li><strong>git status</strong>：查看哪些文件在暂存区中。不在暂存区则表示未被 Git 跟踪。</li><li><strong>git diff</strong>：查看<strong>工作区</strong>和<strong>暂存区</strong>之间的差异。</li><li><strong>git commit -m</strong> <code>&quot;提交信息&quot;</code>：将暂存区的文件提交到本地仓库，并附上提交信息。</li><li><strong>git log</strong>：查看提交历史记录。</li><li><strong>git diff –cached</strong>:查看<strong>暂存区</strong>和<strong>本地仓库</strong>之间的差异。</li><li><strong>git push origin</strong> <code>&lt;分支名&gt;</code>：将本地仓库的更改推送到远程仓库的指定分支上。</li></ol><h2 id="文件拉取"><a href="#文件拉取" class="headerlink" title="文件拉取"></a>文件拉取</h2><p>文件拉取有两种操作方式：</p><ol><li><strong>git pull origin</strong> <code>&lt;分支名&gt;</code>：从远程仓库的指定分支拉取最新的更改并合并到当前分支。</li><li><strong>git fetch origin</strong> <code>&lt;分支名&gt;</code>：从远程仓库获取最新的更改，但不进行合并。需要手动执行<code>git merge</code>命令将更改合并到当前分支。</li></ol><p>第二种方式的安全版：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git checkout &lt;分支名&gt;        //切换到指定分支<br>git fetch origin &lt;分支名&gt;    //从远程仓库获取最新的更改<br>git diff origin/&lt;分支名&gt;   //查看远程分支和本地分支的差异<br>git merge origin/&lt;分支名&gt;  //将远程分支的更改合并到当前分支<br></code></pre></td></tr></table></figure><h2 id="分支管理"><a href="#分支管理" class="headerlink" title="分支管理"></a>分支管理</h2><p>分支是Git的一个重要特性，允许我们在同一个仓库中并行开发不同的功能或修复不同的bug。常用的分支管理指令如下：</p><h3 id="创建分支"><a href="#创建分支" class="headerlink" title="创建分支"></a>创建分支</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git checkout -b &lt;分支名&gt;   //创建并切换到新分支<br>git push -u origin &lt;分支名&gt;  //将新分支推送到远程仓库并关联<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git branch &lt;分支名&gt;        //只创建新分支<br>git push -u origin &lt;分支名&gt;  //将新分支推送到远程仓库并关联<br></code></pre></td></tr></table></figure><ul><li><p>这里分支名常用feature-xxx（新功能）、bugfix-xxx（修复bug）、hotfix-xxx（紧急修复）等命名规范。</p></li><li><p>创建的分支自动继承当前分支的代码和历史记录。</p></li></ul><h3 id="切换分支"><a href="#切换分支" class="headerlink" title="切换分支"></a>切换分支</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git checkout &lt;分支名&gt;      //切换到指定分支<br></code></pre></td></tr></table></figure><h3 id="查看分支"><a href="#查看分支" class="headerlink" title="查看分支"></a>查看分支</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git branch                 //查看本地分支<br>git branch -r              //查看远程分支<br>git branch -a              //查看所有分支<br></code></pre></td></tr></table></figure><h3 id="合并分支"><a href="#合并分支" class="headerlink" title="合并分支"></a>合并分支</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git checkout &lt;目标分支&gt;     //切换到想要的当前分支<br>git merge &lt;源分支&gt;         //将源分支合并到当前分支(源分支是本地分支)<br>或者<br>git fetch origin &lt;源分支&gt;    //从远程仓库获取最新的更改<br>git merge origin/&lt;源分支&gt;  //将远程分支合并到当前分支<br></code></pre></td></tr></table></figure><p>当没有冲突时，Git会自动完成合并操作，然后执行add和commit操作。如果有冲突，Git会提示你手动解决冲突，解决后你需要手动执行add和commit操作。</p><p><strong>这里特别说明一下</strong>：<code>origin/&lt;源分支&gt;</code>代表的是本地记录的<code>远程分支快照</code>,不是远程仓库本身，而是你上次fetch&#x2F;pull时，Git在本地保存的一个远程分支指针。因此为了确保合并的是最新的远程状态，建议先执行<code>git fetch origin &lt;源分支&gt;</code>命令。</p><p>不难看出，<code>merge</code>操作实际上是将两个分支的代码进行整合，生成一个新的提交记录。并且这一行为发生在<code>本地仓库</code>中。</p><h3 id="删除分支"><a href="#删除分支" class="headerlink" title="删除分支"></a>删除分支</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git branch -d &lt;分支名&gt;      //删除本地分支<br>git push origin --delete &lt;分支名&gt; //删除远程分支<br></code></pre></td></tr></table></figure><h2 id="查看提交历史"><a href="#查看提交历史" class="headerlink" title="查看提交历史"></a>查看提交历史</h2><h3 id="git-log"><a href="#git-log" class="headerlink" title="git log"></a>git log</h3><p><code>git log</code>命令用于查看提交历史记录，在文件提交模块有提到过，这里补充一个常用的参数：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">log</span> --oneline        //以简洁的单行格式显示提交历史<br></code></pre></td></tr></table></figure><p><code>git log</code>命令还有很多其他参数，可以根据需要现学现用，这里就不过度展开了。</p><h3 id="git-blame"><a href="#git-blame" class="headerlink" title="git blame"></a>git blame</h3><p><code>git blame &lt;文件名&gt;</code>命令用于查看文件的每一行最后一次修改的提交记录，包括提交者、提交时间和提交信息等。这个命令对于追踪代码的变更历史非常有用。</p><p>同样<code>git blame</code>命令也有很多参数，可以使用<code>git blame --help</code>命令查看详细的帮助信息。</p><h2 id="恢复和回退"><a href="#恢复和回退" class="headerlink" title="恢复和回退"></a>恢复和回退</h2><p>在使用Git的过程中，有时我们可能需要恢复文件到某个特定的状态。例如你写一个.py文件，写着写着红温了，文件改废了，但你又不知道改了哪里，这时候你就非常想回到上一次的提交重新来过。Git提供了多种方式来实现文件的恢复和回退，下面介绍几种常用的方法。</p><p><strong>重中之重</strong>：恢复和回退一定要在操作之前知道后果是什么，否则你是真的欲哭无泪啊！</p><h3 id="checkout"><a href="#checkout" class="headerlink" title="checkout"></a>checkout</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git checkout &lt;commit&gt; -- &lt;文件名&gt;    //将工作区的文件恢复到&lt;commit&gt;提交的状态,同时自动add到暂存区<br></code></pre></td></tr></table></figure><p>这个命令会将指定文件恢复到<commit>提交的状态，丢弃工作区中的所有修改。注意，这个操作是不可逆的，一旦执行，工作区中的修改将无法找回。<strong>注意</strong>, <code>git checkout &lt;文件名&gt;</code> 会优先将<code>工作区</code>恢复到<code>暂存区</code>的状态,如果<code>暂存区</code>没有该文件的修改，会将工作区恢复到上一次 <code>commit</code> 的状态。</p><p><strong>易踩的坑</strong>: 如果去掉文件名，执行<code>git checkout &lt;commit&gt;</code>会把你的所有工作区的文件恢复到指定提交时的状态，但是此时，你的<code>HEAD</code>指针会被移动到该提交上，处于<strong>分离头指针</strong>状态。如果你在这个状态下进行新的提交，这些提交将不会属于任何分支，容易导致丢失。此时你执行<code>git checkout &lt;分支名&gt;</code>可以回到正常状态，但是这也会导致工作区的文件回到该分支的最新状态,即<code>git checkout &lt;commit&gt;</code>之前的状态。所以，这一操作一般用来<strong>看</strong>历史，而不是<strong>改</strong>历史.</p><h3 id="reset"><a href="#reset" class="headerlink" title="reset"></a>reset</h3><h4 id="–soft"><a href="#–soft" class="headerlink" title="–soft"></a>–soft</h4><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git reset --soft &lt;commit&gt;    //将当前分支的HEAD指针移动到&lt;commit&gt;提交,保留暂存区和工作区的修改<br></code></pre></td></tr></table></figure><p>用大白话说就是工作区还在，<code>add</code>还在，<code>commit</code>没了。<br>常常用在你想修改提交说明的时候。</p><h4 id="–mixed-默认"><a href="#–mixed-默认" class="headerlink" title="–mixed (默认)"></a>–mixed (默认)</h4><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git reset --mixed &lt;commit&gt;    //将当前分支的HEAD指针移动到&lt;commit&gt;提交,保留工作区的修改,清空暂存区<br></code></pre></td></tr></table></figure><p>用大白话说就是<code>add</code>没了，<code>commit</code>没了,工作区还在。<br>常常用在你想重新选择要提交的文件的时候。</p><h4 id="–hard"><a href="#–hard" class="headerlink" title="–hard"></a>–hard</h4><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git reset --hard &lt;commit&gt;    //将当前分支的HEAD指针移动到&lt;commit&gt;提交,清空暂存区和工作区的修改<br></code></pre></td></tr></table></figure><p>用大白话说就是<code>add</code>没了，<code>commit</code>没了，工作区也没了。<br>常常用在你这次改动彻底废了，想回到某个提交重新开始的时候。</p><h3 id="revert"><a href="#revert" class="headerlink" title="revert"></a>revert</h3><p>git revert命令用于撤销某个提交的更改，但不会删除该提交记录。相反，它会创建一个新的提交，内容是对指定提交的反向更改。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git revert &lt;commit&gt;    //撤销&lt;commit&gt;提交的更改，在本地仓库生成一个新的提交<br></code></pre></td></tr></table></figure><p>常常用在你已经将更改推送到<strong>远程仓库</strong>，但又想撤销某个提交的情况下。因为它不会修改提交历史，所以不会影响其他协作者的工作。</p><p>你可以理解为在团队协作过程中不能轻易修改远程仓库的历史提交记录，所以只能新增一条提交来表示撤销之前的修改。</p><h3 id="restore"><a href="#restore" class="headerlink" title="restore"></a>restore</h3><p>在修改了一个文件并且还没有将其<code>add</code>到暂存区之前，如果你想放弃对该文件的修改，可以使用<code>git restore</code>命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git restore &lt;文件名&gt;    //放弃对&lt;文件名&gt;的修改，恢复到上一次提交的状态<br></code></pre></td></tr></table></figure><p>如果你已经将修改<code>add</code>到暂存区，但还没有提交，可以使用以下命令来撤销暂存区的修改：</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git restore --staged &lt;文件名&gt;    //撤销&lt;文件名&gt;在暂存区的修改，保留工作区的修改<br></code></pre></td></tr></table></figure><h3 id="reflog"><a href="#reflog" class="headerlink" title="reflog"></a>reflog</h3><p>reflog是Git的一个强大功能，用于记录HEAD指针的移动历史。可以说是我们在误操作后找回丢失提交的最后一道防线。</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">git reflog    //查看HEAD指针的移动历史<br>git reset --hard &lt;reflog条目&gt;    //将当前分支的HEAD指针移动到指定的reflog条目<br></code></pre></td></tr></table></figure><p>reflog条目通常以<code>HEAD@&#123;n&#125;</code>的形式表示，其中<code>n</code>是一个整数，表示从当前HEAD位置向后数的第n次移动。</p><p><strong>总而言之</strong>，git的回退操作由于版本差异很多操作的实现方式虽然不同，但是功能是大差不差的，大家在实践过程中关键是要找到自己顺手的操作方式。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文档介绍了Git的基本概念和常用指令，包括配置、远程仓库关联、文件提交、文件拉取、分支管理、查看提交历史以及恢复和回退等内容。掌握这些指令将帮助你更有效地使用Git进行版本控制和协作开发。<br>纸上得来终觉浅，绝知此事要躬行。希望大家在实际应用中学习Git，而不是看着Blog纸上谈兵。<br>Git功能强大，本文档只是冰山一角，建议在实际使用中不断学习和探索更多高级功能。祝你在使用Git的旅程中一切顺利！</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://git-scm.com/doc">Git官方文档</a></li><li><a href="https://www.runoob.com/git/git-tutorial.html">菜鸟教程 Git 教程</a></li><li><a href="https://git-scm.com/book/zh/v2">Pro Git 电子书</a></li><li><a href="https://learngitbranching.js.org/?locale=zh_CN">可视化理解 Git</a></li></ul><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">是不是我能给的没他多<br>是否再绚烂的星河<br>天亮都要失色<br>听说你为他做的<br>件件是我 曾经求而不得<br>我够不着的烟火<br>偏偏降落 在别人窗口<br>那晚的风<br>吹到今天都还未凉透<br>才松开手<br>你却已握紧别的温柔<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Git </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
            <tag> Git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」P6课上</title>
      <link href="/2026/01/23/P6%E8%AF%BE%E4%B8%8A/"/>
      <url>/2026/01/23/P6%E8%AF%BE%E4%B8%8A/</url>
      
        <content type="html"><![CDATA[<p><strong>写在前面</strong>：P6 其实是 P5 <strong>plus</strong>，依然是三类：<code>计算类</code>、<code>跳转类</code>、<code>访存类</code>。只是计算类一般会涉及对 MDU 的操作，访存类不能在 DM 进行计算（因为 DM 没有了…）。很抱歉这次只做了两道题，第三题看题时间被助教问答占掉了（不嘻嘻）。如果哪些方法写得不太详尽可以参考 P5 的<a href="https://cordial-kid.github.io/2026/01/21/P5%E8%AF%BE%E4%B8%8A/">这篇博客</a>，大同小异。</p><h2 id="MYGO"><a href="#MYGO" class="headerlink" title="MYGO"></a>MYGO</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">pattern_value(x) = 4位x的前导0个数 * 2 + 4位x中1的个数<br>chain(x) = 32位二进制数分成8组，每组的pattern_value(x) * (组数 + 1) 组数从低位开始(0,1,2,3,4,5,6,7).<br>A = chain(GPR(rs))<br>B = chain(GPR(rt))<br>if (A &gt; B) &#123;<br>    hi = GPR[rs]<br>    lo = GPR[rt]<br>&#125;<br>else if(A &lt; B) &#123;<br>    hi = GPR[rt];<br>    lo = GPR[rs];<br>&#125;<br>else &#123;<br>    hi = GPR[rs] ^ GPR[rt];<br>    lo = GPR[rs] ^ GPR[rt];<br>&#125;<br><br>MYGO当作乘除指令来计算，延迟5个周期。<br></code></pre></td></tr></table></figure><p>这个乘除槽类计算指令肯定主体要在 MDU 模块内实现，我的建议是 A、B 的计算使用组合逻辑，hi、lo 的赋值使用时序逻辑。组合逻辑计算别发怵，我敢打赌要是程序设计出这个逻辑，看到这里的各位没有一个怕的，所以大胆上手就行。时序逻辑只要按照之前的判断方式添加就好，别忘了 Start 信号下 cnt 的修改就行。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (MDUOp == `MDU_MYGO) <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">if</span> (A &gt; B) <span class="hljs-keyword">begin</span><br>        HIreg &lt;= GPR[rs];<br>        LOreg &lt;= GPR[rt];<br>    <span class="hljs-keyword">end</span><br>    ...<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>Stall模块跟mult一致即可。</p><h2 id="MBL"><a href="#MBL" class="headerlink" title="MBL"></a>MBL</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">tmpA = IMM_BEQ(IMM指令跳转的数值)<br>tmpB = GPR[rs] + GPR[rt]<br>GPR[31] = PC + 8;<br>if rs == rt &#123;<br>    PC = tmpA;<br>&#125;<br>else if (rs[0] == rt[0]) &#123;<br>    PC = &#123;tmpB[31:0],2&#x27;b0&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>这道题真的是坑点满满！！！</strong></p><ul><li>我们很容易先入为主地把<code>rs == rt</code>理解为<code>GPR[rs] == GPR[rt]</code>，恭喜你，你将收获莫名其妙的 PC。</li><li>在D级我们一定会用到GPR[rs] 和 GPR[rt] 吗？如果我们直接按照之前的<code>rs_Tuse = 0</code>,<code>rt_Tuse = 0</code>来写，那么恭喜你，喜提TLE。</li></ul><p>注意到以上两点这道题也就迎刃而解了。在CMP新增<code>rs</code>,<code>rt</code>输入端口完成比较,新增输出端口<code>Judge</code>辅助判断跳转类型，NPC当然要增加<code>Judge</code>这个输入来计算新地址。</p><p>对于 Stall，我们判断的条件是<code>(D_beq | D_jr | D_jalr | D_bne | (D_mbl &amp;&amp; Beq_jump == 1 &amp;&amp; Judge == 0)) ? 3&#39;d0 :</code>，即只在 <code>rs[0] == rt[0]</code> 的条件下才需要 GPR[rs] 和 GPR[rt]。这就解决了 TLE 问题。</p><h2 id="LBO"><a href="#LBO" class="headerlink" title="LBO"></a>LBO</h2><p>这里希望路过好心人可以告诉笔者一下题目，共同给学弟学妹一个完整的magicbook(bushi)，谢谢谢谢。</p><h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>P6 其实和 P5 差不多，放宽心，前两道题肯定难不倒你，加油哦。再向好心人求一下<code>LBO</code>指令。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">我也很想他 我们都一样<br>在他的身上 曾找到翅膀<br>只是那时的他 是因为你<br>他开始飞翔<br>我也很想他，在某个地方<br>我少了尴尬 你少了肩膀<br>而夏天还是那么短<br>思念却很长<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> verilog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P6课下</title>
      <link href="/2026/01/23/P6%E8%AF%BE%E4%B8%8B/"/>
      <url>/2026/01/23/P6%E8%AF%BE%E4%B8%8B/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>P6 和 P5 的整体思路一样，区别在于 P6 将 IM 和 DM 模块放在了外部（课程组写好了代码），从而使得 CPU 更加纯粹，贴合实际。同时 P6 新增了乘除槽指令，由于乘除槽指令执行周期较长，因此我们在阻塞等方面要加以注意。废话不多说，我们开始吧。</p><h2 id="IFU-PC"><a href="#IFU-PC" class="headerlink" title="IFU -&gt; PC"></a>IFU -&gt; PC</h2><p>在 P6 实验中，IM 外置，因此在 P5 的基础上，我们要删掉这部分，保留 PC 的同时在顶层模块 mips 新增接口与外置 DM 信息交换：<br><code>input [31:0] i_inst_rdata,      //F级PC对应的32位指令</code><br><code>output [31:0] i_inst_addr,      //F级PC</code></p><h2 id="D-CMP"><a href="#D-CMP" class="headerlink" title="D_CMP"></a>D_CMP</h2><p>P6 新增指令<code>BNE</code>，需要在 D_CMP 模块完成比较。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> Beq_jump = (CMPOp == `CMP_BEQ &amp;&amp; RS_Data == RT_Data) ? <span class="hljs-number">1&#x27;b1</span> :<br>                  (CMPOp == `CMP_BNE &amp;&amp; RS_Data != RT_Data) ? <span class="hljs-number">1&#x27;b1</span> :<br>                  <span class="hljs-number">1&#x27;b0</span>;<br></code></pre></td></tr></table></figure><h2 id="D-GRF"><a href="#D-GRF" class="headerlink" title="D_GRF"></a>D_GRF</h2><p>P6不需要<code>$display(&quot;%d@%h: $%d &lt;= %h&quot;,$time,PC,A3,WD);</code></p><h2 id="def"><a href="#def" class="headerlink" title="def"></a>def</h2><p>这个模块其实本来没什么好说的，只要新增指令需要用到的变量我们在这里定义即可。在这里值得说明的是，虽然 P6 指令极多，但是笔者权衡之后依然采用每条指令独立考量的方式，没有像主流思路一样对指令进行分类。两种方式各有利弊，这里只能仁者见仁，智者见智了，举个例子。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> GRFA3Sel = (cal_r | jalr | mf) ? `GRFA3rd :<br>                (cal_i | load | mfc0) ? `GRFA3rt : <br>                (jal) ? `GRFA331 : `GRFA30;<br></code></pre></td></tr></table></figure><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> GRFA3Sel = (Add|Sub|Sll|Jalr|_And|_Or|Slt|Sltu|Mfhi|Mflo) ? `GRFA3rd :<br>                  (Ori | Lw | Lui | Addi | Andi | Lh | Lb) ? `GRFA3rt :<br>                  (Jal) ? `GRFA331 :<br>                  `GRFA30;<br></code></pre></td></tr></table></figure><p>显然，第二段代码要比第一段代码冗杂的多，这是我采用的写法的缺点。但是你也很容易发现，第一种写法将分类和不分类杂糅了起来，导致在coding时极度混乱，容易出错；而第二段代码因为是每条指令独立考虑的，也就不存在这个问题了。个人不太喜欢这种易错感觉，当然大家如何架构还是看自己心意🫡。</p><h2 id="E-ALU"><a href="#E-ALU" class="headerlink" title="E_ALU"></a>E_ALU</h2><p>P6对ALU的改动就是增加了新的指令的计算方式<code>and,slt,sltu</code>。</p><h2 id="E-MDU"><a href="#E-MDU" class="headerlink" title="E_MDU"></a>E_MDU</h2><p>乘除槽的实现是P6的重点和难点。我们要实现8条乘除法相关指令：<code>mult</code> <code>multu</code> <code>div</code> <code>divu</code> <code>mthi</code> <code>mtlo</code> <code>mfhi</code> <code>mflo</code>，前4条指令是计算乘除法，并将结果存入HI，LO两个寄存器中，后4条指令是对HI和LO的读写。<br>我们把MDU模块当作特殊的ALU，对于<strong>前四条指令</strong>，MDU要执行很多个周期并且不需要输出计算结果，只要在时钟上升沿时将计算结果存入HI，LO寄存器即可。对于<code>mfhi,mflo</code>则需要正常输出。对于<code>mthi,mtlo</code>，在时钟上升沿时写入对应寄存器即可，只需要<strong>执行一个周期</strong>。<br>代码如下：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">always</span> @(<span class="hljs-keyword">posedge</span> clk) <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">if</span> (reset) <span class="hljs-keyword">begin</span><br>        HIreg &lt;= <span class="hljs-number">32&#x27;b0</span>;<br>        LOreg &lt;= <span class="hljs-number">32&#x27;b0</span>;<br>        cnt &lt;= <span class="hljs-number">4&#x27;b0</span>;<br>    <span class="hljs-keyword">end</span><br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">begin</span><br>        <span class="hljs-keyword">if</span> (MDUOp == `MDU_MULT) <span class="hljs-keyword">begin</span><br>            &#123;HIreg,LOreg&#125; &lt;= <span class="hljs-built_in">$signed</span>(A) * <span class="hljs-built_in">$signed</span>(B);<br>        <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (MDUOp == `MDU_MULTU) <span class="hljs-keyword">begin</span><br>            &#123;HIreg,LOreg&#125; &lt;= A * B;<br>        <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (MDUOp == `MDU_DIV) <span class="hljs-keyword">begin</span><br>            HIreg &lt;= <span class="hljs-built_in">$signed</span>(A) % <span class="hljs-built_in">$signed</span>(B);<br>            LOreg &lt;= <span class="hljs-built_in">$signed</span>(A) / <span class="hljs-built_in">$signed</span>(B); <br>        <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (MDUOp == `MDU_DIVU) <span class="hljs-keyword">begin</span><br>            HIreg &lt;= A % B;<br>            LOreg &lt;= A / B;<br>        <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (MDUOp == `MDU_MTHI) <span class="hljs-keyword">begin</span><br>            HIreg &lt;= A;<br>        <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (MDUOp == `MDU_MTLO) <span class="hljs-keyword">begin</span><br>            LOreg &lt;= A;<br>        <span class="hljs-keyword">end</span><br>    <span class="hljs-keyword">end</span><br><span class="hljs-keyword">end</span><br><span class="hljs-keyword">assign</span> MDUC = (MDUOp == `MDU_MFHI) ? HIreg :<br>              (MDUOp == `MDU_MFLO) ? LOreg :<br>              <span class="hljs-number">32&#x27;d0</span>;<br></code></pre></td></tr></table></figure><p>注：以下提到的乘除法计算均为<code>mult,multu,div,divu</code>。<br>关于阻塞，之前我们提到了乘除法的计算周期较长，为了不因为乘除法计算的存在而拉低 CPU 的最短时钟周期，我们引入了 MDU。因此当 MDU 模块正在计算时，所有不进入 MDU 的指令都可以正常流水，这也就导致了 MDU 的阻塞与自身息息相关。当 MDU 执行乘除法计算时，</p><ol><li>出现了<code>mfhi,mflo</code>,将指令阻塞在D级。</li><li>出现了<code>mult,multu,div,divu</code>，直接终止运算，因为接下来要覆盖</li><li>出现了<code>mthi,mtlo</code>,将指令阻塞在D级。</li></ol><p>当然课程组为了方便我们设计给了我们一些提示：</p><ol><li>当E级指令是<code>mult multu div divu</code>时，CTRL模块需要输出1个周期的1位<strong>start</strong>信号，表示乘除法计算开始；</li><li>当start信号结束后，如果之前的计算指令是<code>mult multu</code>指令，MDU模块需要连续输出<strong>5</strong>个周期的<strong>busy</strong>信号，表示正在计算HI寄存器和LO寄存器的值；如果之前的计算指令是<code>div divu</code>指令，则需要连续输出<strong>10</strong>个周期的<strong>busy</strong>信号；</li><li>当start信号或busy信号为1时，不进入MDU模块的指令正常流水，要进入MDU模块的所有指令均阻塞在D级；</li><li>为了降低实现难度，保证MDU模块在进行乘除法计算时只会出现<code>mfhi mflo</code>两种乘除法指令。</li></ol><p>也就是说，对于乘除法计算指令涉及的阻塞，我们只要无脑阻塞在D级即可。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-comment">//CTRL</span><br><span class="hljs-keyword">assign</span> Start = Mult | Multu | Div | Divu;<br></code></pre></td></tr></table></figure><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-comment">//E_MDU</span><br><span class="hljs-keyword">always</span> @(<span class="hljs-keyword">posedge</span> clk) <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">if</span> (reset) <span class="hljs-keyword">begin</span><br>        ...<br>    <span class="hljs-keyword">end</span><br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">begin</span><br>        ....<br>        <span class="hljs-keyword">if</span> (Start) <span class="hljs-keyword">begin</span><br>            <span class="hljs-keyword">if</span> (MDUOp == `MDU_DIV || MDUOp == `MDU_DIVU) <span class="hljs-keyword">begin</span><br>                cnt &lt;= <span class="hljs-number">4&#x27;d10</span>;<br>            <span class="hljs-keyword">end</span><br>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(MDUOp == `MDU_MULT || MDUOp == `MDU_MULTU) <span class="hljs-keyword">begin</span><br>                cnt &lt;= <span class="hljs-number">4&#x27;d5</span>;<br>            <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">end</span><br><br>        <span class="hljs-keyword">if</span> (cnt &gt; <span class="hljs-number">4&#x27;d0</span>) <span class="hljs-keyword">begin</span><br>            cnt &lt;= cnt - <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">end</span><br>    <span class="hljs-keyword">end</span><br><span class="hljs-keyword">end</span><br><br>    <span class="hljs-keyword">assign</span> MDUC = (MDUOp == `MDU_MFHI) ? HIreg :<br>                  (MDUOp == `MDU_MFLO) ? LOreg :<br>                  <span class="hljs-number">32&#x27;d0</span>;<br><br>    <span class="hljs-keyword">assign</span> Busy = (cnt != <span class="hljs-number">4&#x27;d0</span>) ? <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;<br></code></pre></td></tr></table></figure><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-comment">//Stall</span><br><span class="hljs-keyword">assign</span> E_stall_MDU = ((D_mult | D_multu | D_div | D_divu | D_mfhi | D_mflo | D_mthi | D_mtlo) &amp;&amp; (Start | Busy));<br></code></pre></td></tr></table></figure><p>这就完了吗？当然不是！观察一下<code>mfhi mflo</code>,他们读取hi和lo的值是要写入<code>rd</code>寄存器的，这就不可避免地带来了数据冲突，因此<code>E_Tnew = (E_Mfhi || E_Mflo) ? 1 : 0;</code></p><p>如此，我们就顺利滑过了E_MDU，庆祝一下吧。</p><h2 id="M-BE"><a href="#M-BE" class="headerlink" title="M_BE"></a>M_BE</h2><p>我们知道，P6实现了DM外置，所以在CPU内部要实现和外置DM的数据交互。M_BE位于DM之前，处理<strong>store</strong>类指令，这也很好理解，毕竟store是向DM写入数据；相应地，M_DE位于DM之后处理load类指令，因为load是从DM取出数据存入GRF中的。</p><p>先来看M_BE。重要变量</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">output</span> <span class="hljs-keyword">reg</span> [ <span class="hljs-number">3</span>:<span class="hljs-number">0</span>] M_Data_Byteen,  <span class="hljs-comment">//字节使能</span><br><span class="hljs-keyword">output</span> <span class="hljs-keyword">reg</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_Data_Wdata    <span class="hljs-comment">//M级DM待写入的数据</span><br></code></pre></td></tr></table></figure><p>四位<code>M_Data_Byteen</code>中的某一位为1代表对应的8位要写入数据，这里只要看仔细<code>sw,sh,sb</code>的Rtl语言正常翻译就行。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">case</span> (BEOp)<br>  `BE_SW: <span class="hljs-keyword">begin</span><br>    M_Data_Byteen = <span class="hljs-number">4&#x27;b1111</span>;<br>    M_Data_Wdata  = WD;<br>  <span class="hljs-keyword">end</span><br>  `BE_SH: <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">if</span> (Addr[<span class="hljs-number">1</span>] == <span class="hljs-number">0</span>) <span class="hljs-keyword">begin</span><br>      M_Data_Byteen = <span class="hljs-number">4&#x27;b0011</span>;<br>      M_Data_Wdata  = &#123;<span class="hljs-number">16&#x27;b0</span>, WD[<span class="hljs-number">15</span>:<span class="hljs-number">0</span>]&#125;;<br>    <span class="hljs-keyword">end</span> <span class="hljs-keyword">else</span> <span class="hljs-keyword">begin</span><br>      M_Data_Byteen = <span class="hljs-number">4&#x27;b1100</span>;<br>      M_Data_Wdata  = &#123;WD[<span class="hljs-number">15</span>:<span class="hljs-number">0</span>], <span class="hljs-number">16&#x27;b0</span>&#125;;<br>    <span class="hljs-keyword">end</span><br>  <span class="hljs-keyword">end</span><br>  `BE_SB: <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">if</span> (Addr[<span class="hljs-number">1</span>:<span class="hljs-number">0</span>] == <span class="hljs-number">2&#x27;b00</span>) <span class="hljs-keyword">begin</span><br>      M_Data_Byteen = <span class="hljs-number">4&#x27;b0001</span>;<br>      M_Data_Wdata  = &#123;<span class="hljs-number">24&#x27;b0</span>, WD[<span class="hljs-number">7</span>:<span class="hljs-number">0</span>]&#125;;<br>    <span class="hljs-keyword">end</span> <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (Addr[<span class="hljs-number">1</span>:<span class="hljs-number">0</span>] == <span class="hljs-number">2&#x27;b01</span>) <span class="hljs-keyword">begin</span><br>      M_Data_Byteen = <span class="hljs-number">4&#x27;b0010</span>;<br>      M_Data_Wdata  = &#123;<span class="hljs-number">16&#x27;b0</span>, WD[<span class="hljs-number">7</span>:<span class="hljs-number">0</span>], <span class="hljs-number">8&#x27;b0</span>&#125;;<br>    <span class="hljs-keyword">end</span> <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (Addr[<span class="hljs-number">1</span>:<span class="hljs-number">0</span>] == <span class="hljs-number">2&#x27;b10</span>) <span class="hljs-keyword">begin</span><br>      M_Data_Byteen = <span class="hljs-number">4&#x27;b0100</span>;<br>      M_Data_Wdata  = &#123;<span class="hljs-number">8&#x27;b0</span>, WD[<span class="hljs-number">7</span>:<span class="hljs-number">0</span>], <span class="hljs-number">16&#x27;b0</span>&#125;;<br>    <span class="hljs-keyword">end</span> <span class="hljs-keyword">else</span> <span class="hljs-keyword">begin</span><br>      M_Data_Byteen = <span class="hljs-number">4&#x27;b1000</span>;<br>      M_Data_Wdata  = &#123;WD[<span class="hljs-number">7</span>:<span class="hljs-number">0</span>], <span class="hljs-number">24&#x27;b0</span>&#125;;<br>    <span class="hljs-keyword">end</span><br>  <span class="hljs-keyword">end</span><br>  <span class="hljs-keyword">default</span>: <span class="hljs-keyword">begin</span><br>    M_Data_Byteen = <span class="hljs-number">4&#x27;b0000</span>;<br>    M_Data_Wdata  = <span class="hljs-number">32&#x27;b0</span>;<br>  <span class="hljs-keyword">end</span><br><span class="hljs-keyword">endcase</span><br></code></pre></td></tr></table></figure><h2 id="M-DE"><a href="#M-DE" class="headerlink" title="M_DE"></a>M_DE</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">case</span> (DEOp)<br>        `DE_LW : <span class="hljs-keyword">begin</span> <br>            RD = M_Data_Rdata;<br>        <span class="hljs-keyword">end</span> <br>        `DE_LH : <span class="hljs-keyword">begin</span><br>            RD = &#123;&#123;<span class="hljs-number">16</span>&#123;M_Data_Rdata[<span class="hljs-number">16</span> * Addr[<span class="hljs-number">1</span>] + <span class="hljs-number">15</span>]&#125;&#125;,M_Data_Rdata[(<span class="hljs-number">16</span> * Addr[<span class="hljs-number">1</span>] + <span class="hljs-number">15</span>) -: <span class="hljs-number">16</span>]&#125;;<br>        <span class="hljs-keyword">end</span><br>        `DE_LB : <span class="hljs-keyword">begin</span><br>            RD = &#123;&#123;<span class="hljs-number">24</span>&#123;M_Data_Rdata[<span class="hljs-number">8</span> * Addr[<span class="hljs-number">1</span>:<span class="hljs-number">0</span>] + <span class="hljs-number">7</span>]&#125;&#125;,M_Data_Rdata[(<span class="hljs-number">8</span> * Addr[<span class="hljs-number">1</span>:<span class="hljs-number">0</span>] + <span class="hljs-number">7</span>) -: <span class="hljs-number">8</span>]&#125;;<br>        <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">default</span>: <span class="hljs-keyword">begin</span><br>            RD = <span class="hljs-number">32&#x27;b0</span>;<br>        <span class="hljs-keyword">end</span><br>    <span class="hljs-keyword">endcase</span><br></code></pre></td></tr></table></figure><p><strong>Attention</strong>:<code>[(16 * Addr[1] + 15) -: 16]</code>这里是减，不要写成加法形式。千万别写成加法形式！！！</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>剩下的就是在CTRL里面进行小修小补，在Stall里面完善一下T_new,T_use，在mips里面添加新增的对外交互接口并找到他们对应的连线。此外，所有新添加的指令，除了乘除类，都在P5有过类似的指令，照着之前的添加即可。</p><h2 id="思考题"><a href="#思考题" class="headerlink" title="思考题"></a>思考题</h2><ol><li>为什么需要有单独的乘除法部件而不是整合进 ALU？为何需要有独立的 HI、LO 寄存器？</li></ol><p>答：因为乘除法的效率比ALU其他运输运算的效率要低很多（5&#x2F;10），不单独设计的话会让CPU整体效率降低，使用单独的HI，LO寄存器是因为乘除法需要两个寄存器来保存结果，和其他指令需要一个不一样。</p><ol start="2"><li>真实的流水线 CPU 是如何使用实现乘除法的？请查阅相关资料进行简单说明。</li></ol><p>答：在真实的流水线 CPU 中，乘法通常有若干个较小的乘法单元组成（组合逻辑），然后每个周期计算特定的几位，依次累加起来，于是会在几个周期后得到正确的最终结果；除法通常使用试商法，通常也是使用组合逻辑在一个周期内计算 4 位左右的商，经过 8 个周期正好可以计算结束。</p><ol start="3"><li>请结合自己的实现分析，你是如何处理 Busy 信号带来的周期阻塞的？</li></ol><p>答：当Busy或Start信号为1并且D级为乘除法指令的时候阻塞。</p><ol start="4"><li>请问采用字节使能信号的方式处理写指令有什么好处？（提示：从清晰性、统一性等角度考虑）</li></ol><p>答：可以在sh和sb的时候只传入写使能信号和数据，不需要为他们单独设计电路，实现高内聚低耦合。</p><ol start="5"><li>请思考，我们在按字节读和按字节写时，实际从 DM 获得的数据和向 DM 写入的数据是否是一字节？在什么情况下我们按字节读和按字节写的效率会高于按字读和按字写呢？</li></ol><p>答：不是，是4字节。处理单字节 &#x2F; 非连续字节数据时。</p><ol start="6"><li>为了对抗复杂性你采取了哪些抽象和规范手段？这些手段在译码和处理数据冲突的时候有什么样的特点与帮助？</li></ol><p>答：应当按照cal_r,cal_i,load,store,mult&#x2F;div,branch,jump来分类指令，这样可以将同类型的指令在生成控制信号的时候统一处理。但是我没这么干，感觉这种分类标准不统一，在实现的时候反而要思前想后，相比于单个指令的实现，我还是觉得两者的复杂度相差不大。</p><ol start="7"><li>在本实验中你遇到了哪些不同指令类型组合产生的冲突？你又是如何解决的？相应的测试样例是什么样的？</li></ol><p>答：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog">mult $<span class="hljs-number">1</span>, $<span class="hljs-number">2</span><br>mfhi $<span class="hljs-number">3</span><br>mflo $<span class="hljs-number">4</span><br></code></pre></td></tr></table></figure><p>将mfhi和mflo阻塞在D级</p><ol start="8"><li>如果你是手动构造的样例，请说明构造策略，说明你的测试程序如何保证覆盖了所有需要测试的情况；如果你是完全随机生成的测试样例，请思考完全随机的测试程序有何不足之处；如果你在生成测试样例时采用了特殊的策略，比如构造连续数据冒险序列，请你描述一下你使用的策略如何结合了随机性达到强测的效果。</li></ol><p>答：在手动构造数据的时候，在P5的基础上，加入连续操作乘除法的指令，并且在乘除法之间的5-10周期内加入其他指令，来检测新增的指令是否有错误。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">爱是愚人的国度<br>不能自拔不懂退出<br>我们都回不去最初<br>曾美丽但还是不满足<br>爱是自愚愚人演出<br>答案清楚才能谢幕<br>剧情 是笑 是哭<br>爱是愚人的国度<br>看我们演的好辛苦<br>是你所谓的领悟<br>我不懂我不哭<br>看悲欢喜怒每一步<br>是疲惫还是依赖的束缚<br>来 你能不能再重复<br>让我懂让我哭<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> verilog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P5课下</title>
      <link href="/2026/01/21/P5%E8%AF%BE%E4%B8%8B/"/>
      <url>/2026/01/21/P5%E8%AF%BE%E4%B8%8B/</url>
      
        <content type="html"><![CDATA[<h2 id="序言"><a href="#序言" class="headerlink" title="序言"></a>序言</h2><p>从P5开始，我们迎来了崭新的篇章，<code>5级流水线</code>。我真的想不出什么其他词汇来形容他的伟大，总之他让无数学生为之倾倒，也让<code>少数</code>情侣为之分道扬镳(别怕)。从这一章节开始，我们设计的CPU难度有了质的飞跃。</p><p>相信很多同学在听了计组理论课关于<code>阻塞</code>和<code>转发</code>的讲解后一头雾水，看了学长的代码之后仍然有一万个为什么。这篇文章笔者将以五级流水线的具体设计为主线，尽可能地解答大家在设计过程中遇到的疑惑，希望能给大家带来一些帮助，如有不详尽之处还请大家多多包涵，也欢迎大家批评指正。</p><h2 id="关于命名方式的一些Tips"><a href="#关于命名方式的一些Tips" class="headerlink" title="关于命名方式的一些Tips"></a>关于命名方式的一些Tips</h2><p>在五级流水线的设计中我们会遇到数不尽的端口，如果命名方式不合理，可能我们在 <code>VScode</code> 的提示下也会云里雾里，但是如果我们有一套自己的命名规则，那么我们可以清楚地识别哪些是端口名称，哪些是连线(实例化时的<code>wire</code>型变量)的名称，从而极大地缩减编码时的工作量和<code>bug</code>率。</p><p>笔者习惯将端口名称大写，将连线名称小写，(<code>clk</code>和<code>reset</code>除外)虽然这种方式不能借助ISE的实例化工具完成，但是至少于编程者而言各变量的含义了然于心，不会越编越乱，以至于自己都不知道自己在干什么。怎么说呢，每个人有每个人独特的方式，大家只要探索到适合自己的命名方式就好。</p><h2 id="设计综述"><a href="#设计综述" class="headerlink" title="设计综述"></a>设计综述</h2><p>五级流水线，顾名思义，当然要弄清楚是哪五级，以及分别有哪些流水线寄存器，以及每一级都包含哪些元件。</p><blockquote><p>F级: IFU<br>D级：CMP,EXT,GRF,NPC,D_reg<br>E级：E_ALU,E_reg<br>M级：M_DM,M_reg<br>W级：W_reg</p></blockquote><p>显然，五级流水线，四个流水线寄存器，构成了P5设计的基本框架。除此之外，还有<code>Stall</code>模块用来处理阻塞，<code>mips</code>模块在组装元件的过程中实现转发，<code>def</code>模块定义一些宏，<code>CTRL</code>模块实现控制信号以及配合<code>Stall</code>模块实现阻塞判断。</p><p>设计图可以参照：<br><img src="/images/Five-Stage_Pipeline_Logisim_Schematic.png" alt="Five-Stage Pipeline Logisim Schematic"></p><p>P5课下要求实现的指令是：add, sub, ori, lw, sw, beq, lui, jal, jr, nop。由于接口繁多，笔者在搭建时本着接口<code>能少就少</code>的原则完成设计，(当然也要给课上留好一定的扩展接口)。</p><p>在大家大概了解了每一个模块的作用以及我们要实现的具体指令以后我们就可以进入详细的设计分析了。</p><h2 id="F-IFU"><a href="#F-IFU" class="headerlink" title="F_IFU"></a>F_IFU</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> F_IFU (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] NPC,<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">input</span> PCWE,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] PC,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] Instr<br>);<br></code></pre></td></tr></table></figure><p><code>F_IFU</code>和<code>P4</code>相比变化不大只是在输入中新增了<code>PCWE</code>。当<code>Stall</code>生效时，<code>IFU</code>中的<code>PC</code>不再更新，直到阻塞结束。</p><h2 id="D-reg"><a href="#D-reg" class="headerlink" title="D_reg"></a>D_reg</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> D_reg (<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">input</span> Flush,   <br>    <span class="hljs-keyword">input</span> WE,    <br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] F_PC,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] F_Instr,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_PC,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_Instr<br>);<br></code></pre></td></tr></table></figure><p>D级寄存器负责连接F级和D级流水线，因为课程组规定阻塞只能发生在<strong>D级</strong>，因此当阻塞指令生效时，<code>WE</code>禁止向<code>D级</code>寄存器写入指令，<code>Flush</code>刷新E级寄存器为<code>nop</code>，从而使得<code>nop</code>信号随着流水线流下去，从而实现阻塞。</p><blockquote><p>Q: PC和Instr要一直流水下去吗？<br>A: 是的。只有PC和instr随流水线一直流水下去我们才能判断每一级流水线的当前指令是什么。</p></blockquote><p><strong>instr主要用途</strong>：阻塞判断，每一级流水线译码(分布式)。<br><strong>PC主要用途</strong>：<code>DM</code>要输出<code>PC</code>，<code>jal</code>指令向<code>GPR[rs]</code>写入涉及<code>PC</code>，</p><h2 id="D-EXT"><a href="#D-EXT" class="headerlink" title="D_EXT"></a>D_EXT</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> D_EXT (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">15</span>:<span class="hljs-number">0</span>] imm,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] ext,<br>    <span class="hljs-keyword">input</span> EXTOp<br>);<br></code></pre></td></tr></table></figure><p><code>EXT</code>和<code>P4</code>没有什么区别。</p><h2 id="D-CMP"><a href="#D-CMP" class="headerlink" title="D_CMP"></a>D_CMP</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> D_CMP (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] RS,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] RT,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] CMPOp,    <span class="hljs-comment">//提前为其他转移信号留下接口</span><br>    <span class="hljs-keyword">output</span> Beq_jump<br>);<br></code></pre></td></tr></table></figure><p><code>D_CMP</code>存在的意义是将<code>BEQ</code>的比较提前，这样可以尽早判断是否跳转，减少流水线的”白流”。</p><h2 id="D-NPC"><a href="#D-NPC" class="headerlink" title="D_NPC"></a>D_NPC</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> D_NPC (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] F_PC,   <span class="hljs-comment">//正常计算F_PC+4</span><br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_PC,   <span class="hljs-comment">//流水</span><br>    <span class="hljs-keyword">input</span> Beq_jump,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] NPCOp,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] RA,     <span class="hljs-comment">//jr和jalr用</span><br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">25</span>:<span class="hljs-number">0</span>] IMM,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] NPC<br>    );<br></code></pre></td></tr></table></figure><p>在<code>D_NPC</code>模块的接口相较于<code>P4</code>多出了<code>Beq_jump</code>,<code>F_PC</code>,<code>D_PC</code>三个信号。<code>Beq_jump</code>不必赘述，我们重点区分一下<code>F_PC</code>和<code>D_PC</code>以及他们的用法。<br>首先大家需要明确,在一般情况下:<code>F_PC = D_PC + 4</code>。(因为流水线走了一级嘛)。</p><p>接着我们来看一个流水线简图：</p><table><thead><tr><th>时钟周期</th><th>F级（取指）</th><th>D级（译码）</th><th>E级（执行）</th><th>M级（访存）</th><th>W级（写回）</th></tr></thead><tbody><tr><td>拍1</td><td>取 BEQ（0x00）</td><td>-</td><td>-</td><td>-</td><td>-</td></tr><tr><td>拍2</td><td>取下条 A（0x04）</td><td>译 BEQ（0x00）并判断</td><td>-</td><td>-</td><td>-</td></tr><tr><td>拍3</td><td>取下下条 B（0x?）</td><td>译下条 A（0x04）</td><td>执行BEQ对地址的更改</td><td>-</td><td>-</td></tr></tbody></table><p>我们可以采用类似延迟槽的思想，<code>A</code>指令无论如何都会被执行，那么我们重点考虑<code>B</code>指令。</p><p><strong>如果</strong> <code>Beq_jump = 0</code>，我们应该接着F_PC取PC + 4，这也就决定了<code>assign PC_4 = F_PC + 4</code>;</p><p><strong>如果</strong> <code>Beq_jump = 1</code>，那我们根据Beq的RTL语言。PC+4使用的应该是D_PC，这也决定了<code>assign IMM_BEQ = &#123;&#123;14&#123;IMM[15]&#125;,IMM[15:0],2'b0&#125;&#125; + D_PC + 4</code>。</p><p>根据上述分析，我们可以同理得出，<code>assign IMM_J_Jal = &#123;D_PC[31:28],IMM,2&#39;b0&#125;</code></p><h2 id="D-GRF"><a href="#D-GRF" class="headerlink" title="D_GRF"></a>D_GRF</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> D_GRF (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] PC,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] A1,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] A2,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] A3,       <br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] WD,<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] RD1,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] RD2<br>);<br></code></pre></td></tr></table></figure><p>在<code>GRF</code>这个模块，虽然模块名称叫<code>D_GRF</code>(因为这里理论上只读，在W级才执行写操作)，但是由于全局<code>GRF</code>的唯一性，我们保留了<code>WD</code>,<code>A3</code>这两个写入数据必要的接口，当指令流水到<code>W级</code>时，根据<code>A3是否等于0</code>来决定是否写入。</p><p>为什么是根据<code>A3是否等于0</code>来决定是否写入呢，因为我们在这里为了简化设计采用了<code>隐式写使能</code>，即取消<code>显式WE信号</code>，转而通过<code>A3</code>的取值来决定是否写入<code>GRF</code>。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">if</span> (A3 != <span class="hljs-number">0</span>) <span class="hljs-keyword">begin</span><br>    register[A3] &lt;= WD;<br>    display(<span class="hljs-string">&quot;%d@%h: $%d &lt;= %h&quot;</span>,<span class="hljs-built_in">$time</span>,PC,A3,WD);<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>对于<code>A3</code>的取值，<code>CTRL</code>模块会出手的，我们在<code>CTRL</code>会详细解释，这里大家只要留个印象即可。因为是在<code>W</code>级流水线写入，所以聪明的你应该可以感觉到<code>WD</code>，<code>A3</code>在<code>mips</code>模块实例化时对应的<code>wire</code>是什么了吧，没猜到也没关系，我们接着往下看。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> RD1 = (A1 == <span class="hljs-number">0</span>) ? <span class="hljs-number">32&#x27;b0</span> : (A1 == A3) ? WD : register[A1];<br><span class="hljs-keyword">assign</span> RD2 = (A2 == <span class="hljs-number">0</span>) ? <span class="hljs-number">32&#x27;b0</span> : (A2 == A3) ? WD : register[A2];<br></code></pre></td></tr></table></figure><p>这段代码翻译过来就是，如果访问<code>0</code>号寄存器，直接给出<code>0</code>，如果访问的寄存器和<code>A3</code>冲突，那就给出<code>WD</code>的值，如果都不是，那就正常给出<code>register[A1]</code>。</p><p>当进入<code>A1</code>和<code>A3</code>冲突的分支时，那么<code>A3</code>一定不为<code>0</code>，相当于写使能有效。这段代码的作用就更加明显：**如果访问的寄存器和W级流水线将要写入的寄存器冲突，那么给出的值是将要写入的WD而不是现在的寄存器值register[A1]**。</p><p>这就是<strong>转发</strong>！！！只不过这是<code>GRF</code>内部转发，我们在<code>mips</code>模块中大量实现的是外部转发。大家还是留个印象，到时候再说😁。</p><h2 id="E-reg"><a href="#E-reg" class="headerlink" title="E_reg"></a>E_reg</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> E_reg (<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">input</span> Flush,<br>    <span class="hljs-keyword">input</span> WE,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_PC,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_Instr,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_EXT,   <span class="hljs-comment">//要用于ALU的计算</span><br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_Rs_data, <br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_Rt_data,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_PC,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_Instr,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_EXT,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_Rs_data,   <br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_Rt_data<br>);<br></code></pre></td></tr></table></figure><p><code>E_reg</code>的接口相较于<code>D_reg</code>又多了<code>D_EXT</code>,<code>D_Rs_data</code>,<code>D_Rt_data</code>,这三个量都是<code>E</code>级不可或缺的变量，所以也没什么可说的😥。</p><h2 id="E-ALU"><a href="#E-ALU" class="headerlink" title="E_ALU"></a>E_ALU</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> E_ALU (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] A,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] B,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] ALUOp,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] C  <br>);<br></code></pre></td></tr></table></figure><p>这个也再正常不过了，我们下一个。</p><h2 id="M-reg"><a href="#M-reg" class="headerlink" title="M_reg"></a>M_reg</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> M_reg (<br>    <span class="hljs-keyword">input</span> Flush,<br>    <span class="hljs-keyword">input</span> WE,<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_PC,      <br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_Instr,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_Rt_data,  <span class="hljs-comment">//sw用</span><br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_C,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_PC,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_Instr,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_Rt_data,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_C<br>);<br></code></pre></td></tr></table></figure><p><code>M_reg</code>相较于<code>E_reg</code>多了<code>E_C</code>,这是<code>ALU</code>计算出的结果，肯定要往后流水，<code>M</code>级和<code>W</code>级都要用。少了的就是没用了，端口太多，全传过去有点抽象😥。</p><h2 id="M-DM"><a href="#M-DM" class="headerlink" title="M_DM"></a>M_DM</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> M_DM (<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">input</span> DMWE,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] PC,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] Addr,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] WD,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] RD<br>);<br></code></pre></td></tr></table></figure><h2 id="W-reg"><a href="#W-reg" class="headerlink" title="W_reg"></a>W_reg</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> W_reg (<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">input</span> Flush,<br>    <span class="hljs-keyword">input</span> WE,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_PC,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_Instr,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_RD,       <span class="hljs-comment">//往GRF写入与DM的运算结果有关</span><br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_C,        <span class="hljs-comment">//往GRF写入与ALU的运算结果有关</span><br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] W_PC,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] W_Instr,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] W_RD,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] W_C<br>);<br></code></pre></td></tr></table></figure><p>至此，我们完成了各个元件的搭建，其实<code>P5</code>和<code>P4</code>相比，这些基本元件几乎没变，变的是<code>CTRL</code>，<code>mips</code>,还新增了<code>Stall</code>。</p><p>接下来就是<code>P5</code>的核心，也是最难的部分————<strong>阻塞与转发</strong>。</p><hr><h2 id="CTRL"><a href="#CTRL" class="headerlink" title="CTRL"></a>CTRL</h2><p>CTRL是译码器，主流的有两种设计方式：</p><p><strong>集中式译码</strong> ：在取指令（F 级）时或者读取寄存器阵列信息（D 级）前，将所有的控制信号全部解析出，然后让其随着流水往后逐级传递。</p><p><strong>分布式译码</strong> ：每一级都部署一个译码器，负责译出当前级所需控制信号。</p><p>为了尽可能减少一个模块中的接口数量，这里采用分布式译码。在设计CTRL模块时输出包含所有的<strong>控制信号</strong>以及<strong>rs,rt</strong>…在每一级可能会用到的信号，在<code>mips</code>模块中只需要在每一级流水线译出该级流水线所需的信号即可。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> CTRL (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] Instr,<br>      <br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] Rs,     <span class="hljs-comment">//转发冲突判断</span><br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] Rt,     <span class="hljs-comment">//转发冲突判断</span><br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] Rd,     <span class="hljs-comment">//转发冲突判断的待选择信号</span><br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">15</span>:<span class="hljs-number">0</span>] Imm1,  <span class="hljs-comment">//EXT用</span><br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">25</span>:<span class="hljs-number">0</span>] Imm2,  <span class="hljs-comment">//NPC用</span><br><br>    <span class="hljs-keyword">output</span> EXTOp,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] CMPOp,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] NPCOp,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] ALUOp,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] ALUBSel,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] GRFWDSel,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] GRFA3Sel,<br>    <span class="hljs-keyword">output</span> DMWE,<br>    <span class="hljs-comment">//配合Stall</span><br>    <span class="hljs-keyword">output</span> Add,<br>    <span class="hljs-keyword">output</span> Sub,<br>    <span class="hljs-keyword">output</span> Ori,<br>    <span class="hljs-keyword">output</span> Lw,<br>    <span class="hljs-keyword">output</span> Sw,<br>    <span class="hljs-keyword">output</span> Beq,<br>    <span class="hljs-keyword">output</span> Lui,<br>    <span class="hljs-keyword">output</span> Sll,<br>    <span class="hljs-keyword">output</span> J,<br>    <span class="hljs-keyword">output</span> Jal,<br>    <span class="hljs-keyword">output</span> Jr,<br>    <span class="hljs-keyword">output</span> Jalr<br>);<br></code></pre></td></tr></table></figure><p>控制信号具体值可以参看译码表。如图：</p><table><thead><tr><th align="center">指令</th><th align="center">opcode(31:26)</th><th align="center">funct(5:0)</th><th align="center">EXTOp</th><th align="center">CMPOp</th><th align="center">NPCOp</th><th align="center">ALUOp</th><th align="center">ALUBSel</th><th align="center">GRFWDSel</th><th align="center">GRFA3Sel</th><th align="center">DMWE</th></tr></thead><tbody><tr><td align="center">功能</td><td align="center">opcode</td><td align="center">func</td><td align="center">立即数扩展</td><td align="center">比较器操作</td><td align="center">下一条PC计算</td><td align="center">ALU运算</td><td align="center">ALUB端口选择</td><td align="center">GRF写回数据来源</td><td align="center">GRF写回地址选择</td><td align="center">数据存储器写使能</td></tr><tr><td align="center">add</td><td align="center">000000</td><td align="center">100000</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_PC4</td><td align="center">ALU_add</td><td align="center">ALUBrt</td><td align="center">GRFWDALU</td><td align="center">GRFA3rd</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">sub</td><td align="center">000000</td><td align="center">100010</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_PC4</td><td align="center">ALU_sub</td><td align="center">ALUBrt</td><td align="center">GRFWDALU</td><td align="center">GRFA3rd</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">ori</td><td align="center">001101</td><td align="center">x</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_PC4</td><td align="center">ALU_ori</td><td align="center">ALUBimm</td><td align="center">GRFWDALU</td><td align="center">GRFA3rt</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">lw</td><td align="center">100011</td><td align="center">x</td><td align="center">EXT_SIGN</td><td align="center">CMP_BEQ</td><td align="center">NPC_PC4</td><td align="center">ALU_add</td><td align="center">ALUBimm</td><td align="center">GRFWDDM</td><td align="center">GRFA3rt</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">sw</td><td align="center">101011</td><td align="center">x</td><td align="center">EXT_SIGN</td><td align="center">CMP_BEQ</td><td align="center">NPC_PC4</td><td align="center">ALU_add</td><td align="center">ALUBimm</td><td align="center">GRFWDALU</td><td align="center">GRFA3rt</td><td align="center">DMWE_ONE</td></tr><tr><td align="center">beq</td><td align="center">000100</td><td align="center">x</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_BEQ</td><td align="center">ALU_sub</td><td align="center">ALUBrt</td><td align="center">GRFWDALU</td><td align="center">GRFA3rt</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">lui</td><td align="center">001111</td><td align="center">x</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_PC4</td><td align="center">ALU_lui</td><td align="center">ALUBimm</td><td align="center">GRFWDALU</td><td align="center">GRFA3rt</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">sll</td><td align="center">000000</td><td align="center">000000</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_PC4</td><td align="center">ALU_sll</td><td align="center">ALUBrt</td><td align="center">GRFWDALU</td><td align="center">GRFA3rd</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">j</td><td align="center">000010</td><td align="center">x</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_J_Jal</td><td align="center">ALU_add</td><td align="center">ALUBrt</td><td align="center">GRFWDALU</td><td align="center">GRFA3rt</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">jal</td><td align="center">000011</td><td align="center">x</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_J_Jal</td><td align="center">ALU_add</td><td align="center">ALUBrt</td><td align="center">GRFWDPC8</td><td align="center">GRFA331</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">jr</td><td align="center">000000</td><td align="center">001000</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_Jr_Jalr</td><td align="center">ALU_add</td><td align="center">ALUBrt</td><td align="center">GRFWDALU</td><td align="center">GRFA3rd</td><td align="center">DMWE_ZERO</td></tr><tr><td align="center">jalr</td><td align="center">000000</td><td align="center">001001</td><td align="center">EXT_ZERO</td><td align="center">CMP_BEQ</td><td align="center">NPC_Jr_Jalr</td><td align="center">ALU_add</td><td align="center">ALUBrt</td><td align="center">GRFWDPC8</td><td align="center">GRFA3rd</td><td align="center">DMWE_ZERO</td></tr></tbody></table><h2 id="Stall"><a href="#Stall" class="headerlink" title="Stall"></a>Stall</h2><p>阻塞的概念是,当我们发现单凭转发已经不足以解决读写冲突的问题时，不得不让当前指令停在<strong>D级</strong>，直到随流水线流水的指令产生写入数据的时间<code>(Tnew)</code>小于等于D级指令将要读取寄存器的时间<code>(Tuse)</code>,D级流水线寄存器才<strong>放行</strong>，在这期间一直是<code>nop</code>从<code>E</code>级不断向后流水。</p><p>这里我们对<strong>Tnew</strong>和<strong>Tuse</strong>做详细阐释：</p><p><strong>Tuse</strong>:对于某一个指令的某一个数据需求，我们定义需求时间<strong>Tuse</strong>为：</p><p>这条指令位于 <strong>D</strong> 级的时候，再经过多少个时钟周期就必须要使用相应的数据。例如，对于 <code>beq</code> 指令，立刻就要使用数据，所以 <code>Tuse = 0</code>;对于 <code>add</code> 指令，等待下一个时钟周期它进入 E 级才要使用数据，所以 <code>T_use = 1</code>;而对于 <code>sw</code> 指令，在 E 级它需要 GPR[rs] 的数据来计算地址，在 M 级需要 GPR[rt] 来存入值，所以 <code>rs_Tuse = 1,rt_Tuse = 2</code>。</p><p><strong>Tnew</strong>: 对于某个指令的数据产出，我们定义供给时间<strong>Tnew</strong>为：</p><p>位于<strong>某个流水级</strong>的某个指令，它经过多少个时钟周期可以算出结果并且存储到<strong>流水级寄存器</strong>里。注意是<strong>流水级寄存器</strong>，不是<strong>GRF</strong>。对于 <code>add</code> 指令，当它处于 E 级，此时结果还没有存储到流水级寄存器里，所以此时它的 <code>Tnew = 1</code>,而当它处于 M 或者 W 级，此时结果已经写入了流水级寄存器，所以此时 <code>Tnew = 0</code>.</p><blockquote><p>当Tnew &gt; Tuse 时，数据不能及时算出，需要通过阻塞来解决。<br>当Tnew &lt;&#x3D; Tuse 数据能够及时算出，可以转发解决。</p></blockquote><p>由于<code>Tnew</code>的是一个<strong>动态值</strong>，而<code>Tuse</code>为一个<strong>静态值</strong>，随着<code>Tnew</code>的不断减小，当<code>Tnew = Tuse</code> 时，D级流水线寄存器就可以<strong>放行</strong>了。以上就是阻塞的大概过程，我们看具体的代码再做理解。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> STALL (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_instr,   <br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_instr,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_instr,<br>    <span class="hljs-keyword">output</span> Stall<br>);<br></code></pre></td></tr></table></figure><p>在D级只产生<code>Tuse</code>,在<code>E</code>，<code>M</code>级产生<code>Tnew</code>,(W级Tnew恒等于0)我们只需要分三个区间分别写出对应的<code>Tnew</code>,<code>Tuse</code>,然后列出阻塞情况，最后通过<strong>或运算</strong>就可以得到阻塞信号<code>Stall</code>;</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> E_stall_rs = (D_rs == E_GRFA3 &amp;&amp; (D_rs != <span class="hljs-number">0</span>)) &amp;&amp; (rs_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> E_stall_rt = (D_rt == E_GRFA3 &amp;&amp; (D_rt != <span class="hljs-number">0</span>)) &amp;&amp; (rt_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rs = (D_rs == M_GRFA3 &amp;&amp; (D_rs != <span class="hljs-number">0</span>)) &amp;&amp; (rs_Tuse &lt; M_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rt = (D_rt == M_GRFA3 &amp;&amp; (D_rt != <span class="hljs-number">0</span>)) &amp;&amp; (rt_Tuse &lt; M_Tnew);<br><span class="hljs-keyword">assign</span> Stall = E_stall_rs | E_stall_rt | M_stall_rs | M_stall_rt;<br></code></pre></td></tr></table></figure><p>第一行代表的含义是<strong>E级导致阻塞，rs地址冲突</strong>。注意，当<code>rs = 0</code>是，默认寄存器的值已知，不需要阻塞。这里附上<code>Tnew</code>和<code>Tuse</code>的阻塞表。</p><p><strong>D_Tuse</strong>:</p><table><thead><tr><th align="center">指令</th><th align="center">add</th><th align="center">sub</th><th align="center">ori</th><th align="center">lw</th><th align="center">sw</th><th align="center">beq</th><th align="center">lui</th><th align="center">sll</th><th align="center">j</th><th align="center">jr</th><th align="center">jal</th><th align="center">jalr</th></tr></thead><tbody><tr><td align="center">D_rs_Tuse</td><td align="center">1</td><td align="center">1</td><td align="center">1</td><td align="center">1</td><td align="center">1</td><td align="center">0</td><td align="center">1</td><td align="center">3</td><td align="center">3</td><td align="center">0</td><td align="center">3</td><td align="center">0</td></tr><tr><td align="center">D_rt_Tuse</td><td align="center">1</td><td align="center">1</td><td align="center">3</td><td align="center">3</td><td align="center">2</td><td align="center">0</td><td align="center">3</td><td align="center">1</td><td align="center">3</td><td align="center">3</td><td align="center">3</td><td align="center">3</td></tr></tbody></table><p><strong>E_Tnew &amp; M_Tnew</strong>:</p><table><thead><tr><th align="center">指令</th><th align="center">add</th><th align="center">sub</th><th align="center">ori</th><th align="center">lw</th><th align="center">sw</th><th align="center">beq</th><th align="center">lui</th><th align="center">sll</th><th align="center">j</th><th align="center">jr</th><th align="center">jal</th><th align="center">jalr</th></tr></thead><tbody><tr><td align="center">E_Tnew</td><td align="center">1</td><td align="center">1</td><td align="center">1</td><td align="center">2</td><td align="center">0</td><td align="center">0</td><td align="center">1</td><td align="center">1</td><td align="center">0</td><td align="center">0</td><td align="center">0</td><td align="center">0</td></tr><tr><td align="center">M_Tnew</td><td align="center">0</td><td align="center">0</td><td align="center">0</td><td align="center">1</td><td align="center">0</td><td align="center">0</td><td align="center">0</td><td align="center">0</td><td align="center">0</td><td align="center">0</td><td align="center">0</td><td align="center">0</td></tr></tbody></table><h2 id="mips"><a href="#mips" class="headerlink" title="mips"></a>mips</h2><p>阻塞处理完，我们就要开始实例化模块并连接电路了，连接过程中实现<strong>外部转发</strong>，我们的五级流水线CPU就基本搭建完成了。总体来说，我们的设计主线是模块的实例化，在实例化过程中我们会发现某个需要使用的变量的缺失，这时我们在每一级的固定定义区定义我们需要的<code>wire</code>类型变量即可，大家完全不必纠结于在每一个模块开始时定义什么。</p><p><strong>特别的，E_GRFa3,E_GRFwd系列的指令需要在开头定义，因为这些指令如果在对应模块定义的话就会出现在定义前使用的情况，使用时编译器会自动隐式定义为1位的wire变量，如果你在之后再次定义的话，编译器会报错重复定义</strong>。</p><p>笔者这里在全局定义区定义了常用变量，大家可以参考这种定义方式，也可以放在固定的流水级，<strong>你开心就好</strong>😊</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-comment">//各级PC和Instr</span><br><span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] F_pc,F_instr,D_pc,D_instr,E_pc,E_instr,M_pc,M_instr,W_pc,W_instr;<br><span class="hljs-comment">//阻塞信号</span><br><span class="hljs-keyword">wire</span> stall;<br><span class="hljs-comment">//各级流水线寄存器使能信号和刷新信号</span><br><span class="hljs-keyword">wire</span> D_reg_flush,E_reg_flush,M_reg_flush,W_reg_flush,D_reg_we,E_reg_we,M_reg_we,W_reg_we;<br><span class="hljs-comment">//PC的使能信号</span><br><span class="hljs-keyword">wire</span> PCwe;<br></code></pre></td></tr></table></figure><p>接下来强调几个重要的模块。</p><h3 id="stall"><a href="#stall" class="headerlink" title="stall"></a>stall</h3><p>阻塞模块是全局变量，需要作为单独一个实例来控制各个流水线寄存器的运作，Stall的取值可能会影响的变量是<code>PC_we</code>,<code>D_reg_we</code>,<code>E_reg_flush</code>。</p><h3 id="D级"><a href="#D级" class="headerlink" title="D级"></a>D级</h3><p><code>D</code>级<code>GRF</code>实例化时<code>A3</code>接口和<code>WD</code>接口分别接<code>W_GRFa3</code>和<code>W_GRFwd</code>，从而实现GRF<strong>内部转发</strong>。而由于W级实现了内部转发，我们在转发<code>D_For_rs_data</code>和<code>D_For_rt_data</code>时只转发<code>E</code>，<code>M</code>即可：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> D_For_rs_data = (D_rs == <span class="hljs-number">5&#x27;d0</span>) ? <span class="hljs-number">32&#x27;d0</span> :<br>                       (D_rs == E_GRFa3) ? E_GRFwd :<br>                       (D_rs == M_GRFa3) ? M_GRFwd :<br>                       D_rs_data;<br></code></pre></td></tr></table></figure><p>这里很多同学会问，转发时如何保证<code>E_GRFwd</code>，<code>M_GRFwd</code>已经算出来了呢，其实很简单，当你在执行转发时，是不是说明<strong>不需要阻塞了</strong>，那不需要阻塞是否意味着你要的东西已经算出来了呢？</p><p>注意<code>D_NPC</code>和<code>D_CMP</code>用的<code>D_rs_data</code>和<code>D_rt_data</code>都是<strong>转发后的</strong>。</p><h3 id="E级"><a href="#E级" class="headerlink" title="E级"></a>E级</h3><p>因为<code>E</code>级没有<code>GRF</code>，即没有内部转发，因此<code>E</code>级<code>E_For_rs_data</code>和<code>E_For_rt_data</code>需要转发<code>M</code>，<code>W</code>。并且在<code>E</code>级要实现<code>E_GRFa3</code>和<code>E_GRFwd</code>这两个转发必要的数据赋值，<code>E_GRFa3</code>实现没什么坑点，值得一提的是，<code>E_GRFwd</code>没有<code>DMrd</code>这个选项，因为在<code>E</code>级，还没有算出<code>M</code>级的结果。</p><p>注意<code>E_ALU</code>用的<code>E_rs_data</code>和<code>E_rt_data</code>是<strong>转发后的</strong>。</p><h3 id="M级"><a href="#M级" class="headerlink" title="M级"></a>M级</h3><p><code>M</code>级只需要转发<code>rt_data</code>,因为<code>rs_data</code>不会在<code>M</code>级被使用。同理实现<code>M_GRFa3</code>和<code>M_GRFwd</code>，此时<code>M_GRFwd</code>有了<code>DMrd</code>这个选项。</p><p>注意<code>M_DM</code>用的<code>M_rt_data</code>是<strong>转发后</strong>的结果。</p><h3 id="W级"><a href="#W级" class="headerlink" title="W级"></a>W级</h3><p><code>W</code>级没什么好转发的，只需要实现<code>W_GRFa3</code>和<code>W_GRFwd</code>即可。</p><h2 id="Some-Tips"><a href="#Some-Tips" class="headerlink" title="Some Tips"></a>Some Tips</h2><ol><li>P5确实难，大家如果一开始上不了手也完全没关系，这不是你的问题。</li><li>老师会建议大家先写设计文档，再搭建CPU。听着很有道理哈，但是对于大部分同学来说听完理论课根本不知道从哪里下手，只能死磕设计文档。你可以把死磕设计文档理解成<strong>盯着markdown学P5</strong>，你就知道这种<strong>推荐行为</strong>有多搞笑了……(当然大佬除外，确实有些同学是具备直接上手设计文档的能力的)。我的建议是大家完全可以直接照着学长的博客搭一遍，搭的过程中你一定会有很多疑问，只要做标记就好，在你搭建的过程中你会对某些问题有自己的理解，在搭建完成后再把整个CPU过一遍，找出自己不理解的地方专项攻克。在做完这些工作之后你一定会对流水线CPU有更深刻的理解，这时你再把<code>Stall</code>，<code>mips</code>，<code>CTRL</code>这三个重点模块自己重新写一遍，就形成了属于自己的CPU。个人感觉这种<strong>盯着代码学P5</strong>要比<strong>盯着markdown学P5</strong>好很多。</li><li>这篇Blog只是介绍了课下的基础指令，单凭这些工作就想<strong>P5课上过，片叶不沾身</strong>有点困难，我们还需要完善某些接口，详见关于P5课上的<a href="https://cordial-kid.github.io/2026/01/21/P5%E8%AF%BE%E4%B8%8A/">这篇博客</a>。</li></ol><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>P5作为流水线CPU的开山之P，是后面几次实验的基础，同学们要认真对待。这篇博客说了这么多，虽然不能让你在看完之后立刻原地完美搭建5级流水线CPU，但是只要其中的一些细节解释能给你些许灵感,足矣。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">修炼爱情的悲欢<br>我们这些努力不简单<br>快乐炼成泪水<br>是一种勇敢<br>几年前的幻想<br>几年后的原谅<br>为一张脸去养一身伤<br>别讲想念我<br>我会受不了这样<br>笑着说爱让人疯狂<br>哭着说爱让人紧张<br>忘不了那个人就投降<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> verilog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」P5课上</title>
      <link href="/2026/01/21/P5%E8%AF%BE%E4%B8%8A/"/>
      <url>/2026/01/21/P5%E8%AF%BE%E4%B8%8A/</url>
      
        <content type="html"><![CDATA[<p>P5 课上三题有明显的规律，第一题一般只涉及 ALU 的修改，第二题是 b 类型的跳转指令，第三题稍微难一点，涉及访存以及对转发阻塞逻辑的修改。下面我们结合题目详细说明一下解题思路和步骤。</p><h2 id="CCC"><a href="#CCC" class="headerlink" title="CCC"></a>CCC</h2><p>opcode &#x3D; 忘了</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">tmp1 = GPR[rs]的前导零<br>tmp2 = GPR[rt]的前导一<br>if (tmp1 &gt; tmp2) GPR[rd] &lt;- GPR[rs][31:16] || GPR[rt][15:0]<br>else GRP[rd] &lt;- GPR[rt][31:16] || GPR[rs][15:0]<br></code></pre></td></tr></table></figure><p><strong>Answer</strong>：在 <code>ALU</code> 把 <code>tmp1</code> 和 <code>tmp2</code> 的值计算出来，并通过 <code>ALUOp</code> 控制 <code>ALU</code> 输出不同的结果。在 <code>CTRL</code> 模块新增指令，使得该指令写入 <code>GPR[rd]</code>，并且写入数据为 <code>ALU</code> 输出结果。在 <code>Stall</code> 模块中 <code>Tnew</code> 和 <code>Tuse</code> 无脑跟 <code>add</code> 保持一致即可。</p><h2 id="BLONEZ-应该吧"><a href="#BLONEZ-应该吧" class="headerlink" title="BLONEZ?(应该吧)"></a>BLONEZ?(应该吧)</h2><p>opcode &#x3D; 111111</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">tmp = GPR[rs]前导1<br>GPR[31] &lt;- PC + 8;<br>if (tmp1 &gt; GPT[rt][5:0]) <br>PC &lt;- IMMbeq的跳转方式<br></code></pre></td></tr></table></figure><p>这个是考场题目，但是比较简单，大家感兴趣可以看一看，我们用一道推荐题来说明这类题目的解决方法。</p><h2 id="BLZTAL"><a href="#BLZTAL" class="headerlink" title="BLZTAL"></a>BLZTAL</h2><p>opcode &#x3D; 100111</p><p>看题：<br><img src="/images/BLZTAL.png" alt="BLZTAL"></p><p><strong>Answer</strong>：对于 <code>b</code> 型指令，我们要在 <code>D_CMP</code> 模块中判断 <code>b_jump</code>，如果是条件链接（像这道题），我们就要将 <code>D_CMP</code> 模块中的 <code>b_jump</code> 指令随着流水线一直流水下去，从而满足每一级的 <code>CTRL</code> 判断 <code>GRFA3sel</code> 和 <code>GRFWDsel</code> 信号的需求；如果是无条件链接（像 <strong>BLONEZ</strong>），那 CTRL 直接生成控制信号写入 <code>31</code> 号寄存器，写入值是 <code>PC + 8</code> 即可。在 <code>STALL</code> 模块 <code>Tuse</code> 和 <code>Tnew</code> 与 <code>Beq</code> 保持一致。</p><p>对于链接类指令 Tuse、Tnew 参照 <code>jal</code> 就行</p><p>特别注意一下 PC 的变化基数是 D_PC 还是 F_PC。</p><p>还有一种情况可能会涉及清空延迟槽的指令，像这样：</p><p><img src="/images/BONALL.png" alt="BONALL"></p><p>为什么要清空延迟槽呢，我理解就是不想执行跳转类指令的下一条指令了，把它用 Flush 清除掉。具体实现需要在 D_CMP 增加一个接口 Flush_check <code>assign Flush_check = (CMPOp == `CMP_BONALL &amp;&amp; !B_jump);</code>，在 <code>mips</code> 中将这一接口与<strong>D_reg_flush**相连，<code>assign D_reg_flush = (!stall &amp;&amp; D_flush_check);</code> 值得注意的是，我们要保证此时流水线不是阻塞状态。如果是阻塞状态我们就等到它阻塞结束再清空，想想为什么呢？因为如果流水线阻塞在 D 级的话，D 级放的其实是跳转类指令，相当于是</strong>把自己送走了**，后面的流水级就丢失了这条指令。</p><h2 id="LBGET"><a href="#LBGET" class="headerlink" title="LBGET"></a>LBGET</h2><p>opcode &#x3D; 110101</p><p><img src="/images/LBGET.png" alt="LBGET"></p><p><strong>Answer</strong>：这是写入地址可选择类 <code>condition: GPR[rt] &lt;- RD; else: GPR[rs] &lt;- RD</code>，我们只有在 DM 完成计算才能知道要写入哪个寄存器，那么 <code>D</code>、<code>E</code>、<code>(M)</code> 就都<strong>没 GRFA3 的视野</strong>，因此难以判断冲突条件是否成立。此时我们有两个选择：<strong>暴力阻塞</strong>；<strong>条件阻塞</strong>。</p><p><strong>暴力阻塞</strong>：只要 D 级指令要访问 GPR，并且访问地址非 0，并且 Tuse &lt; Tnew，我们就阻塞。对应代码实现</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> E_stall_rs = ((E_lbget ? <span class="hljs-number">1</span> : (E_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> E_stall_rt = ((E_lbget ? <span class="hljs-number">1</span> : (E_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rs = ((M_lbget ? <span class="hljs-number">1</span> : (M_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; M_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rt = ((M_lbget ? <span class="hljs-number">1</span> : (M_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; M_Tnew);<br></code></pre></td></tr></table></figure><p><strong>条件阻塞</strong>：D 级指令要访问 GPR，并且访问地址非 0，并且访问地址和当前级流水线的 <code>rs</code> 或 <code>rt</code> 冲突，并且 <code>Tuse &lt; Tnew</code>，我们阻塞。对应代码实现。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> E_stall_rs = ((E_lbget ? (D_rs == E_rt || D_rs == E_rs) : (E_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> E_stall_rt = ((E_lbget ? (D_rt == E_rs || D_rt == E_rt) : (E_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rs = ((M_lbget ? (D_rs == M_rs || D_rs == M_rt) : (M_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; M_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rt = ((M_lbget ? (D_rt == M_rs || D_rt == M_rt) : (M_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; M_Tnew);<br></code></pre></td></tr></table></figure><p>以上是两种阻塞方式，暴力阻塞可能会超时，条件阻塞也可能比测评机要求的周期少(可能是跑的太快了😥)。接着还有两种译码位置，<strong>M级译码</strong>和<strong>W级译码</strong></p><p><strong>W 级译码</strong>：对于判断条件 Condition，我们不在 DM 进行计算，而是在顶层模块 mips 中进行判断，DM 只需要更改 DMOp 从而输出判断需要的数据即可。这里你可能会发现，如果我要写入的数据来自 DM 并且和我判断需要的数据冲突了怎么办呢，那就只好再给 DM 开一个端口往后传判断需要的数据了。在 W 级判断出 condition 后传到 <code>w_ctrl</code>，对 <code>W_GRFA3</code> 判断。显然，在这种情况下，<code>D</code>、<code>E</code>、<code>M</code> 级都没有 GRFA3 的视野，所以 <code>E_Tnew = 3</code>、<code>M_Tnew = 2</code>，并且条件阻塞写法如下：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-comment">//Stall</span><br><span class="hljs-keyword">assign</span> E_stall_rs = ((E_lbget ? (D_rs == E_rt || D_rs == E_rs) : (E_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> E_stall_rt = ((E_lbget ? (D_rt == E_rs || D_rt == E_rt) : (E_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rs = ((M_lbget ? (D_rs == M_rs || D_rs == M_rt) : (M_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; M_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rt = ((M_lbget ? (D_rt == M_rs || D_rt == M_rt) : (M_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; M_Tnew);<br></code></pre></td></tr></table></figure><p>这样就可以得到正确的运算结果，但是不难看出，这并不是最优解，我们在M级其实就可以拿到GRFA3，这样就可以删除M级冲突判断的不确定性，从而减少阻塞的发生，提高CPU性能。</p><p>至于为什么会提到这种在W级译码的方式，21级的很多学长似乎采用了这种做法，优点是<strong>当condition判断需要的数据和DM输出的数据相同时，可以对电路做最小的改动实现功能</strong>，缺点也很明显<strong>运行效率较低并且当condition判断需要的数据和DM输出的数据不相同时需要新增接口</strong>。对了，笔者在测评机提交这种方法时出现了一个测试点TLE，所以这种方法大家听个思路，考场慎用吧。</p><p><strong>M级译码</strong>：对于判断条件condition，我们在DM计算出并且随流水线流到W级，这样M级流水线寄存器就可以较早地拿到GRFA3。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> M_GRFa3 = (M_GRFA3_sel == `GRFA3rt) ? M_rt :<br>                 (M_GRFA3_sel == `GRFA3rd) ? M_rd :<br>                 (M_GRFA3_sel == `GRFA331 ) ? <span class="hljs-number">5&#x27;d31</span> :<br>                 (M_GRFA3_sel == `GRFA3rs) ? M_rs :<br>                 (M_GRFA3_sel == `GRFA30 &amp;&amp; M_lbget &amp;&amp; M_condition1 == <span class="hljs-number">1</span>) ? M_rt :<br>                 (M_GRFA3_sel == `GRFA30 &amp;&amp; M_lbget &amp;&amp; M_condition1 == <span class="hljs-number">0</span>) ? M_rs :<br>                 <span class="hljs-number">5&#x27;d0</span>;<br></code></pre></td></tr></table></figure><p>在Stall模块中因为要判断M_GRFA3，所以需要将condition信号传入Stall</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> STALL (<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] D_Instr,  <br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] E_Instr,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] M_Instr,<br>    <span class="hljs-keyword">input</span> Condition1,<br>    <span class="hljs-keyword">output</span> Stall<br>);<br></code></pre></td></tr></table></figure><p><code>M_GRFA3</code>已知，所以<code>E_Tnew = 2</code> ,<code>M_Tnew = 1</code>,并且条件阻塞判断如下:</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-comment">//Stall</span><br><span class="hljs-keyword">assign</span> E_stall_rs = ((E_lbget ? (D_rs == E_rt || D_rs == E_rs) : (E_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> E_stall_rt = ((E_lbget ? (D_rt == E_rs || D_rt == E_rt) : (E_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rs = ((M_GRFA3 == D_rs) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; M_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rt = ((M_GRFA3 == D_rt) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; M_Tnew);<br></code></pre></td></tr></table></figure><p>注意，关于GRFA3的译码，如果按照笔者个人的习惯，会将condition传入CTRL模块完成GRFA3_Sel译码,但是我们发现在M级，</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog">m_ctrl(<span class="hljs-variable">.Condition</span>(condition),<span class="hljs-variable">.DMOp</span>(DMop)) ;<br>m_dm(<span class="hljs-variable">.DMOp</span>(DMop),<span class="hljs-variable">.Condition</span>(condition));<br></code></pre></td></tr></table></figure><p>ctrl和dm在交互，这就导致了输出信号会是<code>XXXXXXXX</code>,所以我们选择在外部译码。虽然我也不知道这么干为什么不行。。。</p><h2 id="CCS"><a href="#CCS" class="headerlink" title="CCS"></a>CCS</h2><p>这个是考试题</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">high_ones = memword[31:16] 1 的个数<br>low_ones = memword[15:0] 1 的个数<br>if high_ones &gt; low_ones &#123;<br>    memword ^ GPR[rt] -&gt; GPR[31]<br>&#125;<br>else &#123;<br>    memword -&gt; GPR[high_ones]<br>&#125;<br></code></pre></td></tr></table></figure><p>这道题目和上一题的区别在于GPR[high_ones]直到M级才知道，因此冲突判断直接无脑置1即可</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> E_stall_rs = ((E_lbget ? <span class="hljs-number">1</span> : (E_GRFA3 == D_rs)) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> E_stall_rt = ((E_lbget ? <span class="hljs-number">1</span> : (E_GRFA3 == D_rt)) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; E_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rs = ((M_GRFA3 == D_rs) &amp;&amp; D_rs != <span class="hljs-number">0</span>) &amp;&amp; (rs_Tuse &lt; M_Tnew);<br><span class="hljs-keyword">assign</span> M_stall_rt = ((M_GRFA3 == D_rt) &amp;&amp; D_rt != <span class="hljs-number">0</span>) &amp;&amp; (rt_Tuse &lt; M_Tnew);<br></code></pre></td></tr></table></figure><p>剩下的操作跟LBGET一样，大家依葫芦画瓢应该问题不大。</p><h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>笔者的个人仓库里有P5_pro,实现了<code>BLZTAL</code>,<code>LBGET</code>,<code>BONALL</code>,大家如果真的对自己的bug毫无想法可以参考一下哦😋。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">你是我的眼<br>带我领略四季的变换<br>你是我的眼<br>带我穿越拥挤的人潮<br>你是我的眼<br>带我阅读浩瀚的书海<br>因为你是我的眼<br>让我看见<br>这世界<br>就在我眼前<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> verilog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P4课上</title>
      <link href="/2026/01/21/P4%E8%AF%BE%E4%B8%8A/"/>
      <url>/2026/01/21/P4%E8%AF%BE%E4%B8%8A/</url>
      
        <content type="html"><![CDATA[<p><strong>写在前面</strong>：笔者不是大佬，P4 二战方过，这里记载了二战题目的 comprehensive 版本。</p><ul><li>为什么是 comprehensive 版本？因为做完挺早，等助教问答的时候顺手把题抄了。</li><li>为什么没有一战题目？因为一战没心情回忆痛苦的过去。</li><li>为什么二战如此神速？我也不知道，可能是题简单吧。</li></ul><h2 id="SSZE"><a href="#SSZE" class="headerlink" title="SSZE"></a>SSZE</h2><p>opcode &#x3D; 6’b000000<br>func &#x3D; 6’b001111</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">当GPR[rs]和GPR[rt]后缀 0 的个数相同时，将GPR[rd]置 1 ，否则置 0 ;<br></code></pre></td></tr></table></figure><p><strong>Answer</strong>：重点在数 0 的个数，还有就是改指令的时候记得把通路改完整（这个以大家对自己 verilog 代码的理解都不成问题）。</p><h2 id="CABC"><a href="#CABC" class="headerlink" title="CABC"></a>CABC</h2><p>opcode &#x3D; 6’b111100</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">cross1 = GPR[rs][23:16] || GPR[rs][7:0] || GPR[rt][31:24] || GPR[rt][15:8]<br>cross2 = GPR[rt][7:0] || GPR[rt][23:16] || GPR[rs][15:8] || GPR[rs][31:24]<br>xor_result = cross1 ^ cross2<br>if (count_zeros(xor_result[31:16]) == count_zeros(xor_result[15:0])) &#123;<br>    PC &lt;-- IMM_beq<br>&#125;<br>else&#123;<br>    GPR[rt] &lt;-- xor_result<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>Answer</strong>：这和第一题几乎一样，唯一需要注意的是 <code>if</code> 判断条件成立与否，寄存器堆设计的是<code>是否操作</code>，因此在 CTRL 模块中的 WE（寄存器写使能信号）是 <code>(caba &amp;&amp; isequal == 0)</code>。isequal 是 ALU 输出的 <code>count_zeros(xor_result[31:16]) == count_zeros(xor_result[15:0])</code> 判断结果。</p><h2 id="LHBOC"><a href="#LHBOC" class="headerlink" title="LHBOC"></a>LHBOC</h2><p>opcode &#x3D; 101101</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">vaddr &lt;-- GPR[base] + sign_extend(offset)<br>paddr &lt;-- vaddr[31:2] || 2&#x27;b0<br>memword &lt;-- Memory[paddr]<br>tmp1 &lt;-- memword[15] + memword[14] + ...... + memword[0]<br>tmp2 &lt;-- memword[31] + memword[30] + ...... + memword[16]<br>if (tmp1 &gt; tmp2) &#123;<br>    GPR[rt] &lt;-- sign_extend(memword[15:0])<br>&#125;<br>else&#123;<br>    GPR[rt] &lt;-- sign_extend(memword[31:16])<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>Answer</strong>：这个题也挺简单的，注意一下不要被 RTL 语言<strong>骗了</strong>，我们看到这行<code>memword &lt;-- Memory[paddr]</code>，paddr 是 32 位，而 DM 的范围是 <code>0-3072</code>，只需要 12 位即可，我们在 verilog 实现的过程中要像这样</p><blockquote><p>memword &#x3D; DM[addr[13:2]];</p></blockquote><p>addr是ALU的输出结果。</p><h2 id="一些Tips"><a href="#一些Tips" class="headerlink" title="一些Tips"></a>一些Tips</h2><ol><li>课下最好实现这个函数 ——- <strong>sign_extend</strong></li><li>一定要记得有符号数怎么比大小，要不然就上去<code>if</code>手搓吧：<code>$signed(A) &gt; $signed(B)</code></li><li>循环移位别写<code>for</code>循环,会<code>TLE</code>,用这个<code>(A &gt;&gt; n) + (A &lt;&lt; (32 - n))</code></li><li>写函数的时候记得在<code>function</code>后面有<code>begin，end</code>把函数体包起来。</li><li>祝大家P4一战顺利😉😊</li></ol><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">倘若那天<br>把该说的话好好说<br>该体谅的不执著<br>如果那天我<br>不受情绪挑拨<br>你会怎么做<br>那么多如果，可能如果我<br>可惜没如果，只剩下结果<br></code></pre></td></tr></table></figure><h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>谨以此篇，纪念我的第一篇 <code>GitHub</code> 博客。</p>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> verilog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P4课下</title>
      <link href="/2026/01/21/P4%E8%AF%BE%E4%B8%8B/"/>
      <url>/2026/01/21/P4%E8%AF%BE%E4%B8%8B/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>从 P4 开始，我们将开始搭建单周期 CPU，使用的编程语言仅仅是 Verilog，并且对于 Verilog 的要求仅限于语法，没有烦人的状态机。所以被 P1 搞红温的同学可以长舒一口气了😋。</p><h2 id="设计方案综述"><a href="#设计方案综述" class="headerlink" title="设计方案综述"></a>设计方案综述</h2><p>本次实验实现的指令包括 add、sub、ori、lw、sw、beq、lui、sll、j、jal、jr、jalr，通过将各个元件模块化封装，再用<code>wire</code>连接，进而封装成完整的 CPU。</p><p>整体架构图如下：<br><img src="/images/v%E6%95%B4%E4%BD%93%E6%9E%B6%E6%9E%84.png" alt="整体架构图"></p><p>下面将分模块详细介绍一下设计思路和实现方法。</p><h2 id="关键模块实现"><a href="#关键模块实现" class="headerlink" title="关键模块实现"></a>关键模块实现</h2><h3 id="datapath"><a href="#datapath" class="headerlink" title="datapath"></a>datapath</h3><p>这一部分是除了 CTRL 之外的元件集合，相当于顶层模块 MIPS 的两个子分支之一。其实理论上可以将 DM、ALU、CTRL 等元件直接揉到一起写成 mips.v，但是笔者认为像这样分开来写思路更加清晰条理。</p><p>datapath模块的顶层是将ifu，am，alu等组件封装起来，形成一个对CTRL的接口。代码如下：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> DATAPATH (<br>    <span class="hljs-keyword">input</span> clk,<br>    <span class="hljs-keyword">input</span> reset,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] NPCOp,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] WDSel,<br>    <span class="hljs-keyword">input</span> WESel,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] WRA3Sel,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] ALUOp,<br>    <span class="hljs-keyword">input</span> BSel,<br>    <span class="hljs-keyword">input</span> EXTOp,<br>    <span class="hljs-keyword">input</span> DMWr,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">5</span>:<span class="hljs-number">0</span>] opcode,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">5</span>:<span class="hljs-number">0</span>] func<br>);<br><br>  <span class="hljs-comment">// 首先对模块实例化，其次要定义连线，</span><br>  <span class="hljs-comment">// 这里定义的wire是真的wire</span><br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] ifu_pc;<br>  <span class="hljs-keyword">wire</span> ALU_zero;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] instr;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] IFU_npc;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] NPC_pc_4;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] grf_A3;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] grf_WD;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] grf_RD1;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] grf_RD2;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] ALU_C;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] ext_imm;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] ALU_B;<br>  <span class="hljs-keyword">wire</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] dm_rd;<br></code></pre></td></tr></table></figure><p>这里以IFU模块为例说明模块实例化如何实现，请看代码：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog">IFU ifu (<br>    <span class="hljs-variable">.NPC</span>(IFU_npc),<br>    <span class="hljs-variable">.PC</span>(ifu_pc),<br>    <span class="hljs-variable">.instruct</span>(instr),<br>    <span class="hljs-variable">.clk</span>(clk),  <span class="hljs-comment">//out</span><br>    <span class="hljs-variable">.reset</span>(reset)  <span class="hljs-comment">//out</span><br>);<br></code></pre></td></tr></table></figure><p>其中 IFU_npc 代表 NPC 模块的输出 NPC，也是 IFU 的输入。理论上讲这种格式应该是<strong>括号外</strong>是子模块的端口而<strong>括号内</strong>是父模块的信号。为了方便理解可以把它直观看成一根线，一根连接两个端口的线。</p><h4 id="其他子模块"><a href="#其他子模块" class="headerlink" title="其他子模块"></a>其他子模块</h4><p>剩余子模块相对独立，正常翻译logisim代码即可。</p><p>特别说明，为了方便表示电路中的所有连线，新增MUX模块，端口定义如下：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">module</span> MUX (<br>    <span class="hljs-keyword">input</span>  [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] WRA3MUX1,<br>    <span class="hljs-keyword">input</span>  [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] WRA3MUX2,<br>    <span class="hljs-keyword">input</span>  [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] WRA3MUX3,<br>    <span class="hljs-keyword">input</span>  [<span class="hljs-number">2</span>:<span class="hljs-number">0</span>] WRA3Sel,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">4</span>:<span class="hljs-number">0</span>] A3,<br><br>    <span class="hljs-keyword">input</span>  [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] WDMUX1,<br>    <span class="hljs-keyword">input</span>  [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] WDMUX2,<br>    <span class="hljs-keyword">input</span>  [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] WDMUX3,<br>    <span class="hljs-keyword">input</span>  [ <span class="hljs-number">2</span>:<span class="hljs-number">0</span>] WDSel,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] WD,<br><br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] BMUX1,<br>    <span class="hljs-keyword">input</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] BMUX2,<br>    <span class="hljs-keyword">input</span> BSel,<br>    <span class="hljs-keyword">output</span> [<span class="hljs-number">31</span>:<span class="hljs-number">0</span>] ALU_B<br>);<br></code></pre></td></tr></table></figure><h3 id="CTRL"><a href="#CTRL" class="headerlink" title="CTRL"></a>CTRL</h3><p>ctrl 要实现和datapath对接的接口，内部按照logisim设计时的与或关系来实现即可。</p><h3 id="mips"><a href="#mips" class="headerlink" title="mips"></a>mips</h3><p>MIPS 作为顶层电路，实现的是<code>CTRL</code>模块和<code>DATAPATH</code>模块的拼接</p><h3 id="Some-Tips"><a href="#Some-Tips" class="headerlink" title="Some Tips"></a>Some Tips</h3><ul><li>关于连线：在 datapath 模块的线路拼接中，由于一根线的两端连接的元件一般不同，所以建议在给 wire 命名时加上相连的某个元件，在写代码时便于识别信号通路。例如<code>wire ALU_zero;</code></li></ul><p>但是在mips中的线路拼接时，由于只有两个元件，因此所有接口都是一一对应关系。子模块端口和父模块信号可以一样，不会混淆。例如</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">wire</span> [<span class="hljs-number">5</span>:<span class="hljs-number">0</span>] func; <br><br>DATAPATH datapath(<br>   <span class="hljs-comment">//.....</span><br>   <span class="hljs-variable">.func</span>(func)     <span class="hljs-comment">//out</span><br>);<br><br>CTRL ctrl(<br>   <span class="hljs-comment">//.....</span><br>   <span class="hljs-variable">.func</span>(func)     <span class="hljs-comment">//in </span><br>);<br><br></code></pre></td></tr></table></figure><ul><li>关于命名：个人习惯模块名称大写，实例化时的名字小写，例如 <code>DATAPATH datapath</code>。</li></ul><h3 id="Some-Bugs"><a href="#Some-Bugs" class="headerlink" title="Some Bugs"></a>Some Bugs</h3><ol><li>IFU模块中的指令文档命名 <code>code.txt</code></li><li>NPC模块中传入的<code>zero</code>一定仔细考虑好<code>BEQ</code>指令下<code>zero==0</code>和<code>zero!=0</code>的输出分别是什么，千万别搞混</li><li>CTRL模块中的<code>JR</code>和<code>JALR</code>指令有<code>opcode==SPECIAL</code> (一定要记得),并且是<code>opcode</code>不是<code>func</code>。😭</li><li>注意一下在对指令切片时放大logisim仔细瞅瞅，要不然就要硬搓Isim仿真了</li></ol><h2 id="Verilog仿真指南"><a href="#Verilog仿真指南" class="headerlink" title="Verilog仿真指南"></a>Verilog仿真指南</h2><p>verilog的testbench还是较为友好的，只需要开始时reset&#x3D;1，#100后reset &#x3D; 0。</p><p>想要监测指令名称时看波形图<br>想要监测寄存器中的数据时，在 Isim 界面左侧选择 Memory 选项，如图：</p><p><img src="/images/register%E6%9F%A5%E7%9C%8B%E7%A4%BA%E4%BE%8B.png" alt="register查看示例"></p><h2 id="思考题"><a href="#思考题" class="headerlink" title="思考题"></a>思考题</h2><ul><li>Q:这个 addr 信号又是从哪里来的？地址信号 addr 位数为什么是 [11:2] 而不是 [9:0] ?</li></ul><p>A:  </p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">addr由ALU计算得到。一条地址占32位2进制，也就是4个字节，截取 [11:2] 相当于将地址右移四位，将地址转换位第n条指令，从而便于在RAM读写。<br></code></pre></td></tr></table></figure><ul><li>Q:<br>思考上述两种控制器设计的译码方式，给出代码示例，并尝试对比各方式的优劣。</li></ul><p>A:</p><p>指令对应的控制信号如何取值:</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">always</span> @(*) <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">if</span> (add == <span class="hljs-number">1</span>) <span class="hljs-keyword">begin</span><br>        WESel = <span class="hljs-number">1</span>;<br>        WRA3Sel[<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">end</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>控制信号每种取值对应的指令：</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">assign</span> NPCOp[<span class="hljs-number">0</span>] = beq | jalr | jr;<br><span class="hljs-keyword">assign</span> NPCOp[<span class="hljs-number">1</span>] = j | jal | jalr | jr;<br><span class="hljs-keyword">assign</span> NPCOp[<span class="hljs-number">2</span>] = <span class="hljs-number">0</span>;<br></code></pre></td></tr></table></figure><p>优劣对比：</p><ul><li>Q:复位信号的设计都是同步复位，这与 P3 中的设计要求不同。请对比同步复位与异步复位这两种方式的 reset 信号与 clk 信号优先级的关系。</li></ul><p>A :</p><p>异步复位：reset信号优先级大于clk</p><p>同步复位：reset信号优先级小于clk</p><ul><li>Q:C 语言是一种弱类型程序设计语言。C 语言中不对计算结果溢出进行处理，这意味着 C 语言要求程序员必须很清楚计算结果是否会导致溢出。因此，如果仅仅支持 C 语言，MIPS 指令的所有计算指令均可以忽略溢出。 请说明为什么在忽略溢出的前提下，addi 与 addiu 是等价的，add 与 addu 是等价的。提示：阅读《MIPS32® Architecture For Programmers Volume II: The MIPS32® Instruction Set》中相关指令的 Operation 部分（详见文档 page 34、page 35）。</li></ul><p>A:</p><p>在MIPS英文指令集中，对于add指令的定义是 <code>ADDU performs the same arithmetic operation but does not trap on overflow.</code>因此addu和add在忽略溢出的前提下是等价的。</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">等不到天黑<br>烟火不会太完美<br>回忆烧成灰<br>还是等不到结尾<br>他层说的无所谓<br>我怕一天一天被摧毁<br>等不到天黑<br>不敢凋谢的花蕾<br>雨也在跟随<br>放开刺痛的滋味<br>今后不再怕天明<br>我想只是害怕清醒<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> verilog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P3课下</title>
      <link href="/2026/01/21/P3%E8%AF%BE%E4%B8%8B/"/>
      <url>/2026/01/21/P3%E8%AF%BE%E4%B8%8B/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>从 P3 开始，我们正式开始设计 CPU，首先从单周期开始入手，P3 使用 Logisim，并且不会涉及特别复杂的<strong>状态机</strong>，指令也相对容易。</p><h2 id="设计方案综述"><a href="#设计方案综述" class="headerlink" title="设计方案综述"></a>设计方案综述</h2><p>要求实现的指令集包括 add（不考虑溢出）、subu（不考虑溢出）、ori、lw、sw、beq、lui、nop、j、jal、jr、jalr</p><p>整体架构图如下：<br><img src="/images/%E6%95%B4%E4%BD%93%E7%BB%93%E6%9E%84.png" alt="整体结构"></p><p>参考了课件中的缩略简图：<br><img src="/images/%E7%BC%A9%E7%95%A5%E5%9B%BE.png" alt="缩略图"></p><h2 id="关键模块的定义"><a href="#关键模块的定义" class="headerlink" title="关键模块的定义"></a>关键模块的定义</h2><h3 id="IFU：由-PC（程序计数器）和-IM-组成"><a href="#IFU：由-PC（程序计数器）和-IM-组成" class="headerlink" title="IFU：由 PC（程序计数器）和 IM 组成"></a>IFU：由 PC（程序计数器）和 IM 组成</h3><h4 id="端口说明："><a href="#端口说明：" class="headerlink" title="端口说明："></a>端口说明：</h4><table><thead><tr><th>信号名称</th><th>方向</th><th>功能描述</th></tr></thead><tbody><tr><td>NPC[31:0]</td><td>I</td><td>输入NPC</td></tr><tr><td>clk</td><td>I</td><td>时钟信号</td></tr><tr><td>reset</td><td>I</td><td>复位信号</td></tr><tr><td>instruct[31:0]</td><td>O</td><td>输出当前指令</td></tr><tr><td>PC[31:0]</td><td>O</td><td>输出当前指令地址</td></tr></tbody></table><h4 id="元件示意图"><a href="#元件示意图" class="headerlink" title="元件示意图"></a>元件示意图</h4><p><img src="/images/IFU%E5%85%83%E4%BB%B6%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="IFU元件示意图"></p><p><strong>Tips</strong>：尽量将 PC 和 IM 分开写，方便调试时观测 RAM 指令执行的变化</p><h3 id="NPC-次地址计算单元"><a href="#NPC-次地址计算单元" class="headerlink" title="NPC(次地址计算单元)"></a>NPC(次地址计算单元)</h3><h4 id="端口说明"><a href="#端口说明" class="headerlink" title="端口说明"></a>端口说明</h4><table><thead><tr><th>信号名称</th><th>方向</th><th>功能描述</th></tr></thead><tbody><tr><td>ra</td><td>I</td><td>rs寄存器中存储的数据</td></tr><tr><td>PC[31:0]</td><td>I</td><td>当前正在执行的指令地址</td></tr><tr><td>zero</td><td>I</td><td><code>$rs</code> 和 <code>$rt</code>是否相等</td></tr><tr><td>npcOp</td><td>I</td><td>控制信号</td></tr><tr><td>imm[25:0]</td><td>I</td><td>用于拼接出NPC</td></tr><tr><td>npc[31:0]</td><td>O</td><td>次地址</td></tr><tr><td>PC+4[31:0]</td><td>O</td><td>当前正在执行的指令地址+4</td></tr></tbody></table><h4 id="控制信号说明"><a href="#控制信号说明" class="headerlink" title="控制信号说明"></a>控制信号说明</h4><ol><li><strong>npcOp</strong></li></ol><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>3’b000</td><td>PC+4作为次地址</td></tr><tr><td>3’b001</td><td>beq</td></tr><tr><td>3’b010</td><td>j,jal</td></tr><tr><td>3’b011</td><td>jr,jalr</td></tr></tbody></table><h4 id="元件示意图-1"><a href="#元件示意图-1" class="headerlink" title="元件示意图"></a>元件示意图</h4><p><img src="/images/NPC%E5%85%83%E4%BB%B6%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="NPC元件示意图"></p><h3 id="GRF-寄存器堆"><a href="#GRF-寄存器堆" class="headerlink" title="GRF(寄存器堆)"></a>GRF(寄存器堆)</h3><h4 id="端口说明：-1"><a href="#端口说明：-1" class="headerlink" title="端口说明："></a>端口说明：</h4><table><thead><tr><th>信号名称</th><th>方向</th><th>功能描述</th></tr></thead><tbody><tr><td>A1[4:0]</td><td>I</td><td>5位地址输入信号，将其储存的数据读出到 RD1</td></tr><tr><td>A2[4:0]</td><td>I</td><td>5位地址输入信号，将其储存的数据读出到 RD2</td></tr><tr><td>A3[4:0]</td><td>I</td><td>5位地址输入信号，将其作为写入数据的目标寄存器</td></tr><tr><td>WD[31:0]</td><td>I</td><td>32位数据输入信号，将该数据写入目标寄存器A3</td></tr><tr><td>WE</td><td>I</td><td>写使能</td></tr><tr><td>clk</td><td>I</td><td>时钟信号</td></tr><tr><td>reset</td><td>I</td><td>复位信号</td></tr><tr><td>RD1[31:0]</td><td>O</td><td>输出A1指定寄存器中的数据</td></tr><tr><td>RD2[31:0]</td><td>O</td><td>输出A2指定寄存器中的数据</td></tr></tbody></table><h4 id="控制信号说明："><a href="#控制信号说明：" class="headerlink" title="控制信号说明："></a>控制信号说明：</h4><ol><li><strong>weSel</strong></li></ol><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>1’b0</td><td>不可写入</td></tr><tr><td>1’b1</td><td>可写入</td></tr></tbody></table><ol start="2"><li><strong>wrA3Sel[2:0]</strong></li></ol><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>3’b000</td><td>rt作为写入数据的目标寄存器</td></tr><tr><td>3’b001</td><td>rd作为写入数据的目标寄存器</td></tr><tr><td>3’b010</td><td>$31作为写入数据的目标寄存器</td></tr></tbody></table><ol start="3"><li><strong>WDSel[2:0]</strong></li></ol><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>3’b000</td><td>写入寄存器的数据来自ALU的运算结果</td></tr><tr><td>3’b001</td><td>写入寄存器的数据来自DM</td></tr><tr><td>3’b010</td><td>写入寄存器的数据是PC+4</td></tr></tbody></table><h4 id="元件示意图："><a href="#元件示意图：" class="headerlink" title="元件示意图："></a>元件示意图：</h4><p><img src="/images/GRF%E5%85%83%E4%BB%B6%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="GRF元件示意图"></p><h3 id="ALU-算数逻辑单元"><a href="#ALU-算数逻辑单元" class="headerlink" title="ALU(算数逻辑单元)"></a>ALU(算数逻辑单元)</h3><h4 id="端口说明-1"><a href="#端口说明-1" class="headerlink" title="端口说明"></a>端口说明</h4><table><thead><tr><th>信号名称</th><th>方向</th><th>功能描述</th></tr></thead><tbody><tr><td>A[31:0]</td><td>I</td><td>运算数A</td></tr><tr><td>B[31:0]</td><td>I</td><td>运算数B</td></tr><tr><td>shamt[4:0]</td><td>I</td><td>sll的位数</td></tr><tr><td>ALUOp[2:0]</td><td>I</td><td>控制信号</td></tr><tr><td>C[31:0]</td><td>O</td><td>运算输出结果</td></tr><tr><td>zero</td><td>O</td><td>A和B是否相等</td></tr></tbody></table><h4 id="控制信号说明-1"><a href="#控制信号说明-1" class="headerlink" title="控制信号说明"></a>控制信号说明</h4><ol><li><strong>ALUOp[2:0]</strong></li></ol><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>3’b000</td><td>add</td></tr><tr><td>3’b001</td><td>sub</td></tr><tr><td>3’b010</td><td>ori</td></tr><tr><td>3’b011</td><td>lui</td></tr><tr><td>3’b100</td><td>sll</td></tr></tbody></table><ol start="2"><li><strong>BSel</strong></li></ol><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>1’b0</td><td>B的值为<code>$rt</code></td></tr><tr><td>1’b1</td><td>B的值为imm</td></tr></tbody></table><h4 id="元件示意图-2"><a href="#元件示意图-2" class="headerlink" title="元件示意图"></a>元件示意图</h4><p><img src="/images/ALU%E5%85%83%E4%BB%B6%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="ALU元件示意图"></p><h3 id="DM-数据存储器"><a href="#DM-数据存储器" class="headerlink" title="DM (数据存储器)"></a>DM (数据存储器)</h3><h4 id="端口说明-2"><a href="#端口说明-2" class="headerlink" title="端口说明"></a>端口说明</h4><table><thead><tr><th>信号名称</th><th>方向</th><th>功能描述</th></tr></thead><tbody><tr><td>addr[31:0]</td><td>I</td><td>待操作的内存地址</td></tr><tr><td>WD[31:0]</td><td>I</td><td>待写入的内存值</td></tr><tr><td>WR</td><td>I</td><td>写使能信号</td></tr><tr><td>clk</td><td>I</td><td>时钟信号</td></tr><tr><td>reset</td><td>I</td><td>复位信号</td></tr><tr><td>RD[31:0]</td><td>O</td><td>输入地址指向的内存中存储的值</td></tr></tbody></table><h4 id="控制信号说明-2"><a href="#控制信号说明-2" class="headerlink" title="控制信号说明"></a>控制信号说明</h4><ol><li><strong>WR</strong></li></ol><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>1’b0</td><td>不能写入</td></tr><tr><td>1’b1</td><td>可以写入</td></tr></tbody></table><h4 id="元件示意图-3"><a href="#元件示意图-3" class="headerlink" title="元件示意图"></a>元件示意图</h4><p><img src="/images/DM%E5%85%83%E4%BB%B6%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="DM元件示意图"></p><h3 id="EXT-位扩展"><a href="#EXT-位扩展" class="headerlink" title="EXT(位扩展)"></a>EXT(位扩展)</h3><h4 id="端口说明-3"><a href="#端口说明-3" class="headerlink" title="端口说明"></a>端口说明</h4><table><thead><tr><th>信号名称</th><th>方向</th><th>功能描述</th></tr></thead><tbody><tr><td>imm</td><td>I</td><td>输入待扩展的立即数</td></tr><tr><td>extOp</td><td>I</td><td>控制信号</td></tr><tr><td>extend</td><td>O</td><td>位扩展后的结果</td></tr></tbody></table><h4 id="控制信号说明-3"><a href="#控制信号说明-3" class="headerlink" title="控制信号说明"></a>控制信号说明</h4><table><thead><tr><th>控制信号值</th><th>功能</th></tr></thead><tbody><tr><td>1’b0</td><td>0扩展</td></tr><tr><td>1’b1</td><td>符号扩展</td></tr></tbody></table><h4 id="元件示意图-4"><a href="#元件示意图-4" class="headerlink" title="元件示意图"></a>元件示意图</h4><p><img src="/images/EXT%E5%85%83%E4%BB%B6%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="EXT元件示意图"></p><h3 id="CTRL-控制信号模块"><a href="#CTRL-控制信号模块" class="headerlink" title="CTRL(控制信号模块)"></a>CTRL(控制信号模块)</h3><h4 id="端口说明-4"><a href="#端口说明-4" class="headerlink" title="端口说明"></a>端口说明</h4><table><thead><tr><th>信号名称</th><th>方向</th><th>功能描述</th></tr></thead><tbody><tr><td>OPcode[5:0]</td><td>I</td><td>输入标识信号</td></tr><tr><td>FuncCode[5:0]</td><td>I</td><td>输入标识信号</td></tr><tr><td>NPCOp[2:0]</td><td>O</td><td>npc计算方式</td></tr><tr><td>WDSel[2:0]</td><td>O</td><td>GRF存的数据</td></tr><tr><td>WeSel</td><td>O</td><td>GRF写使能</td></tr><tr><td>WRA3Sel[2:0]</td><td>O</td><td>GRF写入地址</td></tr><tr><td>ALUOp[2:0]</td><td>O</td><td>ALU模式</td></tr><tr><td>Bsel</td><td>O</td><td>B的数据类型</td></tr><tr><td>extOp</td><td>O</td><td>有&#x2F;无符号扩展</td></tr><tr><td>DMWR</td><td>O</td><td>DM写使能</td></tr></tbody></table><h4 id="元件示意图-5"><a href="#元件示意图-5" class="headerlink" title="元件示意图"></a>元件示意图</h4><p><img src="/images/CTRL%E5%85%83%E4%BB%B6%E7%A4%BA%E6%84%8F%E5%9B%BE.png" alt="CTRL元件示意图"></p><h2 id="RTL指令"><a href="#RTL指令" class="headerlink" title="RTL指令"></a>RTL指令</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">1. add: GPR[rd] &lt;- GPR[rs]+GPR[rt]<br>2. sub: GPR[rd] &lt;- GPR[rs] - GPR[rt]<br>3. ori: GPR[rt] &lt;- GPR[rs] OR immediate<br>4. lw: GPR[rt] &lt;- memory[GPR[base]+offset]<br>5. sw: memory[GPR[base]+offset] &lt;- GPR[rt]<br>6. beq: if (GPR[rs] == GPR[rt]) then 转移<br>7. lui: GPR[rt] &lt;- immediate&#123;16&#123;0&#125;&#125;<br>8. sll: GPR[rd] &lt;- GPR[rt] &lt;&lt; s<br>9. j : PC &lt;- PC31..28 || instr_index || 00<br>10. jal: PC &lt;- PC31..28 || instr_index || 00<br>    GPR[31] &lt;- PC + 4<br>11. jalr: PC &lt;- GPR[rs]<br>    GPR[rd] &lt;- PC + 4<br>12. jr: PC &lt;- GPR[rs]<br></code></pre></td></tr></table></figure><h2 id="数据通路分析"><a href="#数据通路分析" class="headerlink" title="数据通路分析"></a>数据通路分析</h2><table><thead><tr><th align="center">指令</th><th align="center">opcode</th><th align="center">funct</th><th align="center">npcOp</th><th align="center">WDSel</th><th align="center">weSel</th><th align="center">wrA3Sel</th><th align="center">ALUOp</th><th align="center">BSel</th><th align="center">extOp</th><th align="center">WR</th></tr></thead><tbody><tr><td align="center">功能</td><td align="center">instruct[26:21]</td><td align="center">instruct[5:0]</td><td align="center">npc计算方式</td><td align="center">GRF存的数据</td><td align="center">GRF写使能</td><td align="center">GRF写入地址</td><td align="center">ALU模式</td><td align="center">B的数据类型</td><td align="center">有&#x2F;无符号扩展</td><td align="center">DM写使能</td></tr><tr><td align="center">add</td><td align="center">000000</td><td align="center">100000</td><td align="center">000</td><td align="center">000</td><td align="center">1</td><td align="center">001</td><td align="center">000</td><td align="center">0</td><td align="center">x</td><td align="center">0</td></tr><tr><td align="center">sub</td><td align="center">000000</td><td align="center">100010</td><td align="center">000</td><td align="center">000</td><td align="center">1</td><td align="center">001</td><td align="center">001</td><td align="center">0</td><td align="center">x</td><td align="center">0</td></tr><tr><td align="center">ori</td><td align="center">001101</td><td align="center">x</td><td align="center">000</td><td align="center">000</td><td align="center">1</td><td align="center">000</td><td align="center">010</td><td align="center">1</td><td align="center">0</td><td align="center">0</td></tr><tr><td align="center">lw</td><td align="center">100011</td><td align="center">x</td><td align="center">000</td><td align="center">001</td><td align="center">1</td><td align="center">000</td><td align="center">000</td><td align="center">1</td><td align="center">1</td><td align="center">0</td></tr><tr><td align="center">sw</td><td align="center">101011</td><td align="center">x</td><td align="center">000</td><td align="center">x</td><td align="center">0</td><td align="center">x</td><td align="center">000</td><td align="center">1</td><td align="center">1</td><td align="center">1</td></tr><tr><td align="center">beq</td><td align="center">000100</td><td align="center">x</td><td align="center">001</td><td align="center">x</td><td align="center">0</td><td align="center">x</td><td align="center">001</td><td align="center">0</td><td align="center">x</td><td align="center">0</td></tr><tr><td align="center">lui</td><td align="center">001111</td><td align="center">x</td><td align="center">000</td><td align="center">000</td><td align="center">1</td><td align="center">000</td><td align="center">011</td><td align="center">1</td><td align="center">0</td><td align="center">0</td></tr><tr><td align="center">sll</td><td align="center">000000</td><td align="center">000000</td><td align="center">000</td><td align="center">000</td><td align="center">1</td><td align="center">001</td><td align="center">100</td><td align="center">0</td><td align="center">x</td><td align="center">0</td></tr><tr><td align="center">j</td><td align="center">000010</td><td align="center">x</td><td align="center">010</td><td align="center">x</td><td align="center">0</td><td align="center">x</td><td align="center">x</td><td align="center">x</td><td align="center">x</td><td align="center">0</td></tr><tr><td align="center">jal</td><td align="center">000011</td><td align="center">x</td><td align="center">010</td><td align="center">010</td><td align="center">1</td><td align="center">010</td><td align="center">x</td><td align="center">x</td><td align="center">x</td><td align="center">0</td></tr><tr><td align="center">jalr</td><td align="center">001001</td><td align="center">x</td><td align="center">011</td><td align="center">010</td><td align="center">1</td><td align="center">001</td><td align="center">x</td><td align="center">x</td><td align="center">x</td><td align="center">0</td></tr><tr><td align="center">jr</td><td align="center">001000</td><td align="center">x</td><td align="center">011</td><td align="center">x</td><td align="center">0</td><td align="center">x</td><td align="center">x</td><td align="center">x</td><td align="center">x</td><td align="center">0</td></tr></tbody></table><p><strong>PS</strong>:</p><ol><li>在课下设计时尽可能将控制信号设为<strong>3位</strong>或<strong>2位</strong>，这样在课上考试时就不需要使用实验室逆天鼠标来完成这一部分工作了。</li><li>课下设计尽可能留足扩展空间，不要画的太密，否则考场红温时无疑是雪上加霜😨。</li></ol><h2 id="测试方案"><a href="#测试方案" class="headerlink" title="测试方案"></a>测试方案</h2><h3 id="测试ori指令"><a href="#测试ori指令" class="headerlink" title="测试ori指令"></a>测试ori指令</h3><figure class="highlight mips"><table><tr><td class="code"><pre><code class="hljs MIPS"><span class="hljs-keyword">ori </span>$<span class="hljs-number">0</span>, $<span class="hljs-number">0</span>, <span class="hljs-number">0</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">1</span>, $<span class="hljs-number">1</span>, <span class="hljs-number">1</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">2</span>, $<span class="hljs-number">2</span>, <span class="hljs-number">2</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">3</span>, $<span class="hljs-number">3</span>, <span class="hljs-number">3</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">4</span>, $<span class="hljs-number">4</span>, <span class="hljs-number">4</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">5</span>, $<span class="hljs-number">5</span>, <span class="hljs-number">5</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">6</span>, $<span class="hljs-number">6</span>, <span class="hljs-number">6</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">7</span>, $<span class="hljs-number">7</span>, <span class="hljs-number">7</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">8</span>, $<span class="hljs-number">8</span>, <span class="hljs-number">8</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">9</span>, $<span class="hljs-number">9</span>, <span class="hljs-number">9</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">10</span>, $<span class="hljs-number">10</span>, <span class="hljs-number">10</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">11</span>, $<span class="hljs-number">11</span>, <span class="hljs-number">11</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">12</span>, $<span class="hljs-number">12</span>, <span class="hljs-number">12</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">13</span>, $<span class="hljs-number">13</span>, <span class="hljs-number">13</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">14</span>, $<span class="hljs-number">14</span>, <span class="hljs-number">14</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">15</span>, $<span class="hljs-number">15</span>, <span class="hljs-number">15</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">16</span>, $<span class="hljs-number">16</span>, <span class="hljs-number">16</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">17</span>, $<span class="hljs-number">17</span>, <span class="hljs-number">17</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">18</span>, $<span class="hljs-number">18</span>, <span class="hljs-number">18</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">19</span>, $<span class="hljs-number">19</span>, <span class="hljs-number">19</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">20</span>, $<span class="hljs-number">20</span>, <span class="hljs-number">20</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">21</span>, $<span class="hljs-number">21</span>, <span class="hljs-number">21</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">22</span>, $<span class="hljs-number">22</span>, <span class="hljs-number">22</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">23</span>, $<span class="hljs-number">23</span>, <span class="hljs-number">23</span><br><span class="hljs-keyword">ori </span>$<span class="hljs-number">24</span>, $<span class="hljs-number">24</span>, <span class="hljs-number">24</span><br></code></pre></td></tr></table></figure><p>在MIPS中运行出结果以后在logisim中的GRF查看</p><h3 id="测试sw指令"><a href="#测试sw指令" class="headerlink" title="测试sw指令"></a>测试sw指令</h3><figure class="highlight mips"><table><tr><td class="code"><pre><code class="hljs MIPS"><span class="hljs-keyword">ori </span>$<span class="hljs-number">1</span>, $<span class="hljs-number">1</span>, <span class="hljs-number">20</span><br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">4</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">8</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">12</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">16</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">20</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">24</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">28</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">32</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">36</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">40</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">44</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">48</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">52</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">56</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">60</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">64</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">68</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">72</span>($<span class="hljs-number">0</span>)<br></code></pre></td></tr></table></figure><ul><li>在logisim中的DM中查看</li><li>偏移量范围是 -2^15 ～ 2^15 - 1</li><li>跟寄存器没关系，涉及的是DM只是用了寄存器中的值</li></ul><h3 id="测试lw"><a href="#测试lw" class="headerlink" title="测试lw"></a>测试lw</h3><figure class="highlight mips"><table><tr><td class="code"><pre><code class="hljs MIPS"><span class="hljs-keyword">ori </span>$<span class="hljs-number">1</span>, $<span class="hljs-number">1</span>, <span class="hljs-number">1</span><br><span class="hljs-keyword">sw </span>$<span class="hljs-number">1</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">2</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">3</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">4</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">5</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">6</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">7</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">8</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">9</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">10</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">11</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">12</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">13</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">14</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">15</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">16</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">17</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">18</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br><span class="hljs-keyword">lw </span>$<span class="hljs-number">19</span>, <span class="hljs-number">0</span>($<span class="hljs-number">0</span>)<br></code></pre></td></tr></table></figure><p>先用之前测出的or，sw，然后再用lw往出导，导出到不同的寄存器里面,在logisim中的GRF查看</p><h3 id="测试lui"><a href="#测试lui" class="headerlink" title="测试lui"></a>测试lui</h3><figure class="highlight mips"><table><tr><td class="code"><pre><code class="hljs MIPS"><span class="hljs-keyword">lui </span>$<span class="hljs-number">0</span>，<span class="hljs-number">0</span><br><span class="hljs-keyword">lui </span>$<span class="hljs-number">1</span>,<span class="hljs-number">1</span><br><span class="hljs-keyword">lui </span>$<span class="hljs-number">2</span>,<span class="hljs-number">2</span><br><span class="hljs-keyword">lui </span>$<span class="hljs-number">3</span>,<span class="hljs-number">3</span><br></code></pre></td></tr></table></figure><p>在logisim中的GRF查看，与MIPS的运行结果做对照</p><h3 id="测试addu，subu，beq，nop"><a href="#测试addu，subu，beq，nop" class="headerlink" title="测试addu，subu，beq，nop"></a>测试addu，subu，beq，nop</h3><figure class="highlight mips"><table><tr><td class="code"><pre><code class="hljs MIPS"><span class="hljs-keyword">ori </span> $<span class="hljs-number">1</span>, $<span class="hljs-number">1</span>, <span class="hljs-number">10</span><br><span class="hljs-keyword">ori </span> $<span class="hljs-number">2</span>, $<span class="hljs-number">2</span>, <span class="hljs-number">20</span><br><span class="hljs-keyword">addu </span>$<span class="hljs-number">3</span>, $<span class="hljs-number">1</span>, $<span class="hljs-number">1</span><br><span class="hljs-keyword">addu </span>$<span class="hljs-number">4</span>, $<span class="hljs-number">1</span>, $<span class="hljs-number">2</span><br><span class="hljs-keyword">subu </span>$<span class="hljs-number">5</span>, $<span class="hljs-number">2</span>, $<span class="hljs-number">1</span><br><span class="hljs-keyword">subu </span>$<span class="hljs-number">6</span>, $<span class="hljs-number">1</span>, $<span class="hljs-number">1</span><br><span class="hljs-keyword">lui </span> $<span class="hljs-number">4</span>, <span class="hljs-number">0xffff</span> <br><span class="hljs-keyword">nop</span><br><span class="hljs-keyword"></span><span class="hljs-keyword">beq </span> $<span class="hljs-number">1</span>, $<span class="hljs-number">0</span>, end<br><span class="hljs-keyword">lui </span> $<span class="hljs-number">1</span>, <span class="hljs-number">0xffff</span><br><span class="hljs-symbol">end:</span><br></code></pre></td></tr></table></figure><p>这里因为addu和add的Opcode不同，sub同理，所以在导出之前需要将addu，subu全部转成add，sub，但是MIPS运行仍然使用addu和subu</p><p>将MIPS的DM导出，与logisim的DM导出信息做对照，如果相同则证明程序正确。</p><p>当然以上只是小规模测试，如需广泛测试需要借助python自动生成MIPS代码，进而通过git diff做比较 <strong>(在大规模测试下其实只需要比较DM即可，无需对GRF进行对拍)</strong> ，或者通过python程序进行比较。</p><h2 id="思考题"><a href="#思考题" class="headerlink" title="思考题"></a>思考题</h2><ul><li>上面我们介绍了通过 FSM 理解单周期 CPU 的基本方法。请大家指出单周期 CPU 所用到的模块中，哪些发挥状态存储功能，哪些发挥状态转移功能。</li></ul><blockquote><p>状态存储：IM GRF DM<br>状态转移： NPC ALU</p></blockquote><ul><li>现在我们的模块中 IM 使用 ROM， DM 使用 RAM， GRF 使用 Register，这种做法合理吗？ 请给出分析，若有改进意见也请一并给出。</li></ul><blockquote><p>IM 只会被读取，其中的值不用修改，使用ROM合理<br>DM 需要读取和写入，可以使用RAM和Register。但是DM要求的内存是3072的空间，使用Register的话可能手绘有一定困难<br>GRF频繁读写指令，Register可以保证读写速度</p></blockquote><ul><li>在上述提示的模块之外，你是否在实际实现时设计了其他的模块？如果是的话，请给出介绍和设计的思路。</li></ul><blockquote><p>实现了sll。sll是将<code>$rt</code>寄存器中的值左移shamt位放到<code>$rd</code>中，因此需要在ALU模块新增shamt输入，在CTRL中实现此时wrA3Sel&#x3D;1，weSel&#x3D;1,BSel &#x3D; 0</p></blockquote><ul><li>事实上，实现 nop 空指令，我们并不需要将它加入控制信号真值表，为什么？</li></ul><blockquote><ol><li>nop指令会被识别成R型指令，<code>$rs,$rt,$rd</code>均为0号寄存器，而0号寄存器的值恒为0，因此不需要特殊处理。</li><li>该CPU设计实现了sll，nop指令相当于 <code>sll $0 $0 0</code>,相当于把$0寄存器中的值左移0位并写入<code>$0</code>寄存器，因为<code>$0</code>的值始终为0，不会被修改，因此该指令执行后没有任何影响。</li></ol></blockquote><ul><li>阅读 Pre 的 “MIPS 指令集及汇编语言” 一节中给出的测试样例，评价其强度（可从各个指令的覆盖情况，单一指令各种行为的覆盖情况等方面分析），并指出具体的不足之处。</li></ul><blockquote><p>lw 和 sw指令没有覆盖offset为负数的情况<br>没有测试nop和sub指令</p></blockquote><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">他字字未提喜欢你<br>你句句都是我愿意<br>他一句寂寞时候的回应<br>你却激动不已<br>他次次回避着话题<br>你傻傻热情地贴近<br>穿过多少城市为了他<br>只因为他<br>淋湿了自己<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> logisim </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P3课上</title>
      <link href="/2026/01/21/P3%E8%AF%BE%E4%B8%8A/"/>
      <url>/2026/01/21/P3%E8%AF%BE%E4%B8%8A/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>P3 课上考试加的指令都比较常规，个人认为 P3 考试最大的难点就是机房的鼠标。我只能劝你手别抖了😨。</p><h2 id="一些前置知识"><a href="#一些前置知识" class="headerlink" title="一些前置知识"></a>一些前置知识</h2><p>开始 P3 之后，你所了解的用于 P0 的元件已经不够用了，下面列举几个新的 P3 课上常用的元件。</p><ol><li>Bit Adder：输入一个 n 位二进制数，输出总共有几位 1。</li><li>Bit Finder：输入一个 n 位二进制数，输出最低位的 1 &#x2F; 最高位的 1 &#x2F; 最低位的 0 &#x2F; 最高位的 0 的 index，还有一个输出端口代表 n 位二进制数是否含有 1。</li><li>Shifter：输入一个数以及移动的位数，输出得到的结果。</li></ol><h2 id="rev"><a href="#rev" class="headerlink" title="rev"></a>rev</h2><p>题目：将 GPR[rt] 的高 index 位翻转后放到低位，剩下的位数顺序排列到高位。然后将结果写到 GPR[rt]</p><p>解：先将 GPR[rt] 用 <code>splitter</code> 整体翻转，再左移 + 右移得到 <code>0000...0 + 高 index 位反转结果</code>，和 GPR[rt] 左移得到的<code>剩余位数 + 000...000</code><br>相加就可以得到结果。总的来说就是位数转换都可以通过移位操作来实现。</p><h2 id="blz"><a href="#blz" class="headerlink" title="blz"></a>blz</h2><p>题目：大概是数出 GPR[rs]、GPR[rt] 的前导零数量，然后比大小决定 NPC 的操作，以及向 GRF 写入 tmp1 或 tmp2。</p><p>解：<strong>重点在数前导 0</strong>，用 <code>bit Finder</code> 时记得考虑没有 1 的情况。</p><h2 id="lwf"><a href="#lwf" class="headerlink" title="lwf"></a>lwf</h2><p>题目：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">lwf base rt offset<br>从offset(base)的内存中读出一个数据 <br>判断这个数和rt的and的各个数位之和的奇偶性 <br>奇数则存入(rt+1)mod32寄存器 <br>偶数则存入rt寄存器<br></code></pre></td></tr></table></figure><p>解：<strong>重点在奇偶比较</strong>，比较最低位即可。最低位为0是偶数，为1是奇数。</p><p><a href="https://github.com/Cordial-Kid/BUAA-CO/blob/main/P3/%E5%8D%95%E5%91%A8%E6%9C%9FCPU_in_class.circ">github仓库</a>里为大家提供了这三道题目的代码：</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">谁与我画明月天地间<br>落笔话呢喃<br>谁遇我南山望北川<br>扶摇向九天<br>谁予我谈笑伴云轩<br>知己醉桌前<br>任我清风眠<br>梦回世外仙<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> logisim </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P2</title>
      <link href="/2026/01/20/P2%E7%9A%84%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/"/>
      <url>/2026/01/20/P2%E7%9A%84%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>P2 主要是对 MIPS 指令集架构的理解，重点考察的也就是一些常见的指令，语法上不会像 Verilog 一样坑人，只要是 COpre 下了点功夫的问题都不大。P2 课下可能直接上手汇编代码比较轻松，但是 P2 在上机的时候一般会给出 C 语言代码，这个时候不管你看不看得懂，<strong>照着人肉汇编就对了</strong>，千万别自己乱创造。</p><h2 id="关于递归"><a href="#关于递归" class="headerlink" title="关于递归"></a>关于递归</h2><p>递归一般是考试的最后一题，我的建议是大家提前写好push和pop的宏，然后直接照着C代码翻译就行了。递归的关键点在于保存现场和恢复现场。但是教程中说的那个caller调用和callee调用我是真的不懂什么意思，反正我就是直接把所有用到的寄存器都push一遍，函数返回前全部pop回来就完事了，反正也没说要节省空间。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>细细想来，好像没什么好说的，大家可以去看笔者的 <a href="https://github.com/Cordial-Kid/BUAA-CO/tree/main/P2">GitHub 仓库</a>，里面有 P2 的上机测试题目和参考答案，大家可以参考学习。（尤其是 Factory，涉及大数乘法）</p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">后来我学会浪漫<br>学会打扮自己的脸<br>可距离我想讨你欢心<br>已过了很多年<br>你好像站在那段岁月看了我一眼<br>就把我永远困在遗憾里面<br>后来我偶尔也会<br>打听你在谁的身边<br>只是我再也不能冲动<br>跑去见你一面<br>想赢的那场比赛结束在很久以前<br>最好的你和最好的我之间<br>只能挥挥手告别<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> mips </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P1</title>
      <link href="/2026/01/20/P1%E7%9A%84%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/"/>
      <url>/2026/01/20/P1%E7%9A%84%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>P1 主要是对 Verilog 在预习阶段所学内容的强化，但是显然没有预习阶段的题目那么恶心。P1 的题目大致可以分为两类：一类是组合逻辑电路的设计，另一类是时序逻辑电路状态机的设计。下面分别进行说明。</p><h2 id="组合逻辑电路设计"><a href="#组合逻辑电路设计" class="headerlink" title="组合逻辑电路设计"></a>组合逻辑电路设计</h2><p>组合逻辑电路的设计相对简单，主要考察对 Verilog 语法的掌握以及对基本逻辑电路的理解。说白了就是程序设计换一种语言写而已，不会用很难的算法为难大家的。</p><h2 id="时序逻辑电路状态机设计"><a href="#时序逻辑电路状态机设计" class="headerlink" title="时序逻辑电路状态机设计"></a>时序逻辑电路状态机设计</h2><p>状态机的设计相对复杂一些，主要考察对状态机原理的理解以及 Verilog 实现状态机的能力。状态机的设计一般分为以下 5 个模块：</p><ol><li>变量定义模块</li><li>initial模块</li><li>时序逻辑状态转移模块</li><li>组合逻辑次态计算模块</li><li>组合逻辑输出计算模块</li></ol><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-comment">// 1. 变量定义模块</span><br><span class="hljs-keyword">reg</span> [n-<span class="hljs-number">1</span>:<span class="hljs-number">0</span>] state; <span class="hljs-comment">// 当前状态</span><br><span class="hljs-keyword">reg</span> [n-<span class="hljs-number">1</span>:<span class="hljs-number">0</span>] next_state; <span class="hljs-comment">// 下一个状态</span><br><br><span class="hljs-comment">// 2. initial模块</span><br><span class="hljs-keyword">initial</span> <span class="hljs-keyword">begin</span>  <br>    state = INITIAL_STATE; <span class="hljs-comment">// 初始化状态</span><br>    next_state = INITIAL_STATE; <span class="hljs-comment">// 初始化下一个状态</span><br><span class="hljs-keyword">end</span><br><br><span class="hljs-comment">// 3. 时序逻辑状态转移模块</span><br><span class="hljs-keyword">always</span> @(<span class="hljs-keyword">posedge</span> clk <span class="hljs-keyword">or</span> <span class="hljs-keyword">posedge</span> reset) <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">if</span> (reset) <span class="hljs-keyword">begin</span><br>        state &lt;= INITIAL_STATE; <span class="hljs-comment">// 复位状态</span><br>    <span class="hljs-keyword">end</span> <span class="hljs-keyword">else</span> <span class="hljs-keyword">begin</span><br>        state &lt;= next_state; <span class="hljs-comment">// 状态转移</span><br>    <span class="hljs-keyword">end</span><br><span class="hljs-keyword">end</span><br><br><span class="hljs-comment">// 4. 组合逻辑次态计算模块</span><br><span class="hljs-keyword">always</span> @(*) <span class="hljs-keyword">begin</span><br>    <span class="hljs-keyword">case</span> (state)<br>        STATE_0: <span class="hljs-keyword">begin</span><br>            <span class="hljs-keyword">if</span> (input_condition) <span class="hljs-keyword">begin</span><br>                next_state = STATE_1;<br>            <span class="hljs-keyword">end</span> <span class="hljs-keyword">else</span> <span class="hljs-keyword">begin</span><br>                next_state = STATE_0;<br>            <span class="hljs-keyword">end</span><br>        <span class="hljs-keyword">end</span><br>        STATE_1: <span class="hljs-keyword">begin</span><br>            <span class="hljs-comment">// 其他状态转移逻辑</span><br>        <span class="hljs-keyword">end</span><br>        <span class="hljs-comment">// 其他状态...</span><br>        <span class="hljs-keyword">default</span>: <span class="hljs-keyword">begin</span><br>            next_state = INITIAL_STATE; <span class="hljs-comment">// 默认状态</span><br>        <span class="hljs-keyword">end</span><br>    <span class="hljs-keyword">endcase</span><br><span class="hljs-keyword">end</span><br><br><span class="hljs-comment">// 5. 组合逻辑输出计算模块</span><br><span class="hljs-keyword">always</span> @(state) <span class="hljs-keyword">begin</span>   <br>    out = (state == TARGET_STATE) ? <span class="hljs-number">1&#x27;b1</span> : <span class="hljs-number">1&#x27;b0</span>; <span class="hljs-comment">// 输出逻辑</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>掌握了这个基本框架之后你的状态机就不会经常因为时序问题出bug了。同样的，状态机的设计也可以分为两种方法：一种是使用真值表表示状态机，另一种是使用常规逻辑表达式表示状态机。按照另一种分类方式，可以分为Moore型和Mealy型状态机设计。思路跟P0大同小异，再套用上述提到的模板即可。笔者帮大家整理好了对比代码，上传到了<a href="https://github.com/Cordial-Kid/BUAA-CO/tree/main/P1/Moore_and_Mealy">github仓库</a>，大家可以参考理解，重点关注一下组合逻辑输出计算模块的区别。下面我们重点讲一下状态机Plus版–一般用在<strong>string stream</strong>中，对应上机考试的最后一题。</p><h2 id="状态机的Plus版"><a href="#状态机的Plus版" class="headerlink" title="状态机的Plus版"></a>状态机的Plus版</h2><p>Plus版一般是指在一个模块中设计两个相互独立但又有联系的状态机。举个例子：</p><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">给定按时钟输入的 ASCII 字符流，其中包含若干个由 &quot;&#123;&quot; 与 &quot;&#125;&quot; 包围的简化 JSON 对象（对象之间由空格分隔）。每个键值对的键和值均用双引号包围 &quot;key&quot;:&quot;value&quot;。若对象中出现空字符串键或空字符串值（即 &quot;&quot;），则该对象非法，字段数记为 0。计算每个对象的字段数输出到 cur_num，并维护历史最大合法字段数输出到 max_num。<br></code></pre></td></tr></table></figure><p>这是一道上机考试回忆版题目，可能有的细节不甚详尽，还请大家多多包涵。</p><p>本题的解题思路是在一个模块中设计两个状态机：一个状态机用于维护”IDLE”、”READ”、”CHECK”三个状态，识别JSON对象的开始和结束，第二个状态机负责统计字段数并更新最大值。这里重点和难点是第二个状态机的设置。</p><p>我们用变量number1来统计<code>&quot;</code>的数量，用tmp_cnt来统计两个引号之间的字段数，用cur_num来记录当前对象的字段数，用max_num来记录历史最大字段数。当number1为偶数时，如果tmp_cnt为0，说明出现了空字符串键或空字符串值，此时cur_num置0；否则cur_num加1。每当遇到<code>&#125;</code>时，比较cur_num和max_num的大小，更新max_num。</p><p>详细代码实现参见个人GitHub仓库：<a href="https://github.com/Cordial-Kid/BUAA-CO/blob/main/P1/json.v">https://github.com/Cordial-Kid/BUAA-CO/blob/main/P1/json.v</a></p><h2 id="可能踩的坑"><a href="#可能踩的坑" class="headerlink" title="可能踩的坑"></a>可能踩的坑</h2><p>只有在组合逻辑电路中才能实现<code>sum = sum + i</code>这类型的语句，在时序逻辑电路中是不允许的。</p><figure class="highlight verilog"><table><tr><td class="code"><pre><code class="hljs verilog"><span class="hljs-keyword">always</span> @(*) <span class="hljs-keyword">begin</span><br>    sum = <span class="hljs-number">0</span>;    <span class="hljs-comment">//这一步很重要</span><br>    <span class="hljs-keyword">for</span>(i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">8</span>; i = i + <span class="hljs-number">1</span>) <span class="hljs-keyword">begin</span><br>        sum = sum + i;<br>    <span class="hljs-keyword">end</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">若不是你突然闯进 我生活<br>我怎会把死守的寂寞 放任了<br>爱我的话你都说<br>爱我的事你不做<br>我却把甜言蜜语<br>当作你爱我的躯壳<br>你的悲伤难过我不参破<br>我也会把曾今的且过 当施舍<br>不去计较你太多<br>从此你在我心里<br>只剩绿色<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> verilog </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 P0</title>
      <link href="/2026/01/20/P0%E7%9A%84%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/"/>
      <url>/2026/01/20/P0%E7%9A%84%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>为什么没有pre？因为pre的题对于我这样的小白来说还是太难了，而且后续也不会重点考察，个人认为没有什么参考价值。</p><p>P0 是对预习过程中的 Logisim 的强化练习。我觉得可以大概分成两个部分。一部分是执行自定义指令，例如画一个电路图实现一个 32 位二进制数的某几位反转。另一部分是状态机的搭建。下面分两部分陈述。</p><h2 id="自定义指令"><a href="#自定义指令" class="headerlink" title="自定义指令"></a>自定义指令</h2><p>自定义指令的题目其实并不难，主要是考察你对 Logisim 中各种元件的使用。个人觉得比较重要的几个元件有：</p><ul><li>多路选择器（MUX）</li><li>Splitter</li><li>Bit Extender</li><li>组合逻辑门（AND, OR, NOT, XOR等）</li><li>加法器（Adder）</li><li>Bit Selector</li><li>Comparator</li></ul><p>以上这些元件在预习过程中应当做到较为熟练地掌握。虽然并不全面，但是足以让你应对P0中的自定义指令题目。在做题时看清题意，按照题目要求一步一步设计即可，不需要过于复杂的设计思路。</p><h2 id="状态机设计"><a href="#状态机设计" class="headerlink" title="状态机设计"></a>状态机设计</h2><p>状态机的设计有相对固定的套路，只要熟悉了套路，状态机简直就在送分。首先要明确的是，状态机分为<strong>Moore 型</strong>和<strong>Mealy 型</strong>两种。<strong>Moore 型</strong>状态机的输出仅与当前状态有关，而<strong>Mealy 型</strong>状态机的输出与当前状态和输入均有关。同一个问题两种设计方式都可以，但是在设计时要根据题目要求选择合适的状态机类型。</p><p>理论上来说所有的状态机类型题目都可以用真值表来解决，但是对于某些状态较多的题目来说，真值表会变得非常庞大，不易处理。此时我们可以用<strong>正常的逻辑</strong>来表示现态到次态的转化以及输出的生成。</p><p>同时，电路图大致可分为4个模块。</p><ol><li>Tunnel Module：负责接收输入信号并进行初步处理。</li><li>calculate next state Module：负责根据当前状态和输入信号计算下一个状态。</li><li>state storage Module：负责存储当前状态，通常使用Register实现。</li><li>output Module：负责根据当前状态和输入信号生成输出信号。</li></ol><p>我们以经典的推箱子问题为例说明：大致要求就是从初始状态出发，通过一系列操作将箱子推到目标位置。途中如果碰到障碍物或越界就算失败。</p><p><img src="/images/%E6%8E%A8%E7%AE%B1%E5%AD%90%E7%94%B5%E8%B7%AF%E5%9B%BE.png" alt="推箱子电路图"></p><h3 id="可用真值表表示的状态机"><a href="#可用真值表表示的状态机" class="headerlink" title="可用真值表表示的状态机"></a>可用真值表表示的状态机</h3><p>设计时一般遵循以下步骤：</p><ol><li><strong>明确状态及其编码</strong>：这里要根据题目要求确定状态的数量，并为每个状态分配一个唯一的二进制编码。</li><li><strong>绘制状态转移图</strong>：根据题目描述，绘制状态转移图，明确各状态之间的转移关系以及对应的输入条件。</li><li><strong>编写状态转移表</strong>：将状态转移图转换为状态转移表，列出每个状态在不同输入条件下的下一个状态。</li><li><strong>设计逻辑电路</strong>：根据状态转移表，设计组合逻辑电路来实现<strong>状态转移</strong>和<strong>输出</strong>逻辑。这里可以直接利用logisim内置的真值表工具来实现组合电路。具体步骤如下：</li></ol><p><code>project</code> –&gt; <code>Analyze Circuit</code> –&gt; <code>Input &amp; Output &amp; Table</code> –&gt; <code>填写输入输出变量及真值表</code> –&gt; <code>Generate Circuit</code></p><p>典型例子有：红绿灯控制器。</p><p><img src="/images/%E7%BA%A2%E7%BB%BF%E7%81%AF%E6%AC%A1%E6%80%81%E9%80%BB%E8%BE%91%E8%AE%A1%E7%AE%97%E5%9B%BE.png" alt="红绿灯次态逻辑计算图"><br><img src="/images/%E7%BA%A2%E7%BB%BF%E7%81%AF%E6%AC%A1%E6%80%81%E9%80%BB%E8%BE%91%E7%9C%9F%E5%80%BC%E8%A1%A8.png" alt="红绿灯次态逻辑真值表"><br>输出逻辑计算同理，在此不再赘述。</p><h3 id="常规逻辑表达式表示的状态机"><a href="#常规逻辑表达式表示的状态机" class="headerlink" title="常规逻辑表达式表示的状态机"></a>常规逻辑表达式表示的状态机</h3><p>设计不便于使用真值表的状态机时，就用最原始的，最正常的逻辑思路来计算下一状态即可。</p><p>典型例子有：推箱子。</p><p><img src="/images/%E6%8E%A8%E7%AE%B1%E5%AD%90%E6%AC%A1%E6%80%81%E9%80%BB%E8%BE%91%E8%AE%A1%E7%AE%97%E5%9B%BE.png" alt="推箱子次态逻辑计算图"></p><p>要注意的是次态逻辑的存储时要对次态逻辑的有效性进行判断，例如推箱子中如果出现了碰墙或者越界的情况，次态就不更新，保持不变。这就需要用到多路选择器来选择下一个<code>x</code>到底是<code>x_next</code>还是<code>x_current</code>。</p><h3 id="关于Moore型和Mealy型"><a href="#关于Moore型和Mealy型" class="headerlink" title="关于Moore型和Mealy型"></a>关于Moore型和Mealy型</h3><p><strong>Moore型</strong>和<strong>Mealy型</strong>的状态转移图有很大区别，因为<strong>Mealy型</strong>状态机输出不仅和当前状态有关还和输入有关，因此<strong>Mealy型</strong>状态机中的状态通常比<strong>Moore型</strong>状态机少一个（少的一般是<strong>Moore型</strong>状态机的最后一个状态）。这在真值表中体现为输入影响输出，在状态转移图中体现为输出从<strong>在方框内表示</strong>转化为<strong>在边上表示</strong>。</p><p>如果你使用的是<strong>真值表表示</strong>方法，那么要修改<code>次态转移逻辑</code>和<code>输出逻辑</code>的真值表。次态转移逻辑减少状态，输出逻辑增加<code>input signal</code>作为输入变量。</p><p>如果你使用的是<strong>常规逻辑表达式</strong>的方法，那么不要动<code>次态逻辑计算</code>的设计，只需要修改<code>输出逻辑</code>的设计即可。输出逻辑要使用<code>next state</code>（注意不是<code>current state</code>,也没有<code>input signal</code>）来作为该板块的输入计算输出。</p><h2 id="一些可能的盲区"><a href="#一些可能的盲区" class="headerlink" title="一些可能的盲区"></a>一些可能的盲区</h2><ol><li><strong>异步复位和同步复位</strong>：异步复位是指复位信号一旦被触发，无论时钟信号的状态如何，电路都会立即进入复位状态。而同步复位则是指复位信号只有在时钟信号的上升沿（或下降沿）到来时才会生效。在设计状态机时要根据题目要求选择合适的复位方式。他们的实现方式也不尽相同。</li></ol><p><img src="/images/%E5%BC%82%E6%AD%A5%E5%A4%8D%E4%BD%8D.png" alt="异步复位"><br><img src="/images/%E5%90%8C%E6%AD%A5%E5%A4%8D%E4%BD%8D.png" alt="同步复位"></p><ol start="2"><li><strong>“异步”置位和同步置位</strong>：同步置位则是指置位信号只有在时钟信号的上升沿（或下降沿）到来时才会生效。而“异步”置位跟字面意思不太一样，我们要求<code>reset</code>信号在<code>clk</code>信号为低电平时拉高，在<code>clk</code>信号高电平时接地，同时要求<code>clk</code>信号为高电平时能够在<code>register</code>中存储<code>reset</code>信号为高电平时希望设置的值。同步置位的实现方式和同步复位的方式类似，但是“异步”置位的实现就和异步复位大不相同了。</li></ol><p>因为<code>reset</code>信号的转瞬即逝，我们希望有某个模块可以留住<code>reset</code>的效果，<code>Counter</code>成为了最佳选项。电路连接如下图：</p><p><img src="/images/%E2%80%9C%E5%BC%82%E6%AD%A5%E2%80%9D%E7%BD%AE%E4%BD%8D.png" alt="“异步”置位"></p><p>我们将<code>Counter</code>模块的<code>Data bits</code>设置为1位，<code>Action on overflow</code>设置为<code>Stay at value</code>,再将<code>cnt</code>作为选择信号来选择存入<code>register</code>的数据来源。我们分析可以得出，当<code>reset</code>为低电平时，除了初态，所有的状态<code>cnt</code>均为1。当<code>reset</code>为高电平时，<code>cnt</code>变为0。即使此时<code>reset</code>信号转为低电平，<code>cnt</code>依然保持0。因此在clk上升沿时，register模块存入的数据始终为<strong>x</strong>，从而实现了“异步”置位的效果。</p><p>同步置位就很简单了，如下图：<br><img src="/images/%E5%90%8C%E6%AD%A5%E7%BD%AE%E4%BD%8D.png" alt="同步置位"></p><ol start="3"><li>多个数比大小</li></ol><p>在设计多个数比大小的逻辑时，可以使用多个<code>Comparator</code>模块进行两两比较，然后将比较结果进行综合处理。这里需要用到分类讨论的知识。分类讨论的结果可以可以作为多路选择器的待选择信号，比较结果可以作为选择信号，从而实现不同情况下的输出。</p><p><img src="/images/%E4%B8%89%E4%B8%AA%E6%95%B0%E6%AF%94%E8%BE%83%E5%A4%A7%E5%B0%8F.png" alt="三个数比较大小"></p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">你身边的那位<br>代替了我的座位<br>他的手捧着玫瑰<br>承诺余生都奉陪<br>而我始终学不会<br>已被风吹散的眼泪<br>那逝去的幸福该怎么追回<br>你身边的那位<br>代替了我的称谓<br>能懂你真心多贵<br>不让你情绪破碎<br>而我就应该后退<br>藏进这回忆里昏睡<br>往后的时光请你勇敢地飞<br>勇敢地飞...<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
            <tag> logisim </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>「BUAA-CO」 序言</title>
      <link href="/2026/01/20/%E8%AE%A1%E7%BB%84%E5%BA%8F%E8%A8%80/"/>
      <url>/2026/01/20/%E8%AE%A1%E7%BB%84%E5%BA%8F%E8%A8%80/</url>
      
        <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>计算机组成原理，简称<code>CO</code>，是计算机学院在大二上学期开设的一门<code>5.5</code>学分的核心专业课，也是同学们来到计算机学院遇到的第一个拦路虎。课程分为理论和实验两部分，理论部分考察重点大致为以下几个知识点：<code>组合逻辑</code>，<code>时序逻辑</code>，<code>主存储器</code>，<code>cache</code>，<code>TLB</code>，<code>流水线 CPU</code>，<code>MIPS 代码补全</code>。实验部分也是 CO 最困难的部分，但它其实没有传说中的那么恐怖，唯一在干的事情就是设计 CPU，从<code>Logisim</code>到<code>Verilog</code>，从单周期到流水线，只要诸位好好理解，相信征服 CO 并不是什么难事。<strong>实验最后的给分和实验截止时你所处在的 Project 有关</strong>，不出意外的话有三次 gap 机会，大家要好好珍惜哟。笔者接下来的系列 Blog 将围绕 P0-P7 展开，希望能对各位有所帮助。</p><h2 id="为什么写"><a href="#为什么写" class="headerlink" title="为什么写"></a>为什么写</h2><p>作为一个初入计算机学院懵懂的学生，笔者在初步上手的时候也遇到了很多困难：看不懂大佬学长的博客，听不懂老师上课讲的流水线设计思路，语法学习无从下手……这个时候的那种绝望可能大家只有经历了才知道。好在我遇到了热心友爱的同学、室友，如果没有他们的帮助，我不可能顺利通过 CO。本着<strong>因为淋过雨，所以也想给学弟学妹撑把伞的热忱</strong>，我创作了系列 Blog，该 CO 系列将尽可能使用通俗的语言来讲解晦涩的知识点，细节也会尽可能覆盖到，如果大佬阅读到该博客觉得写得太繁琐的话可以去看其他佬的作品，如果能阅读之后发现相关错误之处请及时联系我，笔者感激不尽🫡。</p><h2 id="CO学习方法的一些个人建议"><a href="#CO学习方法的一些个人建议" class="headerlink" title="CO学习方法的一些个人建议"></a>CO学习方法的一些个人建议</h2><ul><li>CO 学习基本盘当然是在 COpre 阶段掌握好<code>Verilog 语法</code>（重中之重）</li><li>在设计过程中，如果你足够牛，那么自己设计是最好的；如果你喜欢看书，可以跟着书上的指示设计（没干过，但是舍友是这样的，大家可以尝试）；更推荐的方法是从<code>P3</code>开始跟着一个学长的<code>Blog</code>和<code>GitHub 仓库</code>，沿着他的思路设计下去，在设计的过程中做出某些属于自己的优化和改动（千万别纯抄一遍下来），形成自己对流水线的理解。</li><li>在看博客时最好结合 GitHub 的代码一起看，避免不必要的 bug。</li><li>尽量在周中也完成一点工作，不要全压在周末，压力有点大的样子。</li><li>用好测评机，这是一个23级学长写的：<a href="https://github.com/Kie-Chi/COT%E3%80%82">https://github.com/Kie-Chi/COT。</a></li></ul><h2 id="不得不提的事情"><a href="#不得不提的事情" class="headerlink" title="不得不提的事情"></a>不得不提的事情</h2><p>大二上学期有两个比较重要的竞赛，当竞赛撞上闯关制的CO就会擦出不一样的火花，在这里给大家提个醒。</p><ul><li>12.5，<strong>全国部分地区大学生物理竞赛</strong>，这个只要你考前准备，做一点往年题，混个三等奖，你的物理就满分了，真的超值！！！但是可惜的是它正好在周末撞上了 CO 的 P5&#x2F;P6，所以这就体现了在周中推进计组实验的重要性，或者你可以早点开始复习😁。</li><li>12.13，<strong>英语六级</strong>，有很大概率会撞上你的P7，大家有必要提前复习一下。</li></ul><p>如果你在暑假预习了计组并且碰到不会设计的问题并且无人可问，我的建议是跳过（别全跳过了哈），它不会对你开学正式的Project产生什么致命的影响。如果你在暑假<strong>没有</strong>预习计组，No Problem，开学还有时间，足够你翻盘。</p><p>个人感觉P0-P2比后面的P要难，在这几个Project你经常会被打信息差（很多别人会但是你不会的东西），其实他们都有相对固定的套路，就那么点东西，只要掌握了套路，你就可以在前3P轻车熟路了。可惜我是每次被打了信息差才学到的，痛苦了好久，悲！Let me tell you now😋。</p><h2 id="放心，别慌"><a href="#放心，别慌" class="headerlink" title="放心，别慌"></a>放心，别慌</h2><p>很多同学在刚开学的时候是对CO充满畏惧的，在这里跟大家说一下个人感觉。CO实验占比是40%，如果你全部做完的话和AK佬分数是不会差太多的，1-2分的样子。只要你理论不拉跨，应该分数在95分上下浮动。如果你出了点小意外卡在了P5，也别担心，不会挂科的，只要你好好准备理论考试，应该75到85问题不大。</p><p><strong>挂P不可怕，可怕的是就此丧失信心</strong>，认识一个哥们P3先挂三次展示容错，最后也顺利通关了。别怕，轻装上阵吧，加油！</p><p>你准备好了吗，让我们开启这段精彩的旅程吧。</p><p><strong>go go go !出发喽！……</strong></p><h2 id="Lyrics-Sharing"><a href="#Lyrics-Sharing" class="headerlink" title="Lyrics Sharing"></a>Lyrics Sharing</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">你的酒馆对我打了烊<br>子弹在我心头上了膛<br>请告诉我今后怎么扛<br>遍体鳞伤<br>还笑着原谅<br>你的酒馆对我打了烊<br>承诺是小孩子说的谎<br>请告诉我今后怎么扛<br>你无关痛痒<br></code></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 计算机组成 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BUAA </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2025/11/19/hello-world/"/>
      <url>/2025/11/19/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your first post. Check the <a href="https://hexo.io/docs/">documentation</a> for more info. If you run into problems while using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
